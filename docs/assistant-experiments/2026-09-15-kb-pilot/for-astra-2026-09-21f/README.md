# Reviewer pack — corrected again (sixth pack). No new model calls.

All four points accepted. Three of them make my previous report wrong.

1. **The cross-reference defect is bigger than I said: 13, not 7.** Your eighth case (Technical p.10
   §1.1.4.4 "Go to the 'Certificates' sub-sub module") exposed that I had counted with one regex for
   one phrasing. I audited all 41 with a pattern carrying a **self-test on known cases** — which
   immediately caught a second bug of my own (the manuals use curly quotes; an intermediate version
   reported 4). Audited: **Crewing 6** (Recruited/Waitlist/Rejected each told to open In-Progress, on
   Edit/Delete and Export), **Stores → Spares 6**, **Surveys → Certificates 1**. The other 28 begin at
   a button or field and carry no navigation. `XREF-AUDIT.txt` lists them.
   **Not all are defects.** The Crewing six look clearly wrong. The Stores/Spares and
   Surveys/Certificates cases may be genuine screen switches — that is a product question for Jeevan,
   not something to infer from the manual, so nothing is proposed for them yet.
2. **The Risk Assessment gain is RETRACTED.** All three baseline runs contain "the available filter
   options vary by section"; I had read only the first 175 characters. That also invalidated the method
   behind my "24/24", so **all 24 answers have been re-read in full**.
3. **"Unused on 22 questions" → "not explicitly cited".** Whether the added text influenced wording
   silently is not established either way.
4. **"24/24 on both arms" corrected.** On the standard you set — evidence for the question actually
   asked — the arms are not equal: **candidate 24/24, baseline 22/24** (`hist-1` answers the wrong
   feature 3/3; `pmsoffice-5` makes the Dashboard-vs-vessel-Reports comparison in runs 1 and 2).

The one remaining difference (`pmsoffice-5`, candidate states the limit 3/3 vs baseline 1/3) is
reported **weakly on purpose**: one question, three runs, no causal link to the change, may be sampling.

Your independent match of ordered document names, sections and pages across all 204 responses is
recorded as corroboration of the reconstruction, with the caveat that it is still not a capture of the
actual model request.

**Recommendation accepted:** keep the retrieval candidate; cross-reference repair is the next bounded
task on an isolated index, preserving source wording and citations and separating reused steps from
real navigation. **Deployment on hold. Live and all other sites untouched.**
