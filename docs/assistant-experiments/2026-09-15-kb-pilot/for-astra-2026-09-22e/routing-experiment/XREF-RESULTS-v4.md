# Cross-reference repair — fourth comparison: the generation bug fixed, input captured and traced

22-Sep-2026. **Nothing deployed. Live, nginx, SMS RAG and every other running service untouched.**
Arms on 127.0.0.1 only: g1 `kb-pilot-e` (baseline) · g3 `kb-xref-b` (audit-copy layout) ·
g4 `kb-xref-c` (quote kept out of the text; outgoing context captured).

## 1. The reviewer's four instructions

**1. Fix the filter-list adaptation, including parentheses; check all 41 adapted sections.**
Reproduced: rule 2 detected a parenthesised list but only rewrote lists introduced by "such as"/"by",
so *"dropdown filters (Criticality, Rotation Item, or Stock)"* survived **inside my own adapted Stores
block** while the same block said those filters do not exist — contradictory guidance, generated by
us. Fixed (parenthesised lists are rewritten to the verified list or dropped with the omission
reported); pinned by a test that feeds the exact sentence. `XREF_VERSION` → `2026-09-22.3`.
Scan of all 41 adapted blocks in the built index, before the fix: **exactly one** carried a wrong list
(Technical p.43, the Vessel manual's Stores filter 1.1.8.2); Promotion Rank, Issue Date, "annual" and
wrong-screen navigation: 0 each. After the rebuild: **0 of 41**.

**My claim that the remaining problem "cannot be fixed in the documents" is retracted.** The capture
I cited contained my own defect. Per-leak tracing is now mandatory before any mechanism is named
(`trace_leaks.py`, validated on the previous capture, where it attributes those six occurrences to
the continuation of my adapted chunk — the reviewer's finding, reproduced by the tool).

**2. Correct the three false failures using the stored answers; preserve the original scores.**
All three were denials scored as instructions: *"does **not** use the Spares-only Criticality or
Rotation Item filters"*, *"do **not** exist on the Stores screen"*, *"Issue Date does not apply to
Surveys"*. Two causes: the denial pattern ran on the raw sentence (bold inside "does **not** use") and
lacked "does not use / apply / establish". Fixed; the three sentences are pinned in the self-test, and a
bare bullet offering the field (`- **Criticality**`) is pinned as an instruction. Published scores are
preserved next to the corrected ones:

| captured run (v3 pack) | published | corrected |
|---|---|---|
| baseline | 6 / 0 / 15 | 6 / 0 / 15 |
| audit-copy | 16 / 3 / 2 | **18 / 3 / 0** |
| quote-excluded | 16 / 3 / 2 | **17 / 3 / 1** — the one left is the answer that reports the contradiction present in its input (content defect above) |

**3. Rebuild cache-only, capture the actual input, repeat the bounded test.** Done: `kb-xref-c`
rebuilt with the fixed rule — 951 chunks = **947 vectors reused + 4 new, 0 LlamaParse uploads, 0
aborts, 0 documents skipped** (a first attempt silently skipped all 25 on an unchanged build key; the
version bump fixed it). Outgoing context captured for all 21 g4 requests (`capture-g4-run3.jsonl`).

**4. Hold source-section suppression until a failure is demonstrably caused by separately retrieved
source text.** Held — and the evidence now says it is not needed. See §3.

## 2. The rerun — 7 frozen cases × 3 arms × 3 runs = 63 answers, field-level rules

```
                        baseline        audit-copy (b)     quote-excluded (c)
waitlist-export         inc inc inc     inc* cor cor       cor cor cor
monthly-test-create     cor cor cor     cor cor cor        cor cor cor
appraisals-filter       inc inc inc     lim lim lim        lim lim lim
stores-export           inc inc inc     cor cor cor        cor cor cor
stores-filter           inc inc inc     cor cor cor        cor cor cor
surveys-edit            inc inc inc     cor cor cor        cor cor cor
audit-history           cor cor cor     cor cor cor        cor cor cor

mechanical   baseline 6 / 0 / 15     audit-copy 17 / 3 / 1     quote-excluded 18 / 3 / 0
```

**\* the single mechanical failure, read:** *"the manual notes that the sequence is transferable, while
the referenced screen details describe In Progress."* The steps above it say Waitlist. This is the
**provenance** defect (my indexing note attributed to the manual), not a wrong-screen instruction. I
have left it scored *incorrect* rather than widen the discount rules a third time; the judge flags it
as PROVENANCE separately.

**On reading, zero wrong-screen or wrong-field instructions remain on either repaired arm.**
Appraisals stays *limited* by the cap. Audit History holds 3/3 on every arm.

Provenance flags this run: audit-copy 1 (above), quote-excluded 0.

## 3. What actually reached the model — traced per excerpt

`trace_leaks.py` on the 21 captured g4 requests, every source term attributed to its excerpt and to
its position within it:

| where the term sat | occurrences |
|---|---:|
| **our adapted block** (the content defect) | **0** (was 6 in the previous build) |
| our adapted block, inside a denial / indexing note — not an instruction | 15 |
| the manual's own pointer sentence ("Refer to the ‘In-Progress’ sub-sub-module … same steps") | 12 |
| **a source section retrieved on its own** — 1.2.1.5 In-Progress (Waitlist, 3 runs), 1.6.1.2 Crew Promotion (Appraisals, 3 runs), 1.1.3.4 Certificates (Surveys, 3 runs) | **15** |

So the source sections *do* reach the model alongside the adapted chunk, in every run of those three
cases — and **none of those requests produced a wrong instruction**: Waitlist 3/3 correct, Surveys 3/3
correct, Appraisals limited by cap with no Promotion Rank in any answer. The adapted steps win when
they are not contradicted by our own text. **No remaining failure is caused by separately retrieved
source text; suppression stays unbuilt.**

## 4. Judge amendments made after seeing answers — all disclosed

Denial forms added ("does not use/apply/establish", "not to exist", "stated not to"); both patterns
applied to the normalised sentence; PROVENANCE tightened to explicit "the manual notes/states … that
…" then widened for "transferable / screen details describe"; every added case is pinned in the
self-test with a sentence that must match and one that must not. The `must_not` terms themselves were
never changed. Mechanical and read verdicts are reported side by side wherever they differ.

## 5. Still owed

- The LlamaParse charge from the earlier `--dry-run` (~12–16 agentic jobs, ~05:00–05:25 UTC): only the
  LlamaCloud usage page can show it.
- The Crewing field lists remain unverified; Appraisals cannot rise above *limited*.
- Provenance misattribution persists at a low rate (1 of 42 repaired answers this run) despite the
  explicit label. A prompt-side instruction may be the next lever; not tested.
