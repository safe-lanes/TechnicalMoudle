Fix Running Hours History Feature

text
CRITICAL ISSUES TO FIX IN RUNNING HOURS HISTORY:

ISSUE 1: Missing Component Name Column
Currently the history table shows only "Component Code" (702.005.01, 702.005.02) but is missing the "Component Name" column.

FIX REQUIRED:
- Add "Component Name" column to the history table
- Display full component name (e.g., "FO PURIFIER, NO.1", "MAIN DIESEL ENGINE COMPLETE")
- Place it right after "Component Code" column
- Fetch component name from the components table using component_id/code

ISSUE 2: Incorrect UI Flow - Missing Component Selection Screen
CURRENT (WRONG):
- History button directly shows all components' history mixed together in one table
- User cannot select which specific component's history to view

REQUIRED (CORRECT):
The Running Hours History should follow the EXACT SAME two-step flow as the main Running Hours Overview:

STEP 1 - COMPONENT LIST SCREEN:
When user clicks "History" button, show a screen with:
- Same table structure as the main "Overview" tab
- Show all components with columns:
  * Component Name (e.g., "FO PURIFIER, NO.1")
  * Component Code (e.g., "702.005.01")
  * Component Category (e.g., "Ship General")
  * Current Running Hours
  * Last Updated date
  * A "View History" button/icon in the Actions column for each component

STEP 2 - INDIVIDUAL COMPONENT HISTORY:
When user clicks on any specific component (or "View History" button):
- Navigate to detailed history page for THAT component only
- Show tabular history with columns:
  * Date/Time (when RH was updated)
  * Component Code
  * Component Name ← ADD THIS (currently missing)
  * Previous RH (running hours before update)
  * New RH (running hours after update)
  * Change (difference, with +/- indicator and color coding)
  * Updated By (user name/rank)
  * Source (system/manual/CASCADE)
  
REQUIRED FEATURES FOR COMPONENT HISTORY TABLE:
- Pagination (10/25/50/100 items per page)
- Export CSV functionality
- Date range filter (from date - to date)
- Sort by date ascending/descending
- Search functionality
- Show only that specific component's history (filtered by component_id)
- Back button to return to component list

DATABASE QUERY FIX:
Current query likely missing JOIN to get component name:
```sql
-- ADD THIS JOIN to get component name
SELECT 
  rh_audit.date_updated_local,
  rh_audit.component_code,
  components.component_name,  -- ← ADD THIS
  rh_audit.previous_rh,
  rh_audit.running_hours as new_rh,
  (rh_audit.running_hours - rh_audit.previous_rh) as change,
  users.rank as updated_by,
  rh_audit.source
FROM running_hours_audit rh_audit
LEFT JOIN components ON components.code = rh_audit.component_code
LEFT JOIN users ON users.id = rh_audit.updated_by_user_id
WHERE rh_audit.component_id = :selectedComponentId
ORDER BY rh_audit.date_updated_local DESC
REFERENCE IMPLEMENTATIONS:

Check how main Running Hours "Overview" tab displays component list

Copy that exact table structure for the History component list screen

For individual history table, reference Spares/Stores history table styling

Maintain consistent UI components, colors, fonts, spacing

IMPLEMENTATION CHECKLIST:
□ Add Component Name column to history table (with proper JOIN)
□ Create component selection list screen (copy from Overview tab structure)
□ Add "View History" action button for each component
□ Implement navigation: Component List → Individual Component History
□ Add back button to return to component list
□ Filter history by selected component_id
□ Add pagination, export, filters matching Spares/Stores pattern
□ Test with multiple components to ensure filtering works
□ Verify component name displays correctly for all entries

FILES TO MODIFY:

Frontend: client/src/pages/pms/RunningHoursHistory.tsx (or similar)

Backend: server/modules/running-hours/services/runningHoursService.ts

Backend: server/modules/running-hours/repositories/runningHoursRepository.ts

Add proper TypeScript interfaces for component selection and history data

CRITICAL: Do NOT show all components' history mixed together. Each component's history should be viewed separately after selecting that component from the list.