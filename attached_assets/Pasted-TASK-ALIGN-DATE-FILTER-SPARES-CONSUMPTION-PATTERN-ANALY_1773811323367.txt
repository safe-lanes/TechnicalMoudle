TASK: ALIGN DATE FILTER – SPARES CONSUMPTION PATTERN ANALYSIS WITH INVENTORY SPARES

Requirement:

In the ** Reports >Inventory → Spares (Source Date Filter) → Spares Consumption Pattern Analysis page**, the **Date Filter** must match the same structure, functionality, and selected values as used in the **Inventory → Spares screen**.

Reference:

• Screenshot 1 – Reports >Inventory → Spares (Source Date Filter)  
• Screenshot 2 – Reports >Inventory → Spares (Source Date Filter) → Spares Consumption Pattern Analysis (Target Location)

--------------------------------------------------

1. Current Issue

• The Date Filter in the Reports screen is not aligned with the Date Filter used in the Inventory → Spares screen.

--------------------------------------------------

2. Required Change

• Replicate the **same Date Filter UI and functionality** from Inventory → Spares screen to the Reports → Spares Consumption Pattern Analysis page.

--------------------------------------------------

3. UI Alignment

Ensure the following match exactly:

• Date filter type (From Date – To Date / Range Selector)  
• Date picker style  
• Default values  
• Placement and alignment  
• Field size and spacing  

--------------------------------------------------

4. Functional Alignment

• The selected date range in the Reports screen must behave exactly like the Inventory → Spares screen.
• Filtering logic must be consistent for both screens.

--------------------------------------------------

5. Data Synchronization

• If a date range is selected in Inventory → Spares and navigated to Reports, the same date filter values should be reflected (if applicable).

--------------------------------------------------

6. Implementation

• Reuse the same date filter component or logic used in Inventory → Spares.
• Avoid creating a separate or inconsistent date filter.

--------------------------------------------------

Expected Result

• Reports → Spares Consumption Pattern Analysis page has the same Date Filter as Inventory → Spares screen  
• UI and functionality are consistent across both screens  
• Date-based filtering behaves identically
