TASK: INVESTIGATE AND FIX RUNNING HOUR BASED WORK ORDER DUE DATE CALCULATION

BACKGROUND:
The PMS system has two types of maintenance jobs:
1. Calendar-based (Weekly, Monthly, Yearly, etc.) - due dates based on time intervals
2. Running Hours based - due based on equipment running hours (e.g., every 500 hours)

CURRENT PROBLEM:
Running hour based work orders may be incorrectly calculating next due DATES (calendar dates) when they should only track next due RUNNING HOURS. This causes the system to incorrectly show these jobs as "Due" or "Overdue" based on calendar dates rather than actual equipment running hours.

STEP 1: INVESTIGATE CURRENT LOGIC
=====================================

Please search your codebase and identify:

1. **Work Order Completion Logic**: 
   - Find the code that runs when Part B (Work Completion Record) is saved
   - Location: Likely in `/api/work-orders/:id/complete` or similar endpoint
   - Show me the code that handles work order completion

2. **Next Work Order Generation**:
   - Find where new work orders are created after completing a running hour based job
   - Search for: "Running Hours", "maintenanceBasis", "frequency"
   - Show me how the next due date/RH is calculated

3. **Due Date Calculation**:
   - Find where running hour jobs get assigned a calendar due date
   - Check if there's logic that estimates due date based on running hours
   - Search for: "dueDate", "nextDueRH", "nextDue"

4. **Work Order Status Logic**:
   - Find where work orders are marked as "Due" or "Overdue"
   - Check if this logic differentiates between calendar-based and running hour based jobs
   - Location: Likely in work order query/filter logic

STEP 2: IDENTIFY THE ISSUE
=====================================

Answer these questions by examining the code:

Q1: When a running hour based work order is completed:
   - Does it create a new work order with nextDueRH = currentReading + frequency?
   - Does it also assign a calendar dueDate to that new work order?
   - If yes, HOW is that calendar dueDate calculated?

Q2: In the work orders list (/pms/work-orders):
   - Are running hour based jobs shown in "Due" or "Overdue" tabs?
   - What logic determines if a running hour job is "due"?
   - Is it comparing calendar dates OR running hours?

Q3: What fields exist in the WorkOrder model for running hour tracking:
   - nextDueRH (next due running hours)?
   - currentRH (current running hours)?
   - dueDate (calendar date)?
   - maintenanceBasis (to identify if it's "Running Hours" vs "Calendar")?

STEP 3: EXPECTED CORRECT BEHAVIOR
=====================================

**For CALENDAR-BASED jobs (Weekly, Monthly, etc.):**
✅ Next due date = Completion date + interval
✅ Show in "Due" when current date >= due date
✅ Show in "Overdue" when current date > due date + grace period

**For RUNNING HOUR BASED jobs:**
✅ Next due RH = Current reading at completion + frequency
✅ Due date field should be NULL or blank (not calculated)
✅ Show in "Due" when current component RH >= next due RH
✅ Show in "Overdue" when current component RH > next due RH + tolerance
❌ Should NOT appear in "Due/Overdue" based on calendar dates

STEP 4: REQUIRED FIX
=====================================

If running hour based jobs are incorrectly using calendar due dates, implement this fix:

**A. Modify Work Order Completion Logic:**

When completing a running hour based work order:

```javascript
// After Part B is saved
if (workOrder.maintenanceBasis === "Running Hours") {
  const currentReading = partB.runningHours.currentReading;
  const frequency = workOrder.frequency;
  
  // Create next work order
  const nextWorkOrder = await WorkOrder.create({
    ...workOrder, // copy template details
    nextDueRH: currentReading + frequency,
    dueDate: null, // DO NOT set calendar due date
    status: "Planned",
    lastCompletedRH: currentReading,
    lastCompletedDate: new Date()
  });
  
  // Mark current as completed
  workOrder.status = "Completed";
  await workOrder.save();
}
B. Modify Work Order Filtering Logic:

Update the "Due" and "Overdue" filters to exclude running hour based jobs:

javascript
// Get Due work orders
const dueWorkOrders = await WorkOrder.find({
  $or: [
    {
      // Calendar-based jobs
      maintenanceBasis: { $ne: "Running Hours" },
      dueDate: { $lte: new Date() },
      status: "Planned"
    },
    {
      // Running hour based jobs - compare against component running hours
      maintenanceBasis: "Running Hours",
      status: "Planned",
      $expr: { 
        $gte: ["$component.currentRunningHours", "$nextDueRH"] 
      }
    }
  ]
});
C. Add Running Hour Tracking:

Ensure components have current running hours tracked:

javascript
// Component model should have:
{
  currentRunningHours: Number,
  lastRunningHoursUpdate: Date
}

// Update this when running hours are recorded in Part B
D. Dashboard Display:

Add a separate section or indicator for running hour based jobs:

"Due (Running Hours)" - separate from calendar due

Show "Next Due: 6500 RH (Current: 6150 RH)"

Don't show calendar due date for these jobs

STEP 5: PROVIDE OUTPUT
Please provide:

Current code snippets showing how running hour jobs are handled now

Confirmation if the issue exists (calendar due dates being set for RH jobs)

Modified code with the fix implemented

Test results showing:

A running hour job completed at 6150 hours with 500H frequency

Next work order created with nextDueRH = 6650, dueDate = null

This job NOT appearing in "Due" tab until component RH reaches 6650

Let me know what you find in the code and I'll verify the fix is correct.