I want you to redesign the UI of the PMS Reports section only. Do not change the business logic, data logic, API logic, report generation logic, export logic, or any backend behavior. This is only a UI restructuring and rewiring task. The current PMS Reports page is tile-based, but I want it changed to a tree-view-based master-detail layout, similar in interaction style to the Audits reports module layout. The goal is to make the PMS Reports screen more structured, cleaner, and more consistent with the Audits report section style. The existing reports, report categories, filters, and export functionality must continue to work exactly as they do now.

First, remove or hide the “Maintenance Planner” category from the PMS Reports UI. It should not appear as a visible report category in the new redesigned reports screen. Only hide it from the interface; do not break any related underlying code unless it is truly unused and safe to exclude from rendering.

Second, replace the current tile/card layout of the PMS Reports page with a two-pane layout. The left pane should contain a tree view of reports. The right pane should display the selected report in preview/view mode. This should be a proper master-detail interface. The left side is for navigation. The right side is for viewing the selected report content.

In the left pane, the top level of the tree must represent the current main PMS report groups or categories. In other words, whatever report groups currently exist as major tiles in the PMS Reports module should now become expandable parent nodes in the tree. When the user expands a category, the individual report names under that category should appear as child items below it. Do not flatten everything into one long list. Keep the hierarchy clear: category first, then reports inside that category.

Each parent tree node should be expandable and collapsible. Use simple visual affordances such as chevrons, arrows, plus/minus icons, or any clean expand-collapse indicator. The interaction must be obvious. If a category has child reports, clicking the expand control should reveal them. Clicking again should collapse them. The tree should be easy to scan and should not feel like a bulky old-style file explorer. Keep it neat and modern, but simple.

When the user clicks on any actual report name in the tree, that report should load in the right pane in preview/view mode. Do not open a popup. Do not navigate to another page if it can be avoided. Keep the user on the same reports screen. The right side should behave like an embedded report viewer area. The user should be able to browse reports from the left and immediately see the selected report on the right.

At the top of the right pane, show two clear action buttons only for the selected report: one button for downloading as Excel and one button for downloading as PDF. These buttons should be visible only in the context of the selected report view and should trigger the exact same export/download functionality that already exists in the current PMS Reports implementation. Do not rewrite the export logic. Just reconnect the existing behavior to the new UI buttons.

The report preview area in the right pane should show the current report content in view mode using the same data and same generation logic that exists today. If a report already has a preview mode or embedded content logic, reuse it. If the current system loads a report through an existing function, handler, or component, keep that logic and simply display the result in the new right-hand panel. The task is not to reinvent reports; it is to present them in a better layout.

All current filters and report selection logic must remain intact. If there are global filters in the current PMS Reports section, they should remain available in the redesigned screen. Place them in a logical area, such as above the tree, above the right pane, or in a top filter bar, depending on what best matches the existing PMS design system. But do not remove working filters. Do not change how the filtering behaves. The output of the reports should remain exactly as before, only the UI container and navigation pattern should change.

Do not change the names of existing reports unless absolutely necessary for display consistency. Keep all existing report names, existing categories, and existing relationships the same, except for hiding Maintenance Planner from the visible interface. The rest of the report categories should continue to exist as top-level nodes in the tree. Their contained reports should appear under them as child nodes.

Please also maintain the current data-loading rules and permissions behavior. If some reports are shown conditionally today based on vessel, user role, report type, or available data, keep those same rules. The new UI must respect all existing conditions. Only the presentation layer should change.

The layout should be visually balanced. The left tree pane should be narrower than the right viewer pane. The right pane should get most of the space because that is where the report preview is shown. A typical split can be something like a navigation sidebar on the left and a wider content display panel on the right. Make sure the right pane feels like the primary workspace.

The left tree should support scrolling independently if the list gets long. The right pane should also handle its own content area properly, especially for larger reports. Do not let the whole page become messy or awkward due to nested scrolling unless necessary. The interface should feel organized and professional.

Please keep the styling aligned with the existing product look and feel. Do not introduce a completely different design language. The screen should look like part of the same PMS application, but more structured and closer in spirit to the Audits reports section. The intention is consistency, not a flashy redesign. Avoid oversized cards, decorative tiles, or unnecessary graphics. This screen is functional and data-oriented.

Also make sure the selected report in the tree is visually highlighted, so the user always knows which report is currently open in the right pane. Expanded categories should also be clearly distinguishable from collapsed ones. Keep spacing, indentation, and selection states clean and readable.

If no report is selected initially, show a clean placeholder in the right pane instructing the user to select a report from the tree. Do not show a broken or empty container. The empty state should be simple and professional.

Please do not delete working code just because the UI changes. Reuse existing components, handlers, and data sources wherever possible. This task is mostly a refactor of layout and wiring. The functionality must remain the same; only the user interface and interaction model must change.

Before finishing, verify these exact outcomes:

Maintenance Planner is no longer visible in the PMS Reports UI.

The old tile-based report category layout is removed from the PMS Reports page.

The left side shows report categories as expandable tree nodes.

Expanding a category reveals the reports inside it.

Clicking a report opens that report in the right pane.

The right pane shows the selected report in preview/view mode.

Two buttons appear on top of the right pane for Excel and PDF download.

Existing filters, report logic, and export logic still work exactly as before.

No backend or report calculation logic is changed.

Important: do not redesign this into a brand new workflow. This is not a feature rewrite. It is the same PMS Reports module with the same behavior, but presented as a tree-based report browser with a right-side viewer panel, instead of the current tile-based interface