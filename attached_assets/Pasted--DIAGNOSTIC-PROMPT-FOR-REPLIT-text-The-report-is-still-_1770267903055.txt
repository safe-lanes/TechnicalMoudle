 DIAGNOSTIC PROMPT FOR REPLIT
text
The report is still showing the old work order list format. Let me help you debug this issue.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TROUBLESHOOTING CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Please verify and report the status of each item:

1. BACKEND CHANGES:
   □ Did you modify the API endpoint in server/routes.ts?
   □ Show me the EXACT query being executed
   □ Did you test the endpoint directly? What's the response format?
   □ Run this test query and show me the result:
   
   curl http://localhost:5000/api/reports/critical-equipment-status?vesselId=<vessel_id>
   
   Expected result format:
   {
     "data": [
       {
         "component_code": "60.02",
         "component_name": "Aux DG No.02",
         "total_work_orders": 3,
         "overdue_jobs": 1,
         ...
       }
     ],
     "metadata": { ... }
   }

2. FRONTEND CHANGES:
   □ Did you update MaintenanceReports.tsx onGenerate handler?
   □ Is it calling the correct endpoint (/api/reports/critical-equipment-status)?
   □ Show me the current onGenerate code for "critical-equipment" report
   
3. PDF GENERATOR:
   □ Did you add the new generateCriticalEquipmentPDF function?
   □ Where is this function defined?
   □ Show me the autoTable configuration (head and body arrays)
   
4. CACHE/BUILD ISSUES:
   □ Did you restart the dev server after changes?
   □ Did you clear browser cache?
   □ Are you seeing any console errors?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMMON PROBLEMS & SOLUTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PROBLEM 1: Using wrong endpoint
Solution: MaintenanceReports.tsx should call /api/reports/critical-equipment-status
NOT: /api/reports/overdue-jobs or similar

PROBLEM 2: PDF generator not updated
Solution: Check if generateCriticalEquipmentPDF exists and is being called
NOT: Reusing generateOverdueJobsReport

PROBLEM 3: Using old cached report
Solution: Add a cache-busting parameter or clear report cache

PROBLEM 4: Frontend not rebuilt
Solution: Stop and restart the dev server (npm run dev or similar)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ACTION REQUIRED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Show me the following files/sections:

1. server/routes.ts - The critical-equipment-status endpoint (full code)
2. client/src/pages/reports/MaintenanceReports.tsx - The "critical-equipment" report definition (onGenerate handler)
3. client/src/lib/pdfReportGenerator.ts - The generateCriticalEquipmentPDF function
4. Console output - Any errors when generating the report
5. Network tab - The API response when clicking "Generate Report"

Then test:
1. Generate the report
2. Check browser Network tab - which endpoint is being called?
3. What data is being returned?
4. Check console for errors

MOST LIKELY ISSUE:
The old report is probably cached or you're still calling the old endpoint/function. 
Need to see the actual code to diagnose where the disconnect is happening.