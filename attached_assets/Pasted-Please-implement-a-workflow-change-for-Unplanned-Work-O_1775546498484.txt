Please implement a workflow change for Unplanned Work Orders in the PMS > Work Orders module. This is a workflow update only, not a redesign. The current Work Orders module already has tabs such as Planned, Due, Overdue, Pending Approval, and Completed, and this new behavior must ensure that newly created unplanned work orders move directly into Pending Approval after form submission.

Current workflow for unplanned work orders:

User opens the Unplanned Work Order form.

User fills Part A.

After saving, the work order gets created with a unique ID and becomes visible under Planned.

Then Part B becomes available there, and the user has to open it again and complete the remaining details.

Required new workflow for unplanned work orders:

When the user opens the Unplanned Work Order form, Part A and Part B must both be available in the same flow immediately.

The user should be able to fill the entire unplanned work order at one time in a single workflow.

After the user saves/submits the form, the work order should not go to Planned.

Instead, it should be saved directly into the Pending Approval section.

Important implementation rules:

This change applies only to Unplanned Work Orders.

Section A / Part A must remain exactly as it currently appears in the existing form, matching the current layout and fields shown in the attached reference.

Part B is already developed; it should now open immediately along with the unplanned form flow so the user can complete it in the same session.

Do not redesign the form UI, colors, field styling, spacing, headings, or overall page appearance.

Reuse the existing Part A and Part B components/forms as much as possible; only change the workflow and save behavior.

Functional expectation:

User clicks Unplanned W.O.

The form opens with Part A unchanged.

Part B is also available immediately, without waiting for a first save and without requiring the work order to first appear under Planned.

User completes both Part A and Part B together.

On final save/submit, the record is created properly with its work order details and is routed directly to Pending Approval.

It should no longer first appear in Planned as an intermediate step for unplanned work orders.

Please also ensure:

Unique work order creation still happens correctly.

Validation for both Part A and Part B still works correctly.

Approval workflow is triggered exactly as needed after submission.

Pending Approval listing shows the newly created unplanned work order correctly after submission.

No existing workflow for Planned or other work order types is affected.

Existing edit/view behavior continues to work normally after the work order is created.

Scope clarification:

Only modify the Unplanned Work Order creation workflow.

Do not change standard planned work order workflow.

Do not change the visual design of Part A.

Do not rebuild Part B; only make it available immediately in the same creation flow.

Do not alter the UI theme or module styling.

Acceptance criteria:

Part A of Unplanned Work Order remains visually unchanged.

Part B opens immediately in the same workflow during unplanned work order creation.

User can complete both parts in one go.

On save/submit, the unplanned work order goes directly to Pending Approval.

It does not first appear in Planned.

This change applies only to unplanned work orders.

Existing UI design, colors, spacing, and form structure remain unchanged.

Existing approval and record creation logic continues to work properly.

Shorter version
Please change the Unplanned Work Order workflow only. Right now the user fills Part A, saves, the record gets created under Planned with a unique ID, and then Part B is completed later from there. I want this changed so that Part A and Part B are both available immediately in the same form flow, allowing the user to complete the full unplanned work order in one go. After final save/submit, the work order should go directly to Pending Approval instead of first appearing under Planned.

Keep Part A exactly as it currently is in the existing form UI. Part B is already built, so do not redesign it; just make it open immediately in the same flow. Do not change colors, layout, field styling, or module appearance. This change must apply only to Unplanned Work Orders and must not affect other work order workflows.