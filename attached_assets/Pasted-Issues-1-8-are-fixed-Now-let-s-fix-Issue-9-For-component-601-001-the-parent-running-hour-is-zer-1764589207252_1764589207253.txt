Issues #1-8 are fixed. Now let's fix Issue #9: For component 601.001, the parent running hour is zero, but the child component is showing running hours.

Current behavior:
1. Parent component (e.g., 601) shows 0 running hours
2. Child component (e.g., 601.001) shows non-zero running hours
3. This is inconsistent - parent should aggregate or inherit from children

Please investigate and fix:

1. First understand the business logic requirement:
   - Option A: Parent RH = sum/max of children RH (aggregation)
   - Option B: Children inherit parent RH (inheritance)
   - Option C: Independent tracking (each component has own RH)
   
   For maritime PMS, typically Option B (inheritance) is used - child components inherit the running hours of their parent machinery.

2. Find where running hours are updated in server/persistentStorage.ts:
   - Search for "updateRunningHours" or "currentCumulativeRH"
   - Check if parent-child propagation exists

3. Add parent-to-child propagation when parent RH is updated:

   // When updating a parent's running hours, also update children
   async updateComponentRunningHours(componentId: string, newRH: string): Promise<void> {
     const component = this.data.components[componentId];
     if (!component) return;
     
     // Update this component
     component.currentCumulativeRH = newRH;
     component.lastUpdated = new Date().toISOString();
     
     // Find and update all children that inherit RH from this parent
     const children = Object.values(this.data.components).filter(
       c => c.parentId === component.componentCode && c.vesselId === component.vesselId
     );
     
     for (const child of children) {
       // Only propagate if child doesn't have independent RH tracking
       if (!child.hasIndependentRH) {
         child.currentCumulativeRH = newRH;
         child.lastUpdated = new Date().toISOString();
         console.log('[RH_PROPAGATE] Updated child:', child.componentCode, 'to', newRH);
       }
     }
     
     this.persistData();
   }

4. Alternatively, fix at display time - when showing a component's RH:
   - If component's RH is 0 or null, look up parent's RH
   - Display inherited value with indicator

5. Check the Running Hours page (client/src/pages/pms/RunningHours.tsx):
   - How are RH values displayed?
   - Is there logic to show parent vs child RH?

Test by:
1. Find component 601 (parent) and 601.001 (child)
2. Update parent 601's running hours to 1000
3. Check if child 601.001 now shows 1000 (or inherits it)
4. Verify the logic matches maritime PMS requirements