Check 4: What is rhAtPeriodStart?
If rhAtPeriodStart = currentRH → No change in period → 0% is correct!

If rhAtPeriodStart < currentRH → There WAS a change → Calculate should work

If rhAtPeriodStart > currentRH → RH decreased (meter reset) → 0% is correct

Check 5: Verify the calculation manually
Using actual values:

text
currentRH = [value from database]
rhAtPeriodStart = [value from Step 4 query]
rhAccumulated = currentRH - rhAtPeriodStart
maxPossibleHours = [168/720/2160/8760 based on period]
utilizationRate = (rhAccumulated / maxPossibleHours) × 100

If calculated rate ≠ displayed rate → Bug in code
If calculated rate = 0.0% → Check is correct
MANDATORY REQUIREMENTS FOR IMPLEMENTATION
Requirement 1: NO Shortcuts or Approximations
DO NOT:

Sum audit trail deltas (old wrong approach)

Use "days since last update" heuristics

Estimate or interpolate values

Use averages across multiple periods

DO:

Always query for exact RH value at period start date

Always use two-point comparison (current vs period start)

Always use fixed period lengths (7, 30, 90, 365 days)

Requirement 2: Consistent Date Handling
All dates must use the same timezone:

Server time? UTC? Local time?

Pick ONE and use it consistently

Convert all dates to this timezone before comparison

Date comparisons must be inclusive:

text
period_start_date <= audit_entry_date <= today
Requirement 3: Audit Trail Integrity
The system MUST create audit entries when:

RH is updated via "Bulk Update RH" form

RH is updated via individual component update

RH is recorded during work order completion (Layer 7)

RH is inherited or modified

Each audit entry MUST have:

Exact timestamp (enteredAtUTC or equivalent)

The RH value at that time (newRh field)

Component ID

Source (RH_MODULE, WORK_ORDER, MANUAL, etc.)

Requirement 4: Handling Missing Data Gracefully
If no audit entry exists before period start:

Option A (Conservative - Recommended):

Use the OLDEST available audit entry for this component

If NO audit entries exist at all → rhAtPeriodStart = 0

This prevents artificially inflated rates for new components

Option B (Aggressive):

Show "N/A" or "Insufficient Data" instead of 0.0%

Don't calculate utilization if no baseline exists

Choose ONE approach and document it clearly

Requirement 5: Data Quality Warnings
When to log warnings:

Utilization > 100% (data error)

RH decreased (meter reset detected)

No audit entries for component

Gap in audit trail > 90 days

Display warnings to user:

⚠️ icon next to utilization rate

Tooltip: "Data quality issue detected"

Link to audit trail viewer

USER INTERFACE REQUIREMENTS
Display Requirements
Utilization Rate Column:

text
Format: X.X%

Examples:
0.0%
7.1%
55.6%
89.3%
100.0%
Color Coding:

0.0%: Gray or Red (no operation)

0.1% - 50%: Green (low to moderate utilization)

51% - 75%: Yellow (good utilization)

76% - 90%: Orange (high utilization)

91% - 100%: Red (very high/continuous operation)

Tooltip on Hover
When user hovers over utilization rate, show details:

text
Monthly Utilization: 7.1%

Calculation Details:
━━━━━━━━━━━━━━━━━━━━━━
Period: 10-Feb-2026 to 12-Mar-2026 (30 days)
Current RH: 2,001 hrs
RH at Period Start: 1,950 hrs
RH Accumulated: 51 hrs
Maximum Possible: 720 hrs (30 days × 24 hrs/day)

Formula: (51 / 720) × 100 = 7.1%

Interpretation: This machinery ran on average 1.7 hours per day over the last 30 days.
Period Dropdown Behavior
When user changes period:

Immediately recalculate ALL utilization rates

Show loading indicator

Update column header: "Utilization Rate (Weekly)" or "Utilization Rate (Monthly)"

Refresh displayed values

Maintain sorting/filtering state

Dropdown options must be clearly labeled:

text
Weekly (Last 7 days)
Monthly (Last 30 days)
Quarterly (Last 90 days)
Yearly (Last 365 days)
TESTING REQUIREMENTS
After implementing these rules, test with these scenarios:

Test Case 1: Normal Operation
Component with regular RH updates

Monthly period selected

Expected: Non-zero utilization rate

Verify calculation matches manual calculation

Test Case 2: No Operation
Component with RH = 0

Any period

Expected: 0.0% utilization ✅

Test Case 3: Stale Data
Component last updated 60 days ago

Monthly period (30 days)

Expected: 0.0% (no change in last 30 days) ✅

Test Case 4: New Component
Component added 10 days ago

Monthly period (30 days)

Expected: Use 0 as baseline OR show "N/A"

Test Case 5: High Utilization
Component ran continuously

RH increased by 168 hrs in 7 days

Weekly period

Expected: 100.0% utilization

Test Case 6: Meter Reset
RH decreased (e.g., from 5000 to 100)

Any period

Expected: 0.0% with warning icon

Test Case 7: Period Change
Same component viewed with different periods

Weekly: Should show different % than Monthly

Verify both calculations are correct

Test Case 8: Multiple Vessels
Same test data across Vessel 4 and Vessel 11

Should show identical results

Currently showing different (Vessel 4: 6.9%, Vessel 11: 0.0%) ❌ BUG!

SPECIFIC FIX NEEDED FOR CURRENT BUG
Observed Issue:
Vessel 4:

S-Band Radar: Shows 6.9% ✅ WORKING

Vessel 11:

FO PURIFIER, NO.1: Shows 0.0% despite recent update ❌ NOT WORKING

Root Cause Hypothesis:
One of these is wrong:

Query is vessel-specific and failing for Vessel 11

Check if vesselId filter is interfering with audit query

Audit entries not created for Vessel 11 updates

Check if Bulk Update creates audit entries for all vessels

Different component ID structure

Vessel 11 components might have different ID format

Query might not match properly

Date/timezone issue

Audit dates for Vessel 11 stored in different timezone

Period start calculation different per vessel

Required Investigation:
Run these diagnostic queries for FO PURIFIER, NO.1 (702.005.01) on Vessel 11:

Query 1: Check current RH

sql
SELECT currentCumulativeRH, vesselId 
FROM components 
WHERE code = '702.005.01'
Expected: 2,001 hrs, Vessel 11

Query 2: Check audit trail exists

sql
SELECT COUNT(*), MIN(date), MAX(date)
FROM running_hours_audit
WHERE componentId = [FO_PURIFIER_NO1_ID]
Expected: >0 entries, recent max date

Query 3: Check RH at period start (10-Feb-2026)

sql
SELECT runningHours, date
FROM running_hours_audit
WHERE componentId = [FO_PURIFIER_NO1_ID]
  AND date <= '2026-02-10'
ORDER BY date DESC
LIMIT 1
Expected: Some value < 2,001 hrs (if utilization should be non-zero)

Query 4: Check if 11-Mar update created audit entry

sql
SELECT runningHours, date, source
FROM running_hours_audit
WHERE componentId = [FO_PURIFIER_NO1_ID]
  AND date >= '2026-03-11'
ORDER BY date DESC
Expected: Entry with 2,001 hrs on 11-Mar-2026

FINAL INSTRUCTIONS FOR REPLIT
Implement the formula EXACTLY as specified in Rule 1

Use the fixed period definitions from Rule 2

Follow the step-by-step procedure without deviation

Query audit trail correctly using the SQL patterns shown

Handle all edge cases as documented

Test with all scenarios listed above

Run diagnostic queries for Vessel 11 to find why it shows 0.0%

Provide debug output showing:

What period start date was calculated

What rhAtPeriodStart was found

What rhAccumulated was computed

What final utilization was calculated

Fix any vessel-specific bugs so ALL vessels calculate correctly

Document the actual behavior vs expected for the failing cases

