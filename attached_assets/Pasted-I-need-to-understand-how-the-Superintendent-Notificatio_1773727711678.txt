I need to understand how the "Superintendent Notifications" section in the Dashboard is currently functioning. There appears to be a discrepancy between the data displayed and the expected behavior.

Current Observation:

Dashboard for Vessel 3 shows: "Superintendent Notifications: 0 Pending Acknowledgment, 0 Acknowledged This Month"

However, Vessel 3 has 367 overdue work orders

Multiple work orders show "5 Cycles Skipped" badge (indicating high-severity missed cycles)

According to Layer 5 specifications, work orders with 3+ missed cycles OR 21+ days late OR 7+ days backdating should generate superintendent notifications requiring acknowledgment

Questions:

Current Implementation - Please describe:

How is the Superintendent Notifications section in the dashboard currently calculating the pending and acknowledged counts?

What triggers create a superintendent notification?

Is it currently linked to:

Completed work orders with high severity issues (missed cycles ≥3, days late ≥21, backdating ≥7)?

OR is it showing something else?

Which database table/collection stores superintendent notifications?

What is the current query/logic used to fetch the counts displayed in the dashboard?

Vessel Filter Integration:

Does the Superintendent Notifications section respect the vessel dropdown filter selection?

When Vessel 3 is selected, should it show only notifications for Vessel 3?

Currently, is it hardcoded to show 0 or is it querying but finding no results?

Notification Creation Logic:

When a work order is completed with CE remarks (high severity), is a superintendent notification automatically created in the database?

Is there a background job or API endpoint that creates these notifications?

What is the current status - are notifications being created but not displayed, or not being created at all?

Data Flow:

Walk me through the complete data flow from:

Work order completion with high severity → CE remarks entry → Notification creation → Dashboard display

Which API endpoints are involved?

Which components/functions handle notification creation and retrieval?

Frontend vs Backend:

Is the dashboard count hardcoded/static in the frontend?

Or is it making an API call to fetch real-time notification counts?

If API call exists, what is the endpoint and what does it return currently for Vessel 3?

Expected Behavior:
Based on Layer 5 specifications, when work orders with high severity issues (3+ missed cycles, 21+ days late, 7+ days backdating) are completed:

A superintendent notification should be automatically created

It should appear in "Pending Acknowledgment" count on the dashboard

Superintendent can acknowledge it

After acknowledgment, it moves to "Acknowledged This Month" count

The counts should be vessel-specific based on the selected vessel filter

Please provide the current backend implementation details so I can identify where the disconnect is occurring and fix the Superintendent Notifications section to work dynamically with real data.