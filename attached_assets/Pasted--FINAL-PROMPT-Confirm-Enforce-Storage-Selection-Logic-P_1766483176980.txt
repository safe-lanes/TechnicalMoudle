🟦 FINAL PROMPT — Confirm & Enforce Storage Selection Logic (Postgres vs File-Based)
You have already implemented PostgreSQL-based storage and verified that local setup works correctly when DATABASE_URL is present.
This prompt is only to confirm and enforce the intended storage-selection behavior, not to redesign architecture.
🎯 Required Behavior (Very Important)
The application must follow this exact rule:
1️⃣ When DATABASE_URL IS PRESENT
✅ Use PostgreSQL only
❌ Do NOT read from or write to file-based storage
❌ Do NOT fallback to file-based storage under any condition
PostgreSQL is the single source of truth
This applies to:
Reads
Writes
Bulk uploads
Imports
Transformations
Module-to-module flows
2️⃣ When DATABASE_URL IS NOT PRESENT
(This applies mainly to Replit runtime environments where DB secrets may not propagate)
✅ Explicitly fallback to file-based storage
File-based storage is allowed only in this case
This fallback is intentional and temporary
As soon as DATABASE_URL becomes available, file-based storage must be bypassed completely
🔍 What I Want You To Do (Analysis + Confirmation)
Verify current behavior
Confirm how the app currently decides between:
PostgreSQL storage
File-based storage
Identify the exact decision point (factory / config / bootstrap)
Explicitly confirm
Is the logic already behaving exactly as described above? (YES / NO)
If YES
Confirm clearly:
Local setup → PostgreSQL only
Replit without DATABASE_URL → file-based fallback
No code changes required
Stop after confirmation
If NO
Adjust only the storage-selection logic so it strictly follows the rules above
Do not modify schemas, business logic, or module behavior
Do not introduce hybrid behavior when DATABASE_URL exists
After adjustment, explain:
What was changed
Where the decision logic now lives
Why this guarantees correct behavior
⛔ Hard Rules
❌ Do NOT redesign storage architecture
❌ Do NOT remove file storage entirely
❌ Do NOT allow fallback when DATABASE_URL exists
❌ Do NOT use raw SQL
✅ PostgreSQL preferred whenever available
✅ File storage only when DB is unreachable or undefined
✅ End State I Expect
Local development (with env configured): PostgreSQL only
Replit runtime (DB env missing): File-based fallback
No accidental hybrid behavior
Clear, predictable storage behavior
Stop after completing this.