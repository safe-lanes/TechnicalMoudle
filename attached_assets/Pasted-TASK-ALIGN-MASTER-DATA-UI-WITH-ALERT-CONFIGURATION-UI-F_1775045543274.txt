TASK: ALIGN MASTER DATA UI WITH ALERT CONFIGURATION UI (FULL UI STANDARDIZATION)

Objective:

Redesign the **Master Data screen UI** to match the **Alert Configuration UI** in terms of:

• Color scheme  
• Button style  
• Layout structure  
• Padding and spacing  
• Section formatting  

⚠️ Only UI/UX alignment – NO change in functionality or content.

--------------------------------------------------

1. Reference Source

• Target UI: Alert Configuration screen (2nd screenshot)  
• Apply same design system to: Master Data screen (1st screenshot)  

--------------------------------------------------

2. Layout Transformation

A. Replace Dashboard Card Layout

Current:

• Multiple colored cards (tiles)  
• Mixed layout blocks  

Change To:

• Clean **section-based layout** using cards/panels  
• Each section inside a **single bordered container**  

--------------------------------------------------

B. Section Structure

Convert Master Data into structured sections like:

• Section Container (same as Alert Configuration boxes)  
• Section Header  
• Section Content  

--------------------------------------------------

3. Color Standardization

Apply Alert UI color system:

• Background: Light neutral (white / light grey)  
• Cards: White with subtle border  
• Remove colored tiles (purple, green, orange, etc.)  
• Use minimal accent colors only where needed  

--------------------------------------------------

4. Button Styling

Replace all buttons with Alert UI style:

• Solid blue primary button  
• Consistent height and padding  
• Rounded corners (same radius)  
• Same font size and weight  

Examples:

• “View”, “Manage”, “Configure” → convert to uniform button style  
• Remove link-style buttons → convert to proper buttons  

--------------------------------------------------

5. Typography

Match Alert UI:

• Section titles → bold, medium size  
• Subtext → light grey, smaller font  
• Labels aligned consistently  

--------------------------------------------------

6. Spacing & Padding

Apply consistent spacing:

• Equal padding inside all containers  
• Uniform gap between sections  
• Proper margin from screen edges  

--------------------------------------------------

7. Card / Container Styling

Replace current tiles with:

• Flat containers  
• Light border  
• Soft shadow (optional, same as Alert UI)  

--------------------------------------------------

8. Header Alignment

• Align page title “Master Data” similar to “Alert Configuration”  
• Keep subtitle/description style same  

--------------------------------------------------

9. Remove Visual Noise

• Remove excessive colors  
• Remove unnecessary icons (if not present in Alert UI)  
• Keep minimal, clean enterprise look  

--------------------------------------------------

10. Functional Preservation

Do NOT change:

• Navigation  
• Data  
• Actions  
• Backend logic  

Only visual transformation allowed  

--------------------------------------------------

11. Validation

Check:

• Visual match with Alert Configuration screen  
• Same button style across page  
• Same spacing and alignment  
• Clean and consistent UI  

--------------------------------------------------

Expected Result:

• Master Data screen visually matches Alert Configuration UI  
• Clean, minimal, and professional layout  
• Consistent design system across module  
• Improved readability and usability  