STEP 10 — UPDATE THE PENDING APPROVAL TAB IN WO LIST

In the main Work Orders list page (/pms/work-orders), the "Pending Approval" tab currently shows columns: Component, Work Order No, WO Template Code, Job Title, Assigned to, Submitted Date, Status, Actions.

Make the following changes:

Add a "Days Late" column between "Submitted Date" and "Status":

daysLate == 0 → green text: "On Time"

daysLate 1–6 → yellow text: "[N] days late"

daysLate 7–14 → orange text: "[N] days late"

daysLate > 14 → red bold text: "[N] days late ⚠️"

Add an "Approval Tier" column after "Days Late":

standard → small green badge: "Standard"

ce_with_justification → small yellow badge: "CE + Remarks"

superintendent_notification → small orange badge: "Supt. Notified"

superintendent_locked → small red badge: "🔒 Locked"

In the "Actions" column:

If approvalTier == "superintendent_locked": replace action buttons with a grey locked icon (🔒). On hover show tooltip: "Locked — Awaiting Superintendent acknowledgment"

All other tiers: keep existing buttons unchanged

Add a summary bar above the table with 4 stat cards:

🔒 "Locked (Supt. Required)" — count of superintendent_locked — red background

⚠️ "Supt. Notified" — count of superintendent_notification — orange background

📝 "CE Remarks Required" — count of ce_with_justification — yellow background

✅ "Standard Approval" — count of standard — green background

Show all 4 cards even if count is 0 (grey out cards with zero count).

STEP 11 — UPDATE THE COMPLETED TAB IN WO LIST

In the "Completed" tab, add a read-only "Approval Tier" column (after "Status", before "Date Completed") using the same colored badges. This is for historical audit visibility only.

STEP 12 — UPDATE THE WORK ORDER DETAIL PAGE HEADER BANNER

When a WO's status is pending_approval, replace the existing top banner with a tier-based dynamic banner:

superintendent_locked → Red banner: "🔒 LOCKED — This work order has [missedCycles] missed cycle(s) and is awaiting Superintendent acknowledgment before the Chief Engineer can approve."

superintendent_notification → Orange banner: "⚠️ SUPERINTENDENT NOTIFIED — This completion is [daysLate] days late. The Superintendent has been automatically notified. Chief Engineer must approve with detailed remarks."

ce_with_justification → Yellow banner (dark text): "⚠️ REMARKS REQUIRED — This completion is [daysLate] days late. Chief Engineer must provide approval remarks before approving."

standard → Blue banner: "ℹ️ PENDING APPROVAL — Awaiting Chief Engineer review and approval."

STEP 13 — TEST DATA SETUP

After implementing everything, create 4 test WOs in pending_approval status to cover all tiers:

daysLate = 2, missedCycles = 0, approvalTier = "standard" — tests Flow D

daysLate = 10, missedCycles = 0, approvalTier = "ce_with_justification" — tests Flow C

daysLate = 20, missedCycles = 0, approvalTier = "superintendent_notification" — tests Flow B. Also create the corresponding superintendent_notification record.

daysLate = 35, missedCycles = 4, approvalTier = "superintendent_locked" — tests Flow A. Also create the superintendent_notification record with isAcknowledged = false.

Confirm after seeding:

Pending Approval tab shows all 4 with correct badges

Summary bar shows: Locked=1, Supt.Notified=1, CE Remarks=1, Standard=1

/pms/superintendent shows 2 notification rows (for tiers 3 and 4 above)

The locked WO has a disabled Approve button

The standard WO has an enabled Approve button

STEP 14 — DO NOT BREAK EXISTING FEATURES

After all changes, verify these previous layers still work:

Layer 1 — Orange "Cycles Skipped" badges still visible on the Overdue tab

Layer 2 — Next Due Date still auto-calculated from completion date (do not touch this logic)

Layer 3 — A4 Work History section still records and displays completion data

Layer 4B — The existing ceJustification field (crew-filled in the WO form) must still exist and be stored separately from the new ceApprovalRemarks field (CE-filled at approval time). Both fields must coexist in the database and both must be visible on the WO form. Never merge or overwrite these two fields.

Old completed WOs that have no approvalTier stored — treat them as "standard" so nothing breaks.

STEP 15 — FINAL CHECKLIST BEFORE SAYING YOU ARE DONE

Run through each of these in the live app and confirm:

 Pending Approval tab → 4 WOs visible → correct tier badges → summary bar correct

 Open superintendent_locked WO → red banner at top → Approve button disabled

 Open standard WO → blue banner → Approve button enabled

 Open ce_with_justification WO → yellow banner → attempt approval with empty remarks → error shown

 Open superintendent_notification WO → orange banner → attempt approval with <20 char remarks → error shown

 Go to /pms/superintendent → page loads → 2 rows visible → click Acknowledge on locked WO → confirmation modal appears → confirm → WO tier drops to ce_with_justification → CE can now approve with remarks

 Overdue tab → orange "Cycles Skipped" badges still present (Layer 1 intact)

 Open a Completed WO → A4 Work History still shows data (Layer 3 intact)

Report back with confirmation of each checklist item once done.