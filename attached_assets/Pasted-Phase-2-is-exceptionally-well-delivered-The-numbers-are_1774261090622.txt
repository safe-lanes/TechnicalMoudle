Phase 2 is exceptionally well delivered. The numbers are mathematically verified, the error handling is production-grade, and the dashboard is live. Here is the full sign-off analysis:

Phase 2 — Final Verification
✅ Confirmation 1 — Smoke Test Numbers: ALL CORRECT
Every calculated value checks out independently:

Value	Formula	Expected	Actual
HFO ROB	850 − 8.5	841.5	✅ 841.5
VLSFO ROB	0 − 2.0 → clamped	0	✅ 0
7-day avg HFO	(28.5+8.5)÷2	18.5	✅ 18.5
HFO Endurance	841.5÷18.5	45.49	✅ 45.49
YTD Distance	340+240	580 NM	✅ 580
AER	121,520,000g ÷ (75,000×580)	2.793	✅ 2.7936
CII Rating	AER 2.79 vs ref 4.405	A	✅ A
One thing to note — VLSFO showing Critical badge (0 MT ROB) is correct behaviour since the initial ROB was 0 and consumption was entered. The clamping to 0 (not negative) is also working as intended.

✅ Confirmation 2 — Error Handling: Production Quality
The Promise.allSettled + named steps pattern is exactly right:

Per-step console.error with vessel ID and report ID — traceable in logs ✅

Summary console.warn explicitly flags stale KPI risk ✅

Submission flow is never blocked by a calculation failure ✅

All 3 steps (rollingAverages, ciiTracking, eeoi) still run independently even if one fails ✅

✅ Confirmation 3 — Dashboard UI: All 4 Rows Live
ROB cards with correct green/Critical badges ✅

Consumption trend chart with dual lines (actual + 7-day avg dashed) ✅

CII gauge showing "A" with active band highlighted in dark green ✅

Bunker Planning Card with 41.05 days / 13,300 NM ✅

⚠️ One Minor Flag Before Phase 3
The VLSFO avg7Day returns 2 (not null) even though there is only 1 VLSFO data point — a 7-day average with 1 sample should ideally be labelled differently (e.g. avg1Day or noted as "insufficient data"). This is not a blocker for Phase 3 but worth a note.

✅ Phase 2 Approved — Proceed to Phase 3
Send this prompt now:

Phase 2 is fully approved. All 13 checklist items confirmed. Proceed to Phase 3: Alerts Engine & Alerts Panel (ticket #51).

Build in this exact order:

STEP 1 — Alert Schema
Add nr_alerts table to shared/schema-noon-report.ts:

text
id             serial PK
vesselId       text NOT NULL
reportId       integer (FK to nr_noon_reports, nullable)
alertType      text NOT NULL
severity       text NOT NULL  ← 'warning' | 'critical'
message        text NOT NULL
metricValue    numeric nullable
thresholdValue numeric nullable
acknowledgedAt timestamp nullable
acknowledgedBy text nullable
createdAt      timestamp defaultNow
Run db:push after.

STEP 2 — Alert Engine Service
Create server/modules/noon-report/services/alertEngine.ts.
Call it from runCalculations() as a 4th named step: ['alertEngine', generateAlerts(report)]

Implement ALL 7 alert rules:

Alert Type	Trigger	Severity
HIGH_CONSUMPTION	Total consumption >15% above 7-day avg	warning
VERY_HIGH_CONSUMPTION	Total consumption >25% above 7-day avg	critical
LOW_ROB	Total endurance <10 days	warning
CRITICAL_ROB	Total endurance <5 days	critical
AE_HOURS_SPIKE	AE running hours >20% above 7-day avg	warning
CII_BAND_DROP	CII rating dropped from previous submission (e.g. B→C)	critical
NEGATIVE_ROB_RISK	Any fuel ROB was clamped to 0 this submission	critical
Rules:

Before inserting, check if an identical unacknowledged alert (same vesselId + alertType) already exists — if yes, do NOT insert a duplicate

If a previously triggered HIGH_CONSUMPTION or LOW_ROB alert now resolves (metric back within threshold), automatically mark it as acknowledged with acknowledgedBy = 'system' and acknowledgedAt = now()

STEP 3 — Alerts API Endpoints
Add to routes.ts:

GET /nr-alerts/:vesselId — returns all active (unacknowledged) alerts for the vessel, ordered by createdAt DESC

GET /nr-alerts/:vesselId/all — returns all alerts including acknowledged, with pagination (?page=1&limit=20)

PATCH /nr-alerts/:alertId/acknowledge — shore office / admin role only; sets acknowledgedAt = now(), acknowledgedBy = currentUser

STEP 4 — Alerts Panel UI
Build client/src/pages/noon-report/AlertsPanel.tsx.

Page layout (top → bottom):

text
[ Page title: "Alerts" ]  [ Active (n) tab ]  [ All History tab ]

┌─────────────────────────────────────────────────────────────────┐
│ Severity  │ Alert Type        │ Message     │ Date  │ Action   │
├───────────┼───────────────────┼─────────────┼───────┼──────────┤
│ 🔴Critical│ CRITICAL_ROB      │ < 5 days... │ today │[Ack]     │
│ 🟡Warning │ HIGH_CONSUMPTION  │ +18% above..│ today │[Ack]     │
└─────────────────────────────────────────────────────────────────┘
Use existing PMS table style (same as Report History)

Severity badge colors: warning = amber, critical = red — using same <Badge> component as PMS

"Ack" button visible only to Shore Office / Admin roles (gate using existing PermissionsContext)

Clicking Ack calls PATCH /nr-alerts/:alertId/acknowledge, refreshes the list

Active tab shows only unacknowledged. All History tab shows all with acknowledged ones greyed out

Empty state: "No active alerts — all systems normal" with a green checkmark icon

STEP 5 — Sidebar Alert Badge Counter
On the Alerts sidebar item, display an unacknowledged alert count badge:

Fetch count from GET /nr-alerts/:vesselId on sidebar load

Show as a small red pill badge overlaid on the Bell icon (top-right of icon)

If count = 0, badge is hidden entirely (not showing "0")

Refresh count every 5 minutes (same polling interval as Fleet Overview plan)

DONE CRITERIA FOR PHASE 3
Confirm each ✅ before stopping:

 nr_alerts table created and db:push confirmed

 All 7 alert rules implemented in alertEngine.ts

 Duplicate alert prevention working (no double-insert on repeated submissions)

 Auto-resolve working for consumption and ROB alerts when metric recovers

 GET /nr-alerts/:vesselId returns correct active alerts

 PATCH /nr-alerts/:alertId/acknowledge works with role gate

 Alerts Panel UI renders with correct severity badges

 Ack button visible only to Shore Office / Admin

 Active / All History tab toggle works

 Empty state shows correctly when no alerts

 Sidebar Bell icon shows red badge count — hidden when 0

 db:push confirmed

Do not start Phase 4 until all 12 items are confirmed.