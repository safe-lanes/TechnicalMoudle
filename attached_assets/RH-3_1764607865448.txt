Q1 — RH History Sorting

In “View History” modal, should entries be shown:

Newest first (descending)?



Q2 — How should DELTA be displayed in history?

Example: Old = 1000, New = 1030 → Delta = 30.

Should delta store:





Only difference but with tag: added, corrected, meter replaced?

Q3 — RH entry date format

Choose exact UI format:



DD-MMM-YYYY (20-Nov-2025)


Q4 — RH entry timestamp

Should user select time manually or system auto-fill current time?


C. Both hidden; system auto-fills date & time





Q5 — Bulk Update “Present Running Hours”

In Set Total mode, if user leaves a component blank:

Should system treat it as no update?



Blank = keep old value

Q6 — RH Children Pop-Up (your idea)

You suggested:

“WE CAN CREATE A POP-UP WINDOW FOR EACH PARENT TO SHOW THEIR CHILD RH ALSO.”

QUESTION:

Should this child RH popup:

A. Show only child RH values (read-only)


