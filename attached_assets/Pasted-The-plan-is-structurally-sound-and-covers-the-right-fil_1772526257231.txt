The plan is structurally sound and covers the right files. Here's task-by-task validation + what needs to be added or corrected:

T001 – Section Header Bars ✅ + Correction needed
The plan is correct. However, add this clarification:

The three section column headers ("WORK ORDER KPIS", "WORK ORDER STATUS & TRENDS", "INVENTORY & FLEET ANALYSIS") currently sit inside a shared full-width dark navy banner row that spans all three columns. This banner wrapper div itself must also be removed/restyled — not just the individual label text. The three column headers should become independent plain blue labels sitting inside their respective white card areas, not inside a shared colored container.

T002 – Gauge Charts ✅ + Addition needed
The plan is correct. Add this missing detail:

The gauge KPI card titles ("OVERDUE WOS", "COMPLETION RATE", "OUTSTANDING TASKS") are currently rendered as bold uppercase dark text inside the KPI card. In the reference, these card titles use the same small blue label style (#1a6eb5, 11px, uppercase, bold) — not large dark caps. Update gaugeTitle style accordingly.
Also: the small supporting stat text below the gauge ("43% of total", "Due: 93 · Pending: 1") currently has no explicit color — confirm it is set to #6b7280, 12px.

T003 – KPI Number Badges ✅ Correct
No additions needed. The description is precise.

T004 – Trend Line Chart ✅ + Critical correction
The plan says to map lines as Healthy/Watch/Backlog using completed, outstanding, and overdue data. But the current chart is a single-line area chart tracking cumulative completion rate (%) over 6 months — it is NOT showing three separate time-series that map cleanly to Healthy/Watch/Backlog categories as separate vessels or groups. Correct interpretation:

Keep the existing monthly data points but plot three separate line series: one for completionRate (green), one for overdueRate (red), and one for dueRate (orange), all as a percentage over time. Remove area fills and colored reference bands. Use LineChart from Recharts with dot={true} on each line. White background, light gray CartesianGrid strokeDasharray="3 3".

T005 – Donut Charts ✅ + Addition needed
Correct. Add:

The Status Distribution donut section title "STATUS DISTRIBUTION" and subtitle "Click segments to filter" should also follow the updated typography (blue label style, gray subtitle). Currently the subtitle appears in blue — change to #6b7280.
Also ensure the donut chart container div has backgroundColor: '#ffffff' explicitly (not inherited gray from the section).

T006 – Vessel Toggle ⚠️ Correction needed
The plan says "INACTIVE = filled dark navy, ACTIVE = outlined pill." But looking at the reference: "All Vessel" is outlined/border and "My Vessel" is filled dark navy — this means the currently selected one is filled. The PMS dashboard currently has "All Vessel" filled and "My Vessel" outlined — which is actually correct behavior (active = filled).
​

Correction: Do NOT invert the logic. The issue is purely styling, not logic:

Keep current logic (active = filled dark navy, inactive = outline). Only change: the inactive/outline style currently uses light gray background (#f0f0f0) — change to white background (#ffffff) with a dark navy border (#1a3a5c) and dark navy text. Border-radius: 20px. This matches the reference exactly.

T007 – Top Navigation Bar ✅ + Addition needed
Correct. Add:

The active module icon (PMS wrench icon) currently shows in white against the blue tab background. After change, the icon should also switch to #1a6eb5 blue. Ensure SVG/icon stroke or fill color is updated along with text color for the active tab.
Also: the thin horizontal blue accent line at the very bottom of the full navbar (bg-[#52baf3] div) should be kept but changed to a subtle #e2e8f0 light gray border — it serves as a separator, don't remove it entirely.

T008 – Left Sidebar ✅ Correct
No additions needed. The #1565c0 change is appropriate.

T009 – Card Styling ✅ + Addition needed
Correct. Add:

The "OVERDUE WORK ORDERS" table section and "WATCH LIST" section in the lower half of the dashboard also contain dark navy table header rows (tableHeaderStyle). These are data tables and should retain the dark navy headers for readability — the plan already notes this, just make it explicit that only the section banner headers (not data table headers) are being changed.
Also: verify the Vessel/Group Analysis matrix section container card has borderRadius: 8px applied.

T010 – Typography ✅ + Addition needed
Correct. Add one missing item:

The "WATCH LIST" section label and "OVERDUE WORK ORDERS" section label (in the lower cards) should also follow the same blue label style (#1a6eb5, 11px, uppercase, bold) — currently they appear as colored/styled differently. Update these inline section labels too.
The "SPARES STOCK STATUS" label above the donut in the inventory column should also be updated to the same blue label style.

❗ Missing Tasks Not in the Plan
T011 (NEW): Remove/Restyle the PMS Chat Bubble FAB
The floating blue circle chat button (bottom-right corner, "PMS Assistant") does not exist in the reference Audit dashboard. While functionality should be kept, the button styling should be changed from a bright teal/blue filled circle to a smaller, more subtle icon button (white background, #1a6eb5 border and icon, border-radius: 50%, box-shadow: 0 2px 8px rgba(0,0,0,0.15)) to reduce visual noise.

T012 (NEW): "STATUS DISTRIBUTION" Sub-label Color Fix
The text "Click segments to filter" below the STATUS DISTRIBUTION label currently appears in blue. Change to #6b7280 (medium gray), font-size: 11px, to match the reference's subdued subtitle treatment.

T013 (NEW): Vessel/Group Analysis Matrix Dot Colors
The dot indicators in the Vessel/Group Analysis table currently show red/green/orange dots. The reference uses gray dots for all cells when no strong signal exists (neutral gray #9ca3af). Only cells with significant overdue/critical status should show red. Ensure the dot color logic uses gray as the default/neutral state, not green.

Summary: The plan is ~85% correct. The main corrections are:

T006 — Don't invert toggle logic, just fix the inactive state color (white+border, not gray)

T004 — Map trend lines to completionRate/overdueRate/dueRate, not arbitrary Healthy/Watch/Backlog category labels

T001 — Also restyle the shared banner wrapper div, not just text inside it

Add T011, T012, T013 as new tasks for complete coverage.