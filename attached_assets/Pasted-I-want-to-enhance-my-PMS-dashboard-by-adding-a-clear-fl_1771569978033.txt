I want to enhance my PMS dashboard by adding a clear fleet/vessel context bar at the top of the dashboard.
Do NOT change any backend logic, only adjust the UI and use the data that is already available in the app.

Goals of this change:

The user should always know which vessel or fleet they are currently viewing.

The user should be able to quickly switch between “Fleet view” and “Single vessel view”.

The context bar should look like part of the dashboard header, not like a random extra card.

Please do the following:

Add a horizontal context bar just below the main dashboard title/header.

It should span the width of the dashboard content (same width as other cards/sections).

Visually it should look like a slim, clean bar (not a big card), with good spacing and alignment.

Use the same design system as the rest of the dashboard (same font, colors, border radius, etc.).

Information to display in this bar (from left to right):
a) A label like: “Current context:”
b) A dropdown (or select) for View mode with at least:

“Fleet view”

“Single vessel view”
c) When “Single vessel view” is selected, show:

A dropdown to choose Vessel name (populate it from the existing vessels data already used in the app; if you don’t have that list directly, use the same source that other vessel-related pages use).

Next to the vessel name, show small read-only text with Vessel class/type if available (for example: “Class: Handymax” or “Type: Bulk Carrier”). If this data is not available, just show the vessel name.
d) On the far right, show a small text like:

For Fleet view: “Scope: Entire fleet”

For Single vessel: “Scope: [Selected Vessel Name]”

Behaviour requirements:

Default state can be “Fleet view”.

When the user changes the view mode or vessel in this context bar, it should trigger whatever state/props the dashboard already uses to filter data by vessel/fleet.

If such a state does not exist yet, just wire it in a way that the rest of the dashboard can easily read it later (for example, keep selected view mode and vessel in a top-level dashboard state/context).

Do not break existing filters; this context bar is an extra layer on top that defines the “scope” of the dashboard.

UX and layout details:

Keep this bar compact: one row on desktop, with elements aligned vertically centered.

Use clear labels for elements, for example:

Label before view mode dropdown: “View:”

Label before vessel dropdown: “Vessel:”

Make sure spacing between elements is consistent (use same spacing tokens as other parts of the UI).

Ensure it works responsively:

On smaller screens, the elements can wrap to a second line, but they should still look neat and readable.

Do not:

Do not invent new dummy data; reuse the data structures already used for vessels in the app.

Do not remove or rename existing dashboard widgets.

Do not change the meaning of any KPIs; only add this context bar so later I can hook the filters to it.

Explain briefly what components/areas you modified and how the state for “view mode” and “selected vessel” is exposed so I can connect the rest of the dashboard to it afterwards.