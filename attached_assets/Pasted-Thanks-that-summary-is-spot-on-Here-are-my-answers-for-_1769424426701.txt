Thanks, that summary is spot on. Here are my answers for Batch 4 – Existing APIs:

Existing Endpoints
Work orders with filters

Yes, I have existing endpoints for listing work orders with filters.

Endpoint details:

[Paste or describe: method + path, e.g. GET /api/workorders with query params like vesselId, status, fromDate, toDate, componentCode, etc.]

[If there are multiple endpoints (e.g. /api/workorders/overdue), list them briefly.]

Dashboard KPIs (counts by status)

Yes, there are endpoints that power the PMS dashboard KPIs and charts.

Endpoint details:

[Paste/describe each: e.g. GET /api/dashboard/pms/summary, GET /api/dashboard/pms/workorder-status, GET /api/dashboard/pms/spares-status, etc., including key query params like vesselId.]

Low‑stock spares

Yes / No:

[If yes] There is an endpoint used for “Low Stock Spares” on the dashboard.

[Give method + path, e.g. GET /api/spares/low-stock?vesselId=... and important filters/params.]

[If no] Low‑stock is currently computed only on the frontend / via a generic spares list endpoint:

[Describe how the frontend currently gets spares, and how it decides low stock (rob < min).]

Components list

Yes, there is an endpoint to list components.

Endpoint details:

[Give method + path, e.g. GET /api/components?vesselId=...&search=...&parentId=....]

If helpful, I can paste the relevant snippets from server/routes.ts for these four areas.

How the frontend calls APIs
The frontend calls APIs using:

[Confirm or correct] fetch (or Axios, etc.) from React components/hooks.

The vesselId is passed as:

A query param (e.g. ?vesselId=...) derived from React Context + localStorage’s selectedVesselId.

Any other common query params:

[List things like status, page, pageSize, search, etc., if standardised.]

WebSocket / real‑time features
WebSocket usage:

[Yes/No.]

If yes, briefly describe what it is used for and the path (e.g. /ws/alerts).

For the chatbot, I’m fine starting with simple HTTP POST/response for now (no streaming is strictly required, but nice to have later).

Deep linking / URL patterns
Deep links / URL patterns:

[Answer clearly]

If yes: I can open filtered views via URL, for example:

[List a few concrete examples, e.g.:

/pms/workorders?status=overdue&vesselId=V001

/pms/spares?stock=low&vesselId=V001

/pms/components?vesselId=V001&componentCode=ME-PISTON
]

If no: At the moment, filters are managed purely in React state without URL params, but I am open to adding URL‑based filters to support chatbot deep links.

After this, please go ahead with:

Batch 5 – LLM & Infrastructure

Ask me exactly what you need about model provider, API keys, outbound HTTP, cost/latency constraints, data privacy rules, etc.

Batch 6 – Chatbot UX & Behavior

Ask me about preferred UI placement (side panel/floating), context awareness (current vessel/page), confirmation flows for write actions, logging/audit, and how “smart” the assistant should be.