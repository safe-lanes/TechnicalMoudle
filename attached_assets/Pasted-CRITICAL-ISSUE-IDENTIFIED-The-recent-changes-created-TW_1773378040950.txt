CRITICAL ISSUE IDENTIFIED:
The recent changes created TWO-ROW layouts with search bars taking ENTIRE screen width. This is INCORRECT.

CORRECT LAYOUT (Reference: Spares & Components modules):
✓ SINGLE ROW containing: [Vessel] [Other filters] [Search bar] [More filters]
✓ Search bar has FIXED/LIMITED width, NOT full screen width
✓ All filters fit comfortably in ONE line

CURRENT PROBLEM:
❌ Work Orders, Running Hours, Reports, Modify PMS all have:
   - Row 1: [Vessel] [Search - TAKES ENTIRE WIDTH]
   - Row 2: [Other filters]
❌ Search bar is using flex-grow or w-full, expanding to fill all space
❌ This pushes other filters to a second row

═══════════════════════════════════════════════════════════
CORRECT REFERENCE LAYOUTS (DO NOT CHANGE THESE):
═══════════════════════════════════════════════════════════

✓ COMPONENTS (Perfect - Single Row):
[Vessel: Vessel 11 ▼] [Critical Item: All Items ▼] [Search (limited width) 🔍]

✓ SPARES (Perfect - Single Row):
[Vessel: Vessel 11 ▼] [Search (limited width) 🔍] [Criticality ▼] [Stock ▼] [Clear]

═══════════════════════════════════════════════════════════
MODULES TO FIX:
═══════════════════════════════════════════════════════════

1. WORK ORDERS
Current (WRONG - 2 rows):
Row 1: [Vessel: Vessel 11 ▼] [Search (full width)]
Row 2: [Period ▼] [All Ranks ▼] [Criticality ▼] [Clear]

Target (CORRECT - 1 row):
[Vessel: Vessel 11 ▼] [Search (limited width)] [Period ▼] [All Ranks ▼] [Criticality ▼] [Clear]

FIX:
- Remove flex-grow from search bar
- Set search bar width to ~300-400px (same as Spares)
- Put ALL filters in SINGLE row
- Use flex-wrap if needed for responsiveness

───────────────────────────────────────────────────────────

2. RUNNING HOURS
Current (WRONG - 2 rows):
Row 1: [Vessel: Vessel 11 ▼] [Search (full width)]
Row 2: [Utilization Period ▼] [Export] [Clear]

Target (CORRECT - 1 row):
[Vessel: Vessel 11 ▼] [Search (limited width)] [Utilization Period ▼] [Export] [Clear]

FIX:
- Remove flex-grow from search bar
- Set search bar width to ~300-400px
- Put ALL filters in SINGLE row

───────────────────────────────────────────────────────────

3. REPORTS
Current (WRONG - 2 rows):
Row 1: [Vessel: All Vessels ▼] [Search (full width)]
Row 2: [Date Range] [Clear]

Target (CORRECT - 1 row):
[Vessel: All Vessels ▼] [Search (limited width)] [Date Range] [Clear]

FIX:
- Remove flex-grow from search bar
- Set search bar width to ~300-400px
- Put ALL filters in SINGLE row

───────────────────────────────────────────────────────────

4. MODIFY PMS
Current (WRONG - 2 rows):
Row 1: [Vessel: Vessel 11 ▼] [Search (full width)]
Row 2: [All] [Pending Approval] [Approved] [Rejected]

Target (CORRECT - 1 row):
[Vessel: Vessel 11 ▼] [Search (limited width)] [All] [Pending Approval] [Approved] [Rejected]

FIX:
- Remove flex-grow from search bar
- Set search bar width to ~300-400px
- Put status tabs in SAME row as vessel and search

───────────────────────────────────────────────────────────

5. DASHBOARD (Separate Issue)
Current (WRONG - centered):
[2026] [All Vessel] [My Vessel] [Vessel 11 ▼] [Filters]
^-- All elements are CENTERED in the header

Target (CORRECT - left-aligned):
Left side: [Vessel: All Vessel] [My Vessel] [Vessel 11 ▼]
Right side: [2026] [Filters]

FIX:
- Align vessel filter controls to LEFT side of header
- Keep year selector (2026) and Filters button on RIGHT side
- Use justify-between for left/right alignment

═══════════════════════════════════════════════════════════
TECHNICAL SPECIFICATIONS:
═══════════════════════════════════════════════════════════

SEARCH BAR SIZING (Copy from Spares module):
- Width: w-80 or w-96 (320-384px) - NOT w-full or flex-grow
- Max-width: max-w-md
- DO NOT use: flex-1, flex-grow, flex-grow-1, w-full
- Example correct classes: "w-80 max-w-md"

FILTER ROW CONTAINER:
- Display: flex
- Flex-direction: row (NOT column)
- Gap: gap-3 (12px spacing)
- Align-items: items-center
- Flex-wrap: flex-wrap (for responsiveness)
- Example: "flex flex-row gap-3 items-center flex-wrap"

VESSEL DROPDOWN:
- Width: w-48 (192px) - Fixed width
- First element in the row
- Example: "w-48"

OTHER FILTERS:
- Width: Auto or fixed (w-32, w-40, etc.)
- NOT flex-grow
- Should wrap to next line on small screens (flex-wrap)

═══════════════════════════════════════════════════════════
CODE CHANGES REQUIRED:
═══════════════════════════════════════════════════════════

In each module's filter section, change FROM:

❌ WRONG:
<div className="flex flex-col gap-2">  {/* ← Two rows */}
  <div className="flex gap-2">
    <VesselDropdown className="w-48" />
    <SearchBar className="flex-1" />  {/* ← Takes full width */}
  </div>
  <div className="flex gap-2">
    <OtherFilters />
  </div>
</div>

✓ CORRECT:
<div className="flex flex-row gap-3 items-center flex-wrap">  {/* ← Single row */}
  <VesselDropdown className="w-48" />
  <SearchBar className="w-80 max-w-md" />  {/* ← Fixed width */}
  <OtherFilters />
</div>

═══════════════════════════════════════════════════════════
FILES TO MODIFY:
═══════════════════════════════════════════════════════════

1. client/src/pages/pms/WorkOrders.tsx
   - Change filter container from 2 rows to 1 row
   - Set search bar to w-80 instead of flex-1

2. client/src/pages/pms/RunningHours.tsx
   - Change filter container from 2 rows to 1 row
   - Set search bar to w-80 instead of flex-1

3. client/src/pages/pms/Reports.tsx
   - Change filter container from 2 rows to 1 row
   - Set search bar to w-80 instead of flex-1

4. client/src/pages/pms/ModifyPMS.tsx
   - Change filter container from 2 rows to 1 row
   - Set search bar to w-80 instead of flex-1
   - Move status tabs to same row

5. client/src/pages/pms/Dashboard.tsx
   - Change header from centered to justify-between
   - Align vessel controls to left
   - Keep year/filters on right

═══════════════════════════════════════════════════════════
VERIFICATION CHECKLIST:
═══════════════════════════════════════════════════════════

After making changes, verify:
□ All filters are in SINGLE row (not 2 rows)
□ Search bar width matches Spares module (~320-400px)
□ Search bar does NOT take full screen width
□ All filters are visible without horizontal scrolling on desktop
□ Filters wrap gracefully on smaller screens
□ Vessel dropdown is first element (left-aligned)
□ Search bar is second element (after vessel)
□ Layout matches Components and Spares modules exactly
□ Dashboard vessel filter is left-aligned (not centered)

═══════════════════════════════════════════════════════════
CRITICAL NOTES:
═══════════════════════════════════════════════════════════

⚠️ DO NOT change Components or Spares modules - they are CORRECT
⚠️ The goal is SINGLE ROW layout with ALL filters visible
⚠️ Search bar should have LIMITED width, not full width
⚠️ Use w-80 or w-96 for search bars, NOT flex-1 or w-full
⚠️ Match the exact spacing and sizing of Spares module
⚠️ Test on desktop viewport (1920x1080) - all filters must fit in one row