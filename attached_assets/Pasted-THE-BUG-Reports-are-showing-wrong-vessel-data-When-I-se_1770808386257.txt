THE BUG
Reports are showing wrong vessel data. When I select "Vessel 1" from dropdown, it shows "Vessel 4" data. Your analysis is correct - globalFilters is not syncing with VesselContext.

FIX THIS NOW - 3 MANDATORY STEPS
STEP 1: Sync Reports vessel dropdown with VesselContext

File: ReportsModule.tsx

Find the vessel dropdown change handler and modify it to:

typescript
const handleVesselChange = (newVessel: string) => {
  setGlobalFilters(prev => ({ ...prev, vessel: newVessel }));
  
  // CRITICAL: Also update VesselContext so all child components know
  updateVesselContext(newVessel); // or whatever the context update method is called
};
Make sure when user selects vessel in dropdown at /pms/reports, it updates BOTH:

Local globalFilters state

App-wide VesselContext

STEP 2: Pass globalFilters to MaintenancePlanner

File: ReportsModule.tsx line 169

CHANGE FROM:

typescript
<MaintenancePlanner onBack={handleBackToMain} />
CHANGE TO:

typescript
<MaintenancePlanner 
  onBack={handleBackToMain} 
  selectedVessel={globalFilters.vessel}
  dateRange={globalFilters.dateRange}
/>
Then in MaintenancePlanner.tsx:

Accept these props

Use selectedVessel prop INSTEAD of VesselContext

Filter all data queries by this vessel value

STEP 3: Fix the other 6 report category pages

These pages need fixing:

MaintenanceWorkOrders page

RunningHoursCondition page

InventorySpares page

InventoryStores page

IHMReport page

ChangeRequests page

For EACH page, do this:

CHANGE FROM:

typescript
const [selectedVessel, setSelectedVessel] = useState(globalFilters?.vessel || "all");
CHANGE TO:

typescript
const [selectedVessel, setSelectedVessel] = useState(globalFilters?.vessel || "all");

// Add this useEffect to keep it in sync
useEffect(() => {
  if (globalFilters?.vessel) {
    setSelectedVessel(globalFilters.vessel);
  }
}, [globalFilters?.vessel]);
Remove any fallback to VesselContext. Use ONLY the vessel from globalFilters prop.

TESTING CHECKLIST - DO THIS AFTER IMPLEMENTING
Go to /pms/reports

Select "Vessel 1" from dropdown

Click "Maintenance Planner" - should show "Planning view for Vessel 1" (NOT Vessel 4)

Go back, select "Vessel 2"

Click "Maintenance & Work Orders" - should show Vessel 2 data only

Repeat for all 7 report categories

Test "All Vessels" option - should aggregate all vessels' data

ADDITIONAL REQUIREMENTS
Date Range Filter:

Apply same fix to dateRange filter

Pass it to all report pages alongside vessel filter

"All Vessels" Behavior:

When "all" is selected, show aggregated totals in summary cards

Add "Vessel" column in data tables when "all" is selected

Make sure database queries handle both single vessel (WHERE vessel_id = ?) and all vessels (no WHERE or aggregate)

Navigation:

When clicking "Back to Reports", preserve the selected filters

Don't reset to default values

Loading States:

Show loading spinner when switching vessels

Prevent showing old vessel data while new data loads

DO NOT:
Don't use VesselContext as fallback in report pages

Don't initialize vessel filter to first vessel in database

Don't forget to pass filters to MaintenancePlanner (current bug on line 169)

CONFIRM BEFORE PROCEEDING:
Will filter changes auto-refresh open reports?

Will you handle both vessel AND dateRange filters?

Will "All Vessels" show aggregated data with vessel identification?

Now implement Steps 1, 2, 3. Show me the code changes when done.