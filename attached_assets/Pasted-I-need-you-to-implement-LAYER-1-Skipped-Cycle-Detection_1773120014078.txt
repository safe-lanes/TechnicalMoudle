I need you to implement LAYER 1: Skipped Cycle Detection for our PMS (Planned Maintenance System). 
Read this entire prompt carefully before writing any code. Do not skip any step.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKGROUND / PROBLEM STATEMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Currently, when a crew member completes a work order, the next due date is calculated 
from the completion date they enter. There is NO validation or detection if they 
entered a fake/backdated completion date to skip multiple cycles.

Example of the fraud:
- Job frequency: Every 1 Week
- Original scheduled due date (nextDueDate on work order): 01-Jan-2026
- Crew does NOT perform the job for the entire month
- On 31-Jan-2026, they open the form and enter completion date as 31-Jan-2026
- System calculates next due date = 31-Jan-2026 + 1 week = 07-Feb-2026
- Result: 3 weekly cycles (Jan 8, Jan 15, Jan 22) are silently skipped — NEVER DETECTED

We need to detect this automatically every time a work order is completed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WHAT YOU NEED TO IMPLEMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1 — ADD missedCycles CALCULATION IN THE BACKEND SERVICE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File to modify: server/modules/work-orders/services/workOrderCompletionService.ts

When a work order is being completed (inside the completeWorkOrder function, 
BEFORE the next due date is calculated), add the following logic:

1. Get the work order's original scheduled due date. 
   This is stored as nextDueDate on the work_orders table 
   (this is the date the job WAS DUE, before completion).

2. Get the user-entered completion date: dateOfCompletion

3. Get the job's frequencyValue (number, e.g. 1) and frequencyUnit 
   (string, e.g. "Weeks", "Days", "Months", "Years")

4. Convert frequencyValue + frequencyUnit into a duration in milliseconds 
   (or use date-fns for this). For example:
   - 1 Week = 7 days
   - 2 Weeks = 14 days
   - 1 Month = 30 days (use date-fns differenceInCalendarDays or similar)

5. Calculate:
   delayInDays = differenceInCalendarDays(dateOfCompletion, originalDueDate)
   
   If delayInDays is NEGATIVE or ZERO: missedCycles = 0 (job done on time or early)
   If delayInDays is POSITIVE: missedCycles = Math.floor(delayInDays / frequencyIntervalInDays)
   
   For month/year-based frequencies, use date-fns differenceInCalendarMonths or 
   differenceInCalendarYears instead of days.

6. Only apply this logic for Calendar-based maintenance (maintenanceBasis === 'Calendar').
   Skip this calculation for Running Hours based jobs.

7. Store the missedCycles count in the database (see Step 2 below).

IMPORTANT: Do not block or prevent the completion if missedCycles > 0. 
Just calculate and store it. The blocking/approval logic will come in a later layer.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 2 — ADD missed_cycles COLUMN TO THE DATABASE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Add a new column to the work_orders table:
  Column name: missed_cycles
  Type: INTEGER
  Default: 0
  Nullable: true

Also add a corresponding column to the component_maintenance_history table:
  Column name: missed_cycles
  Type: INTEGER
  Default: 0
  Nullable: true

Write and run the necessary database migration for both columns.

Then update the shared schema/types (e.g., shared/schema.ts or wherever the 
WorkOrder type is defined) to include the missedCycles field.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 3 — SAVE THE missedCycles VALUE DURING COMPLETION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

In workOrderCompletionService.ts, after calculating missedCycles:

1. Save it to the work_orders table for this work order:
   UPDATE work_orders SET missed_cycles = alculated value> WHERE id = <workOrderId>

2. Also save it to the component_maintenance_history record being created 
   for this completion.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 4 — RETURN missedCycles IN THE API RESPONSE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After completing the work order, the API response that is returned to the 
frontend should include the missedCycles value so the frontend can display it.

Make sure the GET work order endpoint also returns missed_cycles in its response.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 5 — SHOW THE MISSED CYCLES BADGE ON THE FRONTEND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

I will now describe exactly where on the UI the badge must appear. 
Implement ALL of the following locations:

--- LOCATION 1: Work Orders List Page ---
File: client/src/pages/pms/WorkOrdersPage.tsx (or wherever the work orders 
table/list is rendered)

The work orders list page has these columns:
Component | Work Order No | Job Title | Assigned To | Due Date | Status | Actions

There is a "Completed" tab on this page (currently showing "Completed 4" as a 
badge count). When the user clicks the "Completed" tab, the completed work orders 
are shown in the table.

In the STATUS column of the Completed tab:
- If missed_cycles === 0 or null: show the normal green "Completed" badge (existing behavior)
- If missed_cycles >= 1: show BOTH the green "Completed" badge AND a new 
  separate orange/amber badge that says "⚠ X Cycles Skipped" 
  (where X is the actual missed_cycles number, e.g. "⚠ 3 Cycles Skipped")

The "Cycles Skipped" badge styling should be:
  - Background color: amber/orange (#F59E0B or similar warning color)
  - Text color: white
  - Font size: same as the existing status badges
  - Rounded pill shape (same shape as existing status badges like "Overdue")
  - Display it BELOW or RIGHT OF the existing "Completed" badge, 
    on the same row in the Status column

--- LOCATION 2: Work Order Form Page (Part A - Job Information Section) ---
File: client/src/pages/pms/WorkOrderFormPage.tsx

The Work Order Form has a Part A section. Inside Part A, there is Section A1 
"Job Information" which shows: Job Title, Component Name, Job Code, Maintenance 
Basis, Frequency, Task Type, Assigned To, Approver, Next Due Date, etc.

At the TOP of Section A1 (before the Job Title field), add a prominent 
alert/warning banner that appears ONLY when missed_cycles >= 1:

The banner should look like this:
  ┌─────────────────────────────────────────────────────────────────┐
  │ ⚠  SKIPPED CYCLES DETECTED                                      │
  │  This work order was completed X cycles late. Y job cycles      │
  │  were missed between the scheduled due date and the actual      │
  │  completion date. This has been recorded in the audit trail.    │
  └─────────────────────────────────────────────────────────────────┘

Where X = missed_cycles number and Y = missed_cycles number (same value).
Banner background: amber/orange (#FEF3C7), border: #F59E0B, text: #92400E
The banner should NOT appear if missed_cycles is 0 or null.
The banner should appear whether the form is in read-only mode or edit mode.

--- LOCATION 3: Work Order Form Page (Section A4 - Work History Table) ---

The Work Order Form has Section A4 "Work History" which shows a table with 
columns: DATE | WORK ORDER | DESCRIPTION | PERFORMED BY | STATUS | REMARKS

In this A4 Work History table, for each row that has missed_cycles >= 1, 
add a new column or display it in the STATUS column:
- Show an orange "⚠ X Skipped" badge next to the status in that history row

This allows anyone opening the work order to see in the history that previous 
completions had skipped cycles.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 6 — SHOW SKIPPED CYCLES INFO IN THE COMPLETION FORM (PART B)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: client/src/pages/pms/WorkOrderFormPage.tsx — Part B section

The completion form (Part B) has a section called B2 "Details of Work Carried Out". 
Inside B2.1 "Work Duration", the Due Date is already displayed as a read-only field.

Modify B2.1 as follows:

When the user enters the Completion Date (the dateOfCompletion input field), 
do a LIVE REAL-TIME calculation on the client side:

1. Read the Due Date (already displayed as a static text in B2.1)
2. Read the Completion Date as the user types/selects it
3. Calculate missedCycles on the client side using the same formula as the backend:
   missedCycles = Math.floor(delayInDays / frequencyIntervalInDays)
   (The frequency info is available from Part A's Job Information — 
    read it from the form state)

4. If missedCycles >= 1, IMMEDIATELY show a live warning box BELOW the 
   Completion Date field (inside the B2.1 section), like this:

  ┌─────────────────────────────────────────────────────────────────┐
  │ ⚠  WARNING: X job cycles will be marked as skipped             │
  │  The completion date you entered is X cycles past the           │
  │  scheduled due date. This will be flagged in the audit trail    │
  │  and visible to the Chief Engineer and Superintendent.          │
  └─────────────────────────────────────────────────────────────────┘

  Where X = calculated missedCycles.
  This is a WARNING only — do NOT block the user from submitting.
  The warning disappears if the user changes the date to within 1 cycle.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 7 — DO NOT CHANGE ANYTHING ELSE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Do NOT change how next due date is calculated (that is a separate layer)
- Do NOT block completions
- Do NOT change the approval workflow yet
- Do NOT modify anything related to Running Hours jobs
- Only add the missed_cycles calculation, storage, and display

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SUMMARY OF ALL FILES TO MODIFY:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Backend:
1. server/modules/work-orders/services/workOrderCompletionService.ts 
   → Add missedCycles calculation and save to DB

2. Database migration file 
   → Add missed_cycles column to work_orders and component_maintenance_history tables

3. shared/schema.ts (or equivalent types file) 
   → Add missedCycles field to WorkOrder type

4. Work order GET endpoint 
   → Return missed_cycles in the response

Frontend:
5. client/src/pages/pms/WorkOrdersPage.tsx (the list page) 
   → Show "⚠ X Cycles Skipped" amber badge in Status column on Completed tab

6. client/src/pages/pms/WorkOrderFormPage.tsx 
   → Show amber alert banner at top of A1 section when missedCycles >= 1
   → Show "⚠ X Skipped" badge in A4 Work History table rows
   → Show live real-time warning below the Completion Date input in B2.1

Please implement all of this now, run the migration, and
