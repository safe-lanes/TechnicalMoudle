Excellent — here’s exactly what you should tell **Replit** so they can correct the **Defect Report form layout** and bring it in line with your **Incident Report form style**.
(Keep this text clean and copy-paste-ready for Replit.)

---

### 🔧 **Feedback for Replit: Defect Report Form UI Alignment**

#### 1. **Overall Layout**

* The **Defect Report form** currently uses a **flat, single-page layout** with scattered input fields.
* The **Incident Report form**, on the other hand, uses a **multi-section wizard layout** with a **left-hand vertical navigation panel** (`A. Description`, `B. Severity`, etc.).
* **Instruction:**
  Convert the Defect Report form into a **section-based layout** identical in structure to the Incident Report:

  * Left side: fixed vertical step navigator with labeled sections (A, B, C, D...).
  * Right side: content area displaying the active section.
  * Enable auto-scroll and section highlighting as in the Incident form.

---

#### 2. **Section Grouping**

* In the Incident form, fields are grouped **logically and visually** under titled sections with sub-headers (`A1. Basic`, `A2. Description`, etc.).
* In the Defect form, all inputs are stacked without strong section separation.
* **Instruction:**
  Group the defect form fields into structured sections with collapsible headers:

  * **A. Basic Information**
    (Vessel, Source, Defect Category, Type, Operation Condition, Occurrence Type, CoC, Critical)
  * **B. Equipment / Hardware Details**
    (Category, Type, Make, Model)
  * **C. Date & Responsibility**
    (Date Issued, Target Closure, Responsible Role, Date Completed)
  * **D. Description & Classification**
    (Rich text description editor, severity dropdown, VIQ reference fields, SFI code ref)
  * **E. Cause Analysis**
    (Immediate Cause, Root Cause, Explanation fields)
  * **F. Deferment & Reporting**
    (Deferment check, Third-party check, Drydock specification link)
  * **G. Corrective Actions**
    (Action table + Add Action button)
  * **H. Attachments & Submit**

---

#### 3. **Left-Side Step Indicator**

* Add the **left vertical step panel** like in the Incident form:

  * Each major section (A–H) should appear as a clickable step.
  * Current step should be highlighted with blue accent and icon (circle/line connector).
  * The panel should scroll along with content but remain fixed on desktop.

---

#### 4. **Field Positioning & Alignment**

* Align labels and input fields in a **two-column grid** (label on left, input on right) as in the Incident form.
* Maintain **uniform padding** and equal column width.
* Avoid full-width inputs unless necessary (like Description text editor).
* Ensure form height consistency across sections.

---

#### 5. **Color Scheme & Buttons**

* The current Defect form uses plain white and blue accents; the Incident form uses **color-coded severity indicators** and **consistent blue-gray backgrounds** for sections.
* **Instruction:**

  * Use the same color tone and border styling as the Incident Report (light gray background, blue section headers).
  * Apply color-coded severity (e.g., Red = Major, Yellow = Minor, Green = Observation).
  * Align Save/Submit buttons bottom-right, consistent with Incident form.
  * Replace plain text “SAVE” and “SUBMIT” buttons with unified design (blue with white text, rounded edges).

---

#### 6. **Typography & Label Style**

* Incident form uses bold section headers (A., B., C.) and smaller field labels below them.
* Defect form uses uniform small text — visually monotonous.
* **Instruction:**

  * Use **bold uppercase headers** for section titles.
  * Keep label fonts slightly smaller (e.g., 14px regular weight).
  * Add subtle divider lines between sections.

---

#### 7. **Rich Text Description Field**

* The text editor for Description in the Incident form has **toolbar icons (bold, underline, image, bullets)** with good spacing and a clean border.
* The Defect form’s editor toolbar looks compressed and inconsistent.
* **Instruction:**

  * Use the same toolbar layout, font, and button spacing as the Incident form’s text editor.
  * Maintain uniform padding above and below the editor.

---

#### 8. **Action Table Consistency**

* The “ACTIONS” table in the Defect form appears abruptly at the end with inconsistent spacing.
* In the Incident form, each list (team members, actions, events) is **inside a boxed section** with uniform borders.
* **Instruction:**

  * Wrap the Corrective Actions table inside a bordered section with header “G. Actions Taken”.
  * Add blue-bordered “+ Add Action” button (same as “+ Add Team Member” in Incident form).
  * Keep text and date alignment consistent.

---

#### 9. **Icons & Visual Cues**

* Incident form uses icons beside step names and inputs (calendar, clip, checkbox indicators).
* Defect form lacks these.
* **Instruction:**

  * Add appropriate icons beside date fields, dropdowns, and file uploads.
  * Use the same icon pack (Feather/Lucide as used in Incident form).

---

#### 10. **Scrollable Workflow**

* Incident form supports progressive saving (save per section).
* Defect form currently has one global “Save”.
* **Instruction:**

  * Add per-section “Save & Continue” and global “Submit” at final step.
  * Retain form data persistently between sections.

---

Would you like me to now draft this into a **ready-to-send Replit system prompt** (that directly instructs the model to rebuild the form layout accordingly)?
It will contain structured commands (no commentary) so you can copy-paste it into Replit’s chat directly.
