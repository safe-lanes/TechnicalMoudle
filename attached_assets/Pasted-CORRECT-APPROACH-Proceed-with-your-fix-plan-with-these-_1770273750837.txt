CORRECT APPROACH - Proceed with your fix plan with these clarifications:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONFIRMED ROOT CAUSE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ PDF: Calls /technical/api/reports/unplanned-breakdown-jobs (CORRECT)
❌ Excel: Calls /technical/api/reports/unplanned-jobs (WRONG - old endpoint)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX REQUIRED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Location: client/src/pages/reports/MaintenanceReports.tsx
Line: ~1059 (Excel endpoint mapping)

CHANGE FROM:
case 'unplanned-jobs':
  endpoint = '/technical/api/reports/unplanned-jobs';
  break;

CHANGE TO:
case 'unplanned-jobs':
  endpoint = '/technical/api/reports/unplanned-breakdown-jobs/excel';
  // OR simply reuse the same endpoint as PDF:
  endpoint = '/technical/api/reports/unplanned-breakdown-jobs';
  break;

ALSO ENSURE:
The request includes the same parameters as PDF:
- vesselId
- startDate 
- endDate

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BACKEND ENDPOINT OPTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Option 1 (Recommended - Simpler):
Use the SAME endpoint for both PDF and Excel:
/technical/api/reports/unplanned-breakdown-jobs

The endpoint should:
- Accept query parameters: vesselId, startDate, endDate
- Return JSON with data + metadata
- Frontend decides whether to generate PDF or Excel from the same data

Option 2 (If backend requires separate endpoints):
Create /technical/api/reports/unplanned-breakdown-jobs/excel
- Same query as the main endpoint
- Returns Excel file directly instead of JSON

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CRITICAL REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Both PDF and Excel MUST:
1. ✅ Call the same API endpoint (or equivalent query)
2. ✅ Use the same WHERE clause with status = 'Completed'
3. ✅ Use the same date range filtering
4. ✅ Return the same number of records
5. ✅ Show the same 11-12 columns structure
6. ✅ Include: WO Number, Comp Code, Component Name, Job Title, Description, Created Date, Completed Date, Performed By, Hours, Manhours, Department

REMOVE from Excel (currently showing but not needed):
❌ Job Code (use work_order_no instead)
❌ Status (all are Completed, redundant)
❌ Due Date (unplanned jobs don't have due dates)
❌ Last Done (not relevant for unplanned jobs)
❌ Days Left / Days Overdue (not applicable)
❌ Next Due RH / Current RH / RH Remaining (not applicable)
❌ Critical Equip (not the focus of this report)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TESTING VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After fix, verify:
1. Generate PDF - note the number of records shown
2. Generate Excel - should show SAME number of records
3. Compare first row data - should be IDENTICAL
4. Verify column structure matches

PROCEED WITH THE FIX NOW.