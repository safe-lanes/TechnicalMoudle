FIX UI DISPLAY FOR RUNNING HOUR BASED WORK ORDERS
text
TASK: IMPROVE UI CLARITY FOR RUNNING HOUR BASED WORK ORDERS

CONTEXT:
You've confirmed the backend logic is correct. Running hour based jobs properly use nextDueRH and don't rely on calendar dates for Due/Overdue status. However, there's a UI clarity issue:

PROBLEM:
The "Due Date" column in the work orders table shows dates for ALL work orders, including running hour based ones. For RH-based jobs, this column might show stale/irrelevant calendar dates from old corrupted data, which confuses users.

SOLUTION NEEDED:

1. **Fix the "Due Date" Column Display**:

In the work orders table (both Planned and Completed views), modify the "Due Date" column to show:

**For Calendar-based jobs:**
- Show the actual due date: "17-Feb-2026"

**For Running Hour based jobs:**
- Show the RH target instead: "Next Due: 6500 RH"
- OR show: "—" (blank/dash) in the Due Date column
- AND add the RH info in a tooltip or sub-text

**Implementation:**

Update the table rendering logic (likely in `/pms/work-orders` page component):

```javascript
// In the work orders table render
{
  header: "Due Date",
  cell: (workOrder) => {
    if (workOrder.maintenanceBasis === "Running Hours") {
      // Show RH target instead of calendar date
      return workOrder.nextDueRH 
        ? `${workOrder.nextDueRH} RH`
        : "—";
    } else {
      // Show calendar date for calendar-based jobs
      return workOrder.dueDate 
        ? formatDate(workOrder.dueDate) 
        : "—";
    }
  }
}
Add Visual Indicator:

Add a badge or icon to distinguish RH-based jobs:

javascript
// In the Component column or Job Title column
{
  header: "Job Title",
  cell: (workOrder) => {
    return (
      <div>
        {workOrder.jobTitle}
        {workOrder.maintenanceBasis === "Running Hours" && (
          <Badge variant="outline" className="ml-2">
            RH
          </Badge>
        )}
      </div>
    );
  }
}
Update Tooltip/Hover Details:

When users hover over a running hour based job, show:

"Maintenance Basis: Running Hours"

"Next Due RH: 6500 hours"

"Current RH: 6150 hours"

"Remaining: 350 hours"

Clean Vessel 3 Data (Optional Data Migration):

If you want to clean the old corrupted data in Vessel 3:

javascript
// One-time data cleanup script
const workOrders = await WorkOrder.find({
  vessel: "Vessel 3",
  maintenanceBasis: "Running Hours"
});

for (const wo of workOrders) {
  wo.dueDate = undefined; // Remove stale calendar dates
  await wo.save();
}

console.log(`Cleaned ${workOrders.length} running hour work orders`);
DELIVERABLES:

Modified work orders table to show "X RH" in Due Date column for RH-based jobs

Visual badge/indicator to distinguish RH jobs from calendar jobs

(Optional) Data cleanup script for Vessel 3

Please implement these UI improvements and let me know when they're deployed.