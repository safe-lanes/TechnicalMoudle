Please review the PMS > Work Orders (W.O) module table and confirm whether the following enhancement is possible without changing the current UI theme, colors, layout style, or overall visual design. The existing Work Orders table currently uses fixed-width column headers and wrapped text, which increases row height when content is long. I want to check whether this can be improved with a more user-friendly table behavior similar to Excel.

First, I want to know whether it is possible to make the table columns adjustable by the user, so the user can drag and resize column widths manually, similar to how columns are resized in Excel. This should apply to the Work Orders module tables and should work naturally within the existing table structure. The objective is to allow users to widen or reduce column widths depending on the content they want to read.

Second, I want the text inside table cells to remain on a single line by default, without wrapping. The reason is that currently long values may wrap into multiple lines and increase row height, which makes the table look uneven and harder to scan. I want row height to remain consistent across the table. If a column is too narrow, the text can remain partially hidden or truncated initially, but once the user increases the column width, the full text should become visible in the same row rather than wrapping into the next line.

Third, please check whether sorting can be added to all column headers in the Work Orders module table, similar to the sorting behavior already available in the Work Order Planner table. I want each relevant column header to support sorting in ascending and descending order where applicable, while maintaining the existing style and appearance of the current module.

Important requirements:

Do not change the existing color scheme.

Do not change the overall UI design, styling, spacing concept, or current page identity.

Keep the current PMS look and feel exactly the same.

Add the enhancement in a way that feels native to the existing module, not like a new table design.

Please evaluate and implement the following if feasible:

User-adjustable column width using drag-to-resize behavior.

Single-line cell content with no wrapping.

Consistent row height across records.

Full text visibility when the user expands the column width.

Sorting on all applicable column headers, similar to the Work Order Planner table.

No disruption to pagination, search, filters, export, or existing table actions.

Please also confirm any technical limitations if there are constraints with the current table component, and if needed, suggest the cleanest way to achieve this while preserving the current UI unchanged. If sorting already exists in the planner table through a reusable component or logic, please reuse the same pattern in the Work Orders table.

Acceptance expectations:

Column widths can be adjusted manually by the user.

Text does not wrap inside cells.

Row height remains uniform.

Expanding a column reveals more of the full text.

Sorting is available in all appropriate headers.

Existing UI colors, styling, and module appearance remain unchanged.

Existing functionality such as filters, pagination, export, and actions continues to work correctly.