Great work! These skeletons are solid. I approve them with the following refinements before you proceed to full build:

✅ Approved Components:
System Prompt - Maritime terminology and tone are perfect

Architecture - Tool-calling pattern with OpenAI is correct

Chat UI - Side panel design matches requirements

useChat hook - Good state management pattern

🔧 Required Refinements:
1. chatbotService.ts - Tool Execution
Issue: The tool execution is calling storage methods directly, but based on your existing API structure, we should call the actual REST endpoints internally instead.

Fix: Change executeTool to use internal API calls that match your existing routes:

typescript
case "get_work_orders":
  // Instead of: return await storage.getWorkOrders(...)
  // Do: Call the existing internal endpoint
  const response = await fetch(`http://localhost:${process.env.PORT}/technical/api/work-orders?vesselId=${args.vesselId}&status=${args.status || ''}`);
  return await response.json();
OR better: Import and reuse your existing storage/service layer functions if they're already abstracted. Can you confirm which pattern you prefer?

2. chatbotService.ts - Add Missing Core Tools
Please add these tool definitions to the CHATBOT_TOOLS array:

typescript
{
  type: "function",
  function: {
    name: "get_due_work_orders",
    description: "Get work orders that are due (not yet overdue) for a vessel",
    parameters: {
      type: "object",
      properties: {
        vesselId: { type: "string" },
        dateRange: { type: "string", description: "Optional: 'week', 'month', or specific date range" }
      },
      required: ["vesselId"]
    }
  }
},
{
  type: "function",
  function: {
    name: "get_work_order_detail",
    description: "Get detailed information about a specific work order by ID",
    parameters: {
      type: "object",
      properties: {
        workOrderId: { type: "string" }
      },
      required: ["workOrderId"]
    }
  }
},
{
  type: "function",
  function: {
    name: "get_critical_spares",
    description: "Get spares that are both low stock AND marked as critical",
    parameters: {
      type: "object",
      properties: {
        vesselId: { type: "string" }
      },
      required: ["vesselId"]
    }
  }
}
And their corresponding cases in executeTool.

3. chatbot.ts Route - Add Auth Middleware
Your existing routes use auth middleware. Add it to the chatbot route:

typescript
import { requireAuth } from "../middleware/auth"; // Adjust path as needed

router.post("/technical/api/chat", requireAuth, async (req: Request, res: Response) => {
  // ... existing code
});
Also, get the actual user role from req.user instead of hardcoding:

typescript
const context = {
  ...req.body.context,
  userRole: req.user?.role || "Ship" // Use authenticated user's role
};
4. System Prompt - Add Response Format Examples
Enhance the system prompt with specific formatting guidance:

typescript
## Response Format Examples

When listing work orders:
Overdue Work Orders (8)

WO-1234 - ME Piston And Rings Inspection (500H) - Overdue by 15 days

WO-1235 - Provision Ref. Compressors Inspection (15000H) - ⚠️ CRITICAL - Overdue by 30 days
...

text

When summarizing KPIs:
PMS Status for Vessel 1

⚠️ Overdue: 8 work orders (need immediate attention)

📋 Due: 151 work orders

✅ Completed: 6 work orders this period

🔧 215 spares are low stock (87 at minimum, 128 below minimum)

Would you like me to:

Show overdue work order details

Navigate to low stock spares

Draft a briefing for the superintendent

text
undefined
5. useChat.ts - Get User Role from Auth Context
Change this line:

typescript
userRole: "Ship" // TODO: Get from auth context
To:

typescript
import { useUser } from "@/hooks/useUser"; // or wherever your auth context is

const { user } = useUser();
// ... later in sendMessage:
userRole: user?.role || "Ship"
6. Missing Components
You listed ChatMessage.tsx and SuggestedPrompts.tsx but didn't provide skeletons. Please provide those:

ChatMessage.tsx - Should handle:

User vs assistant message styling

Markdown rendering (for tables, lists, bold)

Tool usage indicators (optional debug info)

SuggestedPrompts.tsx - Should render:

Clickable prompt chips

Different styles for primary vs secondary

Disabled state when loading

7. Wire Into Main App
Show me how to:

Mount the chatbot router in server/routes.ts

Add <ChatProvider> and <ChatButton> to client/src/App.tsx

Create ChatButton.tsx (floating button bottom-right)

Questions Before You Proceed:
API Calls in Tools: Should tools call internal REST endpoints, or directly use storage layer functions? (I recommend storage layer if it's already abstracted cleanly)

OpenAI Model: You specified gpt-4o - is that correct, or should it be gpt-4-turbo or gpt-4-0125-preview?

Replit AI Integration: You mentioned using Replit's built-in OpenAI integration. Do I need to change the import or initialization? (e.g., does Replit inject API keys automatically?)

Once you address these refinements and answer the questions, I'll give final approval and you can start the full implementation in Build mode.