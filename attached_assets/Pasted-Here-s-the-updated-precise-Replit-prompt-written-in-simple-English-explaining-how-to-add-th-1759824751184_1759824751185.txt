Here’s the **updated, precise Replit prompt** — written in simple English — explaining how to add the **Condition of Class (CoC)** checkbox in the defect form and how the sidebar CoC icon should work.

---

## 🧩 Replit Prompt – Add “Condition of Class (CoC)” Checkbox and Sidebar Display

### 🎯 Objective

We want to add a **Condition of Class (CoC)** checkbox in the **Defect Form**, and make the **CoC sidebar icon** show only those defects where this checkbox is ticked.

---

### 🧱 Step 1: Add CoC Checkbox in Defect Form

**Where:**
Inside the Defect Form (the same form used for creating or editing defects).

**Placement:**
Under the existing “Responsible Role” field, in the **Date column (right side)**, add a new small block labeled:

> **Condition of Class (CoC)**
> [ ] Tick if this defect is a Class Condition.

**Field type:** Checkbox
**Variable name:** `is_coc`
**Default value:** `false` (unchecked)
**Tooltip:** “Tick this box if the defect is related to a Condition of Class issued by the classification society.”

**Behavior:**

* When checked, the value `is_coc = true` must be stored in the database.
* When unchecked, `is_coc = false`.
* When editing a defect, the checkbox should reflect the saved state.
* In view-only mode, show `CoC: Yes` or `No`.

---

### 🗂️ Step 2: Update Database / Backend

**Add new field to defect table:**

```ts
is_coc: boolean // default false, cannot be null
```

**API updates:**

* `POST /defects` and `PUT /defects/:id` must accept and store `is_coc`.
* `GET /defects` must include `is_coc` in the response.
* Allow filtering via URL parameter:
  Example: `/defects?is_coc=true`

---

### 📋 Step 3: Display CoC Defects in Sidebar

**Existing sidebar icon:**
You already have the “📘 CoC – Condition of Class” icon in the sidebar.

**Expected behavior when clicked:**

1. Open a page with the same table layout as the main **Defect Log** screen.
2. Show only defects where `is_coc = true`.
3. The list should include both **Active** and **Resolved** tabs (like the main Defects screen).
4. Tabs:

   * **Active CoC** → defects with `status = Open` and `is_coc = true`
   * **Resolved CoC** → defects with `status = Closed/Resolved` and `is_coc = true`
5. If no CoC defects exist, show message:
   “No Condition of Class defects found.”

---

### 💡 Step 4: Highlight CoC in Defect List

* In the main Defect Log table, add a small blue badge labeled **“CoC”** beside the Category name for items where `is_coc = true`.
* Example:
  **Category:** `Defect  🔵 CoC`

This helps users quickly identify which defects are also marked as CoC.

---

### ⚙️ Step 5: Reports and Export (Optional)

* Add a new column in the CSV/Excel export named **Condition of Class**, showing “Yes” or “No”.
* Allow report filters for CoC (example: “Show only CoC defects”).

---

### ✅ Test Flow After Implementation

1. Open **New Defect Form** → Tick **Condition of Class (CoC)** → Save.
2. Open **Defect Log** → Defect appears normally with **CoC** badge.
3. Click **CoC (Condition of Class)** in the sidebar → Only CoC-marked defects appear.
4. Closing a CoC defect moves it from **Active CoC** → **Resolved CoC** tab.
5. Unticking CoC in Edit mode removes it from the sidebar list.

---

Would you like me to include this CoC checkbox visually **aligned at the bottom of the “Date” column (right side)** or **below “Defect Type” on the left side**, to make the form more balanced?
(Your earlier feedback about alignment will matter here — I can finalize the layout accordingly.)
