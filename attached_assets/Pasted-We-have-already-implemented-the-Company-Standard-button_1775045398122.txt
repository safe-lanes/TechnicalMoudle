We have already implemented the Company Standard button and popup on the Lead Time & Grace Period Settings page, and the page now shows a saved company-standard summary below the button. The current page visibly shows a summary like “Fixed 7 days for WOs due in last week of month; Month End for remaining WOs” and vessel list summaries like “Lead: 7d / 50hrs,” which means company-level grace is now partly configurable, but company-level lead time is still not configured through the same control.

We now need to enhance the Company Standard popup so that it becomes a complete company-level default configuration for both:

Lead Time settings

Grace Period settings

for both:

Calendar-Based Jobs

Running Hours-Based Jobs

Main requirement
The Company Standard popup must no longer handle only grace configuration for calendar-based jobs.

It must now support:

Lead Time for Calendar-Based Jobs

Grace Period for Calendar-Based Jobs

Lead Time for Running Hours-Based Jobs

Grace Period for Running Hours-Based Jobs

This is necessary because lead time is what controls when work orders move from Planned to Due, and this needs a company-standard default just like grace rules. The current page title already says Lead Time & Grace Period Settings, and vessel summaries already display lead values, so the company-standard configuration must cover both concepts consistently.

Business interpretation to follow
The company-standard popup should define the default settings that apply when vessel-specific settings are not overriding them.

Important:

Vessel-specific settings can override company standard later.

For now, this popup should act as the default company-level rule source.

It must cover both calendar-based and RH-based default settings.

Required popup enhancement
Redesign the Company Standard popup into two clear sections:

Section 1: Calendar-Based Jobs
This section should allow configuration of:

Lead Time

Grace Period

Section 2: Running Hours Based Jobs
This section should allow configuration of:

Lead Time

Grace Period

The popup should clearly separate these sections so users immediately understand which values apply to which job type.

1. Calendar-Based Jobs section
This section must contain:

A. Lead Time settings
Allow user to define the company-standard lead time for calendar-based jobs.

Required fields:

Lead Time for Critical Jobs — numeric input in days before due

Lead Time for Non-Critical Jobs — numeric input in days before due

This is important because lead time is what determines when a Planned WO becomes Due. The current vessel popup already uses this structure with separate critical and non-critical lead times for calendar jobs, so the company-standard popup should follow the same logic for consistency.

B. Grace Period settings
Keep and enhance the grace settings already created for calendar-based jobs.

Use the same grace-method structure already implemented:

Grace method
Use radio buttons, one selection only:

Fixed Days

Month End

Specific Date of Next Month

Scope
Use radio buttons, one selection only:

Apply to all Work Orders

Apply only to WOs due in last week of month

Conditional fallback
If scope = Apply only to WOs due in last week of month, show an extra numeric input for:

Grace period for remaining WOs not due in last week of month

This fallback can remain fixed days only for now.

2. Running Hours Based Jobs section
Add a completely separate section for RH jobs.

This section must contain:

A. Lead Time settings
Allow company-standard lead time for RH-based jobs.

Required fields:

Lead Time for Critical Jobs — numeric input in hours before due

Lead Time for Non-Critical Jobs — numeric input in hours before due

This is required because RH jobs also move from Planned to Due based on lead time, and currently the page already uses RH lead values in vessel summaries such as 50hrs.

B. Grace Period settings
Allow company-standard grace period for RH-based jobs.

Required field:

Grace Period — numeric input in hours after due

This should be simple and direct. RH grace does not need the same month-end / next-month-date complexity as calendar-based jobs.

So for RH grace:

keep it as numeric hours only,

e.g. 168 hours after due.

Popup layout expectations
The enhanced Company Standard popup should be structured and easy to scan.

Recommended hierarchy:

Company Standard Settings

Calendar-Based Jobs

Lead Time

Grace Period

Running Hours Based Jobs

Lead Time

Grace Period

Save / Cancel

Do not make it feel cluttered. Use section headings and spacing similar to the existing vessel popup pattern.

Data model / persistence expectation
The company-standard configuration should now store and return all of the following:

Calendar lead time
calendarLeadDaysCritical

calendarLeadDaysNonCritical

Calendar grace settings
calendarGraceMethod

calendarGraceValue

calendarGraceScope

calendarFallbackGraceDays (when last-week-only scope selected)

RH lead time
rhLeadHoursCritical

rhLeadHoursNonCritical

RH grace settings
rhGraceHours

The currently saved company-standard summary on the page only reflects part of the grace rule, so after this enhancement the data model and summary generation need to include both lead and grace values. The page already shows company standard summary and vessel lead summaries, so this richer structure will fit the page naturally.

Summary display after save
After saving, the page should show a clearer summary below the Company Standard button.

Instead of showing only the calendar grace summary, show a compact but useful summary including both lead and grace.

Example style:

Calendar Lead: 7d / 14d; Calendar Grace: Fixed 7 days for last-week WOs, 30 days for remaining WOs

RH Lead: 50h / 100h; RH Grace: 168h

Or a similarly compact readable format.

The summary should make it obvious that both calendar and RH defaults are configured.

Functional behavior expectations
Calendar lead time
These values will later be used as company defaults for:

when Planned calendar WOs move to Due,

if vessel-specific lead settings are absent or not overriding.

Calendar grace
These values will later be used as company defaults for:

when calendar WOs remain Grace P,

when they finally become Overdue,

if vessel-specific settings use Company Standard mode.

RH lead time
These values will later be used as company defaults for:

when Planned RH WOs move to Due,

if vessel-specific RH lead settings are absent or not overriding.

RH grace
These values will later be used as company defaults for:

RH grace behavior after due point,

if vessel-specific RH grace settings are absent or not overriding.

Important scope note
This task is to enhance the Company Standard popup and functionality only.

Do not yet redesign the vessel-specific popup in this task.

However, make sure the saved company-standard values are structured in a way that vessel-specific settings can later reference them cleanly as defaults.

UI behavior details
Calendar grace method
Fixed Days -> show numeric days input

Month End -> no extra main numeric input

Specific Date of Next Month -> show numeric day-of-next-month input

Calendar grace scope
Apply to all WOs -> no fallback field

Apply only to WOs due in last week of month -> show fallback numeric days input for remaining WOs

RH grace
always numeric hours input only

Lead times
all lead fields should be simple numeric inputs

show units clearly:

days before due

hours before due

hours after due

Validation expectations
Please validate and test these cases:

Calendar
Company-standard calendar lead values save and reload correctly.

Company-standard calendar grace rule saves and reloads correctly.

Last-week-only scope correctly reveals fallback field.

Summary reflects saved calendar lead + grace values.

RH
RH critical/non-critical lead values save and reload correctly.

RH grace hours save and reload correctly.

Summary reflects RH lead + RH grace values.

Overall
Company Standard button still opens popup correctly.

Existing saved company-standard grace data migrates safely into the enhanced structure.

No existing vessel settings are unintentionally changed.

Implementation guardrail
Please do not implement this as only a UI change.

The popup must be enhanced structurally so the company-standard configuration actually stores and returns both:

lead settings,

grace settings,
for both calendar and RH jobs.

The page is named Lead Time & Grace Period Settings, and the vessel list already reflects lead-time concepts, so company standards must now match that same operational model.

Final expected result
After this enhancement:

Company Standard popup configures full default settings, not just partial grace logic.

Calendar-based jobs have default company lead + grace settings.

Running-hours-based jobs have default company lead + grace settings.

Saved summary reflects both job types.

This becomes the logical company-level foundation before vessel-specific override enhancement is tackled later.