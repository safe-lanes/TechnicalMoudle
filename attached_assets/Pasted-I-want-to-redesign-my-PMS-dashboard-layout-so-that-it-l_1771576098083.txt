I want to redesign my PMS dashboard layout so that it looks professional, consistent, and user‑friendly at first glance.
Do NOT change any backend logic or data; only change layout, grouping, and visual presentation.

Main problems today:

The first view feels messy and “empty” at the same time.

Tiles/cards are of different sizes and shapes, which makes the layout look unstructured.

Important KPIs are not clearly separated from secondary details.

Overall goals:

Create a clear Executive Summary at the top with a small set of key KPIs.

Use consistent card sizes and alignment.

Make it obvious what is most important vs secondary.

Reuse the existing numbers and charts; just present them better.

Create a new “Executive Summary” band at the top

Right below the page title/context bar, create a single band/section called “Executive Summary”:

This band should have a subtle background (very light neutral color) and padding, so it looks like one cohesive block.

Inside this band, create a uniform grid of cards, all same height and width on desktop.

In this band, include:

a) Scope summary card

Title: “Scope”

Content example (using existing data):

First line: “[Fleet]” or “[Vessel name]” depending on view mode.

Second line: “Total WOs: [number] - Spares: [number] - Stores: [number]”.

This should be a small, left‑aligned card.

b) Maintenance health card

Title: “Maintenance health”

Big number or score based on existing KPIs, for now just text like:

First line (big): “Overdue: [overdue count]”

Second line (smaller): “[overdue % of total] of all work orders - Due this week: [value if available]”.

This card size should match other cards in the grid.

c) Risk & compliance card

Title: “Risk & compliance”

Use existing numbers, such as: critical overdue WOs (if you have a field) and critical low‑stock spares.

First line (big): “Critical issues: [number]”.

Second line (small): “Critical overdue WOs + critical low‑stock spares”.

Same size as Maintenance health card.

d) Trend summary card

Title: “Trend”

Reuse existing “Outstanding tasks %” and “% vs last month”.

First line (big): e.g. “Backlog: 66% outstanding”.

Second line (small): “Change vs last month: +24% (worsening/improving)”.

Same size as other cards.

e) Two compact charts (optional but still within the band)

Place two small charts side by side below or next to the cards while keeping layout clean:

Work Order Status Distribution (donut/bar).

6‑Month Maintenance Trend (small line/area chart).

These charts should be visually aligned and sized consistently (same height).

The key point: All cards and these charts should be in a clean grid, with consistent width, height, and spacing, so nothing looks random.

Standardize card/tile sizes and shapes

Use one standard card component for all KPI tiles in the Executive Summary and in the other bands.

All KPI cards in the same row should have:

Same height.

Same padding.

Same border radius and shadow.

Titles should be the same font size and weight across all KPI cards.

Main number text should be a single consistent size (e.g., larger and bold).

Secondary text should be a single smaller size and lighter color.

Reorganize the existing bands under the Executive Summary

Below the Executive Summary, keep three bands but make them clean and consistent:

a) “Work-order health” section

Title: “Work-order health”.

Below the title, show a row of same-sized KPI cards: Overdue, Due, Pending approval, Completed, Total WOs.

Under this row, place the “Overdue Work Orders” list, with a small subtitle “Overdue Work Orders (Top N of [total])”.

b) “Maintenance effectiveness” section

Title: “Maintenance effectiveness”.

Show KPI cards related to completion %, outstanding tasks, etc., again using same card style and size.

Optionally place the 6‑month trend chart here if it is not already in the top band.

c) “Inventory health” section

Title: “Inventory health”.

Use a consistent grid of cards: Components, Spares inventory, Stores inventory, Low stock counts.

Under these cards, show the “Low Stock Spares” list with a subtitle like “Low Stock Spares (Sample from [total])”.

Clean up spacing and alignment

Reduce large random gaps between sections; instead use a consistent vertical spacing between:

Executive Summary band.

Work‑order health section.

Maintenance effectiveness section.

Inventory health section.

Ensure all section titles are left‑aligned and use the same style.

Ensure cards align to a clear grid (same number of columns on desktop, for example 3 or 4).

Visual polish

Add small but consistent icons to KPI cards (e.g., clock, checklist, box) but keep them aligned to the left or top right in the same place in each card.

Use consistent colors for status concepts:

Red (or danger) only for high‑risk states like Overdue and Critical.

Amber for “Due soon” or Watch.

Green for Completed/OK.

Avoid having some cards full width and some very narrow; choose a fixed column layout (for example, 3 or 4 columns) and stick to it on desktop.

Responsiveness

Desktop/wide:

Executive Summary at top with cards and compact charts in a tidy grid.

Below that, sections stacked vertically, each with rows of uniform cards.

Tablet/mobile:

Cards stack to fewer columns (2 or 1), but maintain consistent card style.

Executive Summary still appears as the first block.

Do not

Do not change how any KPI is calculated.

Do not remove existing lists or charts, only relocate and resize them as described.

Do not introduce new dummy KPIs; only reuse existing data in a clearer way.

At the end, please briefly describe:

Which React/layout components you modified to create the Executive Summary band and the standardized card grid.

How many columns you used for the card grid on desktop and how it behaves on smaller screens.