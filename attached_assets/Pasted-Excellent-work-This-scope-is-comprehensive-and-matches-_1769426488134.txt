Excellent work! This scope is comprehensive and matches real-world maritime PMS use-cases perfectly.

I approve this capability map with a few refinements:

Approved Scope - Proceed with Implementation
Phase 1 (Read-Only - Build Now):

All 7 categories as defined, focusing on these core tools first:

Essential Tools (implement these first):

get_work_orders({ vesselId, status?, componentCode?, dateRange?, limit? })

get_work_order_detail({ workOrderId })

get_overdue_work_orders({ vesselId })

get_due_work_orders({ vesselId, dateRange? })

get_work_order_counts({ vesselId })

get_low_stock_spares({ vesselId })

get_critical_spares({ vesselId })

get_components({ vesselId, critical?, search?, parentId? })

get_running_hours({ componentId })

get_jobs({ vesselId, componentId? })

generate_deep_link({ page, filters })

High-Priority Gap Tools (add these after core tools work):

get_spare_detail({ spareId })

get_spares_by_component({ componentId })

get_stores({ vesselId, lowStock? })

Approved Suggested Prompt Chips:

Primary (always visible):

"Show overdue work orders"

"Low stock spares"

"PMS status summary"

"What's due this week?"

Secondary (contextual):

"Critical components"

"Running hours check"

"Draft maintenance briefing"

"What should I prioritize?"

Key Requirements for Implementation:
System Prompt Design:

The chatbot should understand maritime/PMS terminology (Chief Engineer, Main Engine, ROB, running hours, class surveys, etc.)

Should be concise and action-oriented (vessel crew are busy)

Should offer to create deep links after answering questions

Should suggest follow-up questions naturally

Response Format:

Use tables/lists for multiple items

Highlight critical/urgent items clearly

Include relevant numbers/KPIs

Always end with "Would you like me to..." suggestions

Context Awareness:

Automatically use the selected vessel from context

Reference the current page when relevant

Remember conversation history within session

Error Handling:

If a tool returns no results, suggest alternatives

If user asks about future Phase 2 features (write actions), explain "I can't do that yet, but I can show you..."

Now Please Proceed With:
Create the detailed implementation task list with specific file-by-file steps

Design the system prompt template with maritime context

Show me example TypeScript skeletons for:

server/routes/chatbot.ts (the POST /technical/api/chat endpoint)

server/services/chatbotService.ts (tool execution + OpenAI integration)

client/src/components/chat/ChatPanel.tsx (the side panel UI)

client/src/hooks/useChat.ts (chat state management)

Once I see these skeletons, I'll review them and then you can start creating the full implementation