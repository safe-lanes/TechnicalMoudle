Raplit Prompt — Align Spares Consumption Pattern Analysis with Stores Consumption Pattern Analysis
Change Type

UI + Functional Alignment

🎯 Objective

The Spares → Consumption Pattern Analysis Report must have:

Same functionality

Same filters

Same calculations

Same layout structure

Same UI design

Same user interaction behavior

As the:

Stores → Consumption Pattern Analysis Report


This is a full feature parity requirement.

📌 Source Reference Screen
Stores → Consumption Pattern Analysis Report


This screen will be treated as the Master Reference.

📌 Target Screen
Spares → Consumption Pattern Analysis Report


This must replicate the reference screen behavior exactly.

🎨 UI Alignment Requirements

The Spares report must match Stores report in:

1️⃣ Header Section

Same header style

Same color theme

Same title placement

Same breadcrumb alignment

2️⃣ Filter Section

All filters available in Stores report must exist in Spares report with:

Same placement

Same spacing

Same alignment

Same dropdown style

Same date picker behavior

Same button styling

Examples (if present in Stores):

Vessel selector

Date range

Category filter

Criticality filter

Analysis type

Grouping option

3️⃣ Grid / Table Layout

Table must match exactly:

Column header style

Column alignment

Sorting behavior

Column resizing

Pagination style

Export options

Scroll behavior

Row spacing

Font size

4️⃣ Charts / Graphs (If Available in Stores)

If Stores report contains:

Bar chart

Line chart

Trend analysis

Consumption comparison chart

Then Spares report must:

Use same chart type

Same layout positioning

Same legend behavior

Same tooltip style

Same axis formatting

⚙️ Functional Parity Requirements

Spares report must replicate:

Same calculation logic (adapted for spares dataset)

Same aggregation logic

Same grouping options

Same trend comparison logic

Same period comparison logic

Same export functionality (Excel / PDF / CSV if applicable)

Same performance behavior

🔎 Data Logic Clarification

Only dataset changes:

Stores → Stores inventory consumption
Spares → Spares inventory consumption

All calculation formulas must follow the same analytical model.

📐 Layout Structure

Screen structure must follow identical section order:

Header
Filters
Summary Section (if exists)
Charts (if exists)
Data Grid
Export Controls


Spacing and proportions must match exactly.

⚠️ Constraints

No redesign

No UI improvisation

No layout shift

No color deviation

No removal of existing Stores features

No backend restructuring unless required for parity

🧪 Validation Checklist

Before completion confirm:

Visual comparison shows near-identical layout

Functional testing produces same interaction behavior

Filters respond the same way

Charts behave identically

Export results format matches

📌 Scope

Only:

Spares → Consumption Pattern Analysis Report


No change in Stores report.

✅ Expected Outcome

Complete UI consistency

Functional parity

Unified reporting experience

Improved user familiarity

No confusion between Stores and Spares modules