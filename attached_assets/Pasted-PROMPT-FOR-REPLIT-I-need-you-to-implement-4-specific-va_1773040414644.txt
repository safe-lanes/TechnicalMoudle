PROMPT FOR REPLIT:

I need you to implement 4 specific validation and UX improvements in the Running Hours module of this PMS (Planned Maintenance System) application. These changes apply to two areas: the individual Update RH modal (opened via the gear icon on each row) and the Bulk Update RH modal (opened via the "+ Bulk Update RH" button). Do not touch any other module or page. Here are the 4 changes in detail:

Change 1: Prevent Running Hours from Decreasing in "Set Total" Mode

In both the individual Update RH modal and the Bulk Update RH modal, when the mode is set to "Set Total", the user is entering the new absolute total running hours for a component. Running hours on a machine can never go backwards — a component that has run 9,000 hours cannot suddenly show 5,000 hours unless it is a meter replacement.

Implement the following behavior:

When the user enters a New Value (in individual modal) or a Present Running Hours value (in bulk modal) that is less than the current/previous running hours, immediately show an inline validation warning in red text below that input field. The message should say something like: "New value cannot be less than current running hours (X hrs). Use 'Meter Replaced' if the meter was reset."

The Save button should be disabled or blocked for that specific row/component as long as this invalid condition exists.

This check must be done in real-time as the user types, not just on Save click.

This restriction applies only to "Set Total" mode. In "Add Delta" mode, a different check applies (see Change 2).

Exception: If the Meter Replaced checkbox is ticked for that component, this validation should be bypassed entirely, because a meter replacement legitimately resets the reading.

Change 2: Block Negative Values in "Add Delta" Mode

In both the individual Update RH modal and the Bulk Update RH modal, when the mode is set to "Add Delta", the user is entering the number of additional hours to add on top of the current running hours. A delta (increment) value should never be zero or negative — you cannot add negative running hours to a machine.

Implement the following behavior:

When the user enters a value that is zero or negative (e.g., 0, -1, -500) in "Add Delta" mode, immediately show an inline validation warning in red text below that input field. The message should say something like: "Delta value must be a positive number. Running hours can only increase."

The Save button should be disabled or blocked for that specific row/component as long as this invalid condition exists.

This check must be done in real-time as the user types, not just on Save click.

This applies to both the individual Update RH modal (the "New Value" field when mode is Add Delta) and every row's input in the Bulk Update RH modal.

Change 3: Auto-fill "Old Meter Final" with Current Running Hours When Meter Replaced is Checked

In the Bulk Update RH modal, each component row has a "Meter Replaced" checkbox. When this checkbox is ticked, two additional fields expand below that row: "Old Meter Final" and "New Meter Start".

Currently, "Old Meter Final" is blank and "New Meter Start" defaults to 0. The issue is that the "Old Meter Final" value should logically always be the last recorded reading of the old meter — which is the component's current running hours already stored in the system (shown in the "Previous Running Hours" column for that row).

Implement the following behavior:

When the user ticks the "Meter Replaced" checkbox for any component row, automatically pre-fill the "Old Meter Final" field with that component's current running hours value (the same value shown in the "Previous Running Hours" column of that row).

The field should remain editable — the user should be able to override the auto-filled value if needed, in case they want to record a slightly different final reading.

"New Meter Start" should continue to default to 0 as it does currently, since new meters typically start from zero. No change needed there.

This same auto-fill logic should also be applied in the individual Update RH modal if a "Meter Replaced" option exists or is added there in future. For now, focus on the Bulk Update modal.

If the user unticks the Meter Replaced checkbox after it was ticked, the expanded fields should collapse/hide again (existing behavior), and the auto-filled value should be cleared/reset for next time.

Change 4: Add Component Code Column in the Bulk Update RH Modal Table

In the Bulk Update RH modal, the inner table currently shows these columns: Component Name, Previous Running Hours, Present Running Hours, Meter Replaced, and Status. The Component Code is missing.

This is a problem because several components have very similar names — for example, there are three entries named "AE Turbochargers" (AE1, AE2, AE3 with codes 651.053, 652.053, 653.053) and multiple compressor units. Without the component code visible, a user can easily update the wrong component.

Implement the following behavior:

Add a "Component Code" column to the Bulk Update RH modal's table, positioned as the second column, right after "Component Name" and before "Previous Running Hours". So the column order becomes: Component Name → Component Code → Previous Running Hours → Present Running Hours → Meter Replaced → Status.

The Component Code values should be displayed as plain text (not necessarily a hyperlink, since this is inside a modal). They should be styled consistently with the rest of the table — same font size, same row height.

The column header should be labeled "Component Code" matching the same header style (same background color, font weight) as the other headers in that modal table.

Ensure the modal width accommodates this extra column cleanly — either slightly widen the modal or adjust column widths proportionally so nothing overflows or gets cut off.

General Instructions:

Do not change anything outside the Running Hours module (the /pms/running-hrs route and its associated components/modals).

Do not alter the visual design/theme, color scheme, or layout of the page beyond what is described above.

All validation messages should appear inline (below the relevant input field), not as popups or toast notifications.

Validation must work correctly for both the individual Update RH modal and the Bulk Update RH modal wherever applicable, as described per change above.

Test that existing working functionality (Status changing to "Ready to update", Meter Replaced expanding sub-fields, Mode toggle between Set Total and Add Delta, Date and Comments fields) continues to work without regression after these changes.

