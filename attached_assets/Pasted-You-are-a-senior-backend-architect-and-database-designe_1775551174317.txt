You are a senior backend architect and database designer.

I have an Excel file containing two sheets:
1. adm_available_ranks
2. adm_vessel_org_chart

Your task is to generate PostgreSQL schema based on these sheets with the following strict requirements:

-----------------------------------
GENERAL RULES
-----------------------------------
1. Use EXACT table names as given:
   - adm_available_ranks
   - adm_vessel_org_chart

2. Preserve ALL columns from the Excel sheet.
3. Infer appropriate PostgreSQL data types.
4. Add PRIMARY KEY where applicable.
5. Add UUID column support where present.
6. Use snake_case naming convention.
7. Add audit columns if not present:
   - created_at
   - updated_at
   - created_by
   - updated_by
   - is_deleted (boolean, default false)

-----------------------------------
TABLE: adm_available_ranks
-----------------------------------
1. This is the MASTER table for ranks.
2. Ensure:
   - id → PRIMARY KEY
   - ar_uuid → UNIQUE (UUID)
3. Columns like:
   - name
   - label
   - category
   - rank_id (external reference)
   - sort_order
   - is_system_rank
   - applicable_to_company
   - is_deleted
   - is_sync
   must be properly typed.

-----------------------------------
TABLE: adm_vessel_org_chart
-----------------------------------
1. This table defines hierarchy between ranks.
2. Use:
   - rank_id → child rank
   - parent_rank_id → parent rank
3. Add FOREIGN KEY constraints:
   - rank_id → adm_available_ranks(id)
   - parent_rank_id → adm_available_ranks(id)
4. Remove redundancy:
   - If "rank" (text) column exists, keep it but mark as optional or deprecated.
5. Add sort_order column support.

-----------------------------------
OUTPUT FORMAT
-----------------------------------
1. Provide pure PostgreSQL CREATE TABLE statements.
2. Include all constraints:
   - PRIMARY KEY
   - FOREIGN KEY
   - UNIQUE
3. Add indexes where useful:
   - UUID
   - rank_id
   - parent_rank_id

-----------------------------------
EXTRA REQUIREMENTS
-----------------------------------
1. Ensure schema is production-ready.
2. Ensure referential integrity.
3. Use DEFAULT values where appropriate.
4. Do NOT rename tables or columns.

-----------------------------------

Return only SQL schema. No explanation.