Task: Fix critical mismatch between PDF and Excel data sources in Maintenance & Work Orders reports

Issue
There is a critical data consistency problem in the Reports module. For Maintenance & Work Orders reports 1, 2, 3, 7, and 8, the PDF preview/export and the Excel export are not using the same data source or logic, so they can show different results for the same selected filters.

From current analysis, the issue is:

PDF preview/export uses frontend-filtered data based on /technical/api/work-orders

Excel export uses a separate backend service

Because of this split, the same report can produce different records in PDF vs Excel for the same vessel, period, and other filters

This is a critical issue because report exports must be fully consistent across formats. The Reports module has one shared global filter area and a single reporting workflow, so PDF and Excel must reflect the exact same filtered dataset for the same report.

Affected reports
Please check and fix this specifically for Maintenance & Work Orders reports:

Due Jobs (7 days)

Overdue Jobs

Completed Jobs Register

Job Postponement Log

Crew Workload Distribution

Objective
Unify the data source and filtering logic so that both PDF and Excel use the same backend service / same canonical dataset for each affected report.

Required fix
Please do the following only:

Identify how PDF data is currently being built for the affected reports.

Identify how Excel data is currently being built for the same reports.

Trace the source difference between:

frontend-filtered /technical/api/work-orders data

separate backend export service

Refactor the implementation so that:

both PDF and Excel use the same backend service

both formats use the same filter inputs

both formats use the same business logic

both formats generate records from the same report-specific dataset

Ensure that the following filters are applied identically in both export formats:

vessel

period

search

report-specific filters if applicable

Ensure that if the on-screen report is based on a derived dataset, the export should come from the same canonical backend logic rather than duplicating frontend filtering rules.

Verify that PDF row counts and Excel row counts match for the same filter combination unless there is an intentional formatting-only difference.

Important implementation rule
The backend must become the single source of truth for the affected reports.

That means:

Do not let PDF use one logic path and Excel use another.

Do not keep frontend-only filtering for PDF if Excel is backend-driven.

Do not duplicate report logic in two places.

Instead:

create or use one backend report service per report,

return the exact filtered dataset,

use that same dataset for both PDF and Excel generation.

Expected result
After the fix:

PDF and Excel exports for the same report must contain the same records for the same filters

no mismatch should exist between previewed/exported PDF data and Excel data

backend service should be the single source of truth

report export behavior should be predictable and uniform

Output I want from you
After fixing, provide:

which files were changed

what the root cause was

how the data flow now works

confirmation that PDF and Excel are now using the same backend source for reports 1, 2, 3, 7, and 8

Strict instruction
Do not change anything else.

no UI redesign

no table restructuring

no column changes

no filter redesign

no unrelated cleanup

no business rule changes except what is needed to unify PDF and Excel data source behavior

Only fix this export data inconsistency issue.