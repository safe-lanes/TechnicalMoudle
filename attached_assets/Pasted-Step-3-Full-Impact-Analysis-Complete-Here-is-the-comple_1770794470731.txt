Step 3 — Full Impact Analysis (Complete) Here is the complete refactor plan for migrating id from TEXT/UUID to INTEGER auto-increment: Database Impact — 31 tables with vessel_id columns: users, defect_sequences, running_hours_audit, components, ihm_items, ihm_maintenance_log, spares, spares_history, stores_ledger, stores_items, change_request, alert_events, alert_config, jobs, work_orders, work_order_executions, work_order_execution_details, defects, import_history, pms_vessel_settings, certificates, surveys, locations, spare_component_links, spare_location_stock, inventory_transactions, job_component_links, vessel_certificate_applicability, vessel_certificate_data, vessel_survey_applicability, vessel_survey_data Server Impact — 60 files, including: Core: postgresStorage.ts, routes.ts, storage.ts, initDb.ts, memStorage.ts Services: 7 service files (jobDueScanner, workOrderService, runningHoursService, etc.) All 7 V2 modules (repositories, services, controllers, routes) Middleware: auth.ts, validation.ts Frontend Impact — 89 files, including: Context providers: VesselContext.tsx, AuthContext.tsx, OfflineContext.tsx Hooks: useVessels.ts All 6 V2 API files 70+ page/component files across PMS, Defects, Reports, Admin, Spares, Stores Recommended Refactor Sequence (for Phase D): Create new migration: change id to SERIAL, update all 31 FK tables to reference new integer id Update VesselContext to use vuuid for external references, id (integer) for internal Update all API endpoints to accept/return vuuid where external identification is needed Update all frontend components/pages to use vuuid for vessel selection/filtering Update V2 module schemas and repositories Risk Areas: Dropdowns/filters that bind to vessel.id (need to switch to vuuid) URL parameters using UUID vessel IDs LocalStorage storing vessel UUID (VesselContext) Fleet vessel mapping joins Work order/defect numbering that uses vessel ID This is a large but well-defined refactor. I recommend tackling it in a dedicated session after reviewing the plan.