Current Behavior Summary
1. PMS Components Page (client/src/pages/pms/Components.tsx)
Initial State:

const [expandedNodes, setExpandedNodes] = useState<Set<string>>(new Set());

Tree starts completely collapsed (empty Set)
All 8 root categories are visible but closed
Expansion Logic:

Clicking a node toggles its expanded state
When a component is selected via URL param or search, the tree auto-expands parents to reveal it
Search filtering also expands matching branches
2. Spares Page (client/src/pages/spares/Spares.tsx)
Initial State:

const [expandedNodes, setExpandedNodes] = useState<Set<string>>(new Set());

Also starts completely collapsed (empty Set)
Same behavior as Components page
3. Component Tree Selector (client/src/components/ComponentTreeSelector.tsx)
Initial State:

const [expandedNodes, setExpandedNodes] = useState<Set<string>>(new Set());

Starts completely collapsed
Used in modals/pickers for selecting components
4. Add/Edit Component Form (client/src/components/AddEditComponentForm.tsx)
Initial State:

const [expandedSections, setExpandedSections] = useState<Set<string>>(new Set(["A"]));

Section A is initially expanded (different from tree!)
This is for the A-H form sections, not the component tree
No component tree selector inside this form
Current Behavior Consistency
Location	Tree Initial State	Section Panels Initial State
Components Page	Collapsed (empty Set)	Section A expanded by default
Spares Page	Collapsed (empty Set)	N/A
ComponentTreeSelector	Collapsed (empty Set)	N/A
AddEditComponentForm	N/A	Section A expanded by default
Observations
Tree is consistent - All component trees start collapsed across the app

Form sections differ - The A-H accordion sections in AddEditComponentForm start with Section A expanded, while the Component details panel on Components.tsx also has expandable sections

Auto-expand on selection - When navigating to a specific component (via URL or search), the tree auto-expands the path to that component

