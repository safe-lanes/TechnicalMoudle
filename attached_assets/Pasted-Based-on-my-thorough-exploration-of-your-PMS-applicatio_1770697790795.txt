Based on my thorough exploration of your PMS application, here's a technically detailed prompt for the Replit Agent:

TECHNICAL VALIDATION & IMPLEMENTATION PROMPT FOR CONSUMPTION PATTERN ANALYSIS
CRITICAL: Before implementing, answer these technical questions to ensure backend data availability:

1. DATABASE SCHEMA VALIDATION
Question 1: Audit Trail & Transaction History

Does the stores_ledger table (or equivalent transaction/history table) contain timestamped records for every CONSUME, RECEIVE, ADJUSTMENT, and INITIAL event?

Required fields verification:

date or createdAt (timestamp) - For time-series analysis

event_type or event (ENUM: CONSUME, RECEIVE, ADJUSTMENT, INITIAL)

item_code or part_code (FK to stores/lubes/chemicals)

item_name

qty_change (negative for consumption, positive for receiving)

rob_after (Remaining on Board after transaction)

uom (Unit of Measurement)

vessel_id (for multi-vessel filtering)

user_id or user (who recorded the transaction)

remarks (optional context)

If NO: How do you currently track historical consumption? Do you only store current ROB (Remaining on Board) without history? Without historical transaction data, monthly consumption rates cannot be calculated.

Question 2: Item Master Data

Are Stores, Lubes, Chemicals stored in:

Separate tables (stores, lubes, chemicals, others) OR

Single table with type discriminator (items table with item_type field)?

Do item records contain:

item_code (unique identifier)

item_name

category (Mechanical, Safety, Consumables, etc.)

uom (pcs, ltr, kg, etc.)

min_stock (minimum stock level)

current_rob (current stock - can be calculated from ledger)

location

Question 3: Work Order Consumption Linkage

Does the Work Order model have a related collection/table for "Spare Parts Consumed" (Section B4)?

Schema verification:

work_order_id (FK)

item_code or spare_part_id (FK to items)

qty_used (quantity consumed)

location (from which location consumed)

comments

created_at (timestamp)

If YES: Are these Work Order consumption records also written to the stores_ledger table for unified consumption tracking?

If NO: How do you reconcile WO consumption with stores ledger? Are they separate systems?

Question 4: Date Range & Data Retention

What is the earliest transaction date in your stores_ledger table?

Do you have at least 3-6 months of historical consumption data for meaningful pattern analysis?

Are there any data retention policies that purge old transactions?

Question 5: Current ROB Calculation

Is current_rob (Remaining on Board):

Stored field updated on each transaction, OR

Calculated field by summing all transactions from INITIAL?

For consumption rate calculations, we need to verify: Opening Stock + Received - Consumed +/- Adjustments = Current ROB

2. API & DATA ACCESS VALIDATION
Question 6: Existing API Endpoints

Do you have existing endpoints that query stores_ledger? List them.

Current endpoint patterns observed:

/api/stores/:vesselId - Get inventory

/api/stores/:vesselId/history - Get transaction history

Need confirmation: Can we filter history by:

Date range (startDate, endDate)

Event type (event_type: 'CONSUME')

Item categories

Item codes

Question 7: Performance Considerations

How many transaction records exist per vessel (approximate)?

Have you implemented database indexing on:

stores_ledger.date or createdAt

stores_ledger.vessel_id

stores_ledger.item_code

stores_ledger.event_type

If NO: Large date range queries may be slow. We should implement pagination or add indexes.

3. REPORT REQUIREMENTS CLARIFICATION
Question 8: Monthly Consumption Rate Calculation
To calculate "Monthly Consumption Rate" for each item:

text
Monthly Consumption = SUM(ABS(qty_change)) 
WHERE event_type = 'CONSUME' 
AND date BETWEEN start_of_month AND end_of_month
GROUP BY item_code, MONTH(date)
Confirm:

Should consumption rates be calculated as:

Average per month (total consumed / number of months)?

Month-by-month breakdown (Jan: 5 pcs, Feb: 8 pcs, Mar: 3 pcs)?

Both (monthly breakdown + rolling average)?

Question 9: Stock Turnover Ratio

text
Stock Turnover = Total Consumed in Period / Average ROB in Period
Problem: We need rob_after value from multiple points in time to calculate average ROB.

Does your stores_ledger capture rob_after for every transaction?

Or do we need to calculate it retrospectively from transaction sequence?

Question 10: Work Order Consumption Attribution

When spare parts are consumed via Work Orders (Section B4):

Are these automatically recorded in stores_ledger as CONSUME events?

Or is it manual entry - user must separately update stores inventory?

For Component/Equipment linkage:

Do CONSUME events store component_id or work_order_id for linking?

Can we join: stores_ledger → work_orders → components to show consumption by equipment?

4. IMPLEMENTATION APPROACH (Based on Answers)
IF historical data EXISTS in stores_ledger:

typescript
// Backend API Route: /api/reports/stores-consumption-analysis/:vesselId
GET /api/reports/stores-consumption-analysis/:vesselId?startDate=2025-01-01&endDate=2026-01-31&category=Mechanical

Response:
{
  summary: {
    totalItemsConsumed: number,
    totalQuantityConsumed: number,
    uniqueItemsConsumed: number,
    dateRange: { start, end }
  },
  consumptionTrends: [
    { month: "2025-01", totalQty: 125, itemCount: 15 },
    { month: "2025-02", totalQty: 98, itemCount: 12 }
  ],
  topConsumedItems: [
    { itemCode, itemName, category, totalQtyConsumed, avgMonthlyConsumption, uom }
  ],
  categoryBreakdown: [
    { category: "Mechanical", totalQty: 450, percentage: 45 }
  ],
  stockEfficiency: [
    { itemCode, itemName, stockTurnover, movementSpeed: "fast|slow", currentROB, minStock }
  ],
  forecastingData: [
    { itemCode, avgMonthlyConsumption, projectedNextMonth, reorderWarning: boolean }
  ],
  transactionDetails: [
    { date, itemCode, itemName, qtyConsumed, robAfter, user, workOrderNo, remarks }
  ]
}
IF historical data DOES NOT EXIST:
We need to implement:

Ledger system - Start recording all future transactions with timestamps

Data migration - Backfill historical data if any records exist elsewhere

Alternative approach - Use current ROB snapshots if periodic inventory audits were recorded

5. FINAL CHECKLIST BEFORE IMPLEMENTATION
Confirm:

 stores_ledger table exists with date, event_type, qty_change fields

 Transaction history has at least 3 months of data

 Can filter ledger by vessel_id, date range, event_type

 UOM handling - confirm all items have valid UOM for aggregation

 Work Order consumption is tracked (either in ledger or separate table)

 Component/Equipment linkage exists for consumption attribution

 Database indexes exist on commonly queried fields

 Excel export library available (exceljs or similar)

 PDF generation works for charts (puppeteer/html2pdf configured)

IMPLEMENTATION PRIORITY:

Phase 1: Backend API with basic consumption aggregation (if data exists)

Phase 2: Frontend report page with tables and filters

Phase 3: Add visualizations (Recharts - already installed)

Phase 4: Drill-down and export features

Phase 5: Advanced forecasting and stock efficiency metrics

PLEASE RESPOND WITH:

Database schema confirmation (show actual table structure)

Sample query result from stores_ledger for last 30 days

Confirmation on Work Order consumption tracking mechanism

Any limitations or data gaps I should be aware of

Only after validation, proceed with implementation.

This technical prompt ensures that we validate all backend prerequisites before building the frontend report, preventing implementation failures due to missing data or incorrect assumptions about the database schema.