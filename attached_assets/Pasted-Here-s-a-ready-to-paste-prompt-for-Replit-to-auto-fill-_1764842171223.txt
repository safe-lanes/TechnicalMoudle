Here’s a ready-to-paste prompt for Replit to auto-fill realistic dummy data in sections **D, F, G, H** of the **Component Detail** screen.

---

### PROMPT FOR REPLIT

You are working on my shipboard PMS software.
Focus on **Module 2 – Components**, specifically the **Component Detail screen (sections A–H)**.

You can see sample components like:

* `403.001 Steering gears, complete`
* `401.005 Rudders w/nozzle`

Right now, sections **A, B, C, E** are populated, but **D, F, G, H** are blank.
I want you to **auto-generate realistic dummy data** for these four sections **for all components that currently have no records there**.

#### 1. Do NOT change these sections

* **A. Component Information**
* **B. Running Hours & Condition Monitoring**
* **C. Jobs**
* **E. Spares**

Leave all existing data exactly as it is. Only read them to understand the component type (Steering gear, Rudder, Pump, Motor, etc.).

---

#### 2. Section D – Maintenance History (dummy historical work orders)

For each component that has no maintenance history yet:

1. Create **2–4 dummy history records** based on realistic shipboard work:

   * Fields should follow the existing table structure for Maintenance History, e.g.:

     * `WO No` / `Work Order Code`
     * `Job Title`
     * `Task Type` (e.g. “Inspection”, “Overhaul”, “Replacement”)
     * `Done Date`
     * `Running Hours at Completion` (if applicable)
     * `Status` (“Completed”)
     * `Done By` (e.g. “2/E”, “C/E”, “Fitter”, “3/O”)
     * `Remarks` (short realistic text)

2. Make the job titles & remarks **match the component**. Examples:

   * For Steering Gear:

     * “Quarterly Steering Gear Inspection”
     * “Hydraulic Oil Filter Renewal”
   * For Rudder:

     * “Rudder Stock Clearance Check”
     * “Greasing of Pintle Bearing”

3. Use dates within the **last 2–3 years**, spread out logically (e.g., every 6 months or per RH frequency).

4. Store these records in whatever data structure the app already uses for maintenance history, so they render correctly under **Section D. Maintenance History**.

---

#### 3. Section F – Drawings & Manuals (dummy technical documents)

For each component with no drawings/manuals:

1. Add **3–5 dummy document entries** with fields like:

   * `Document Name`
   * `Type` (Drawing / Manual / Datasheet)
   * `Version` (e.g. “Rev.0”, “Rev.1”)
   * `Uploaded By`
   * `Uploaded On`
   * `File Size` (just a fake value)
   * `Download / View` action

2. Names must be realistic and match the equipment. Examples:

   **Steering Gear:**

   * “Steering Gear GA Drawing – RV700-2.pdf”
   * “Steering Gear Hydraulic System P&ID – Rolls Royce.pdf”
   * “Steering Gear Operation & Maintenance Manual – RV700-2.pdf”
   * “Steering Gear Foundation & Bolt Layout.dwg”

   **Rudder:**

   * “Rudder Arrangement Drawing – 401.005.dwg”
   * “Rudder & Nozzle Assembly GA.pdf”
   * “Rudder Stock Material Certificate.pdf”

3. These are **dummy files only**:

   * There is no need to actually upload binary files; just create entries so UI shows realistic rows in **Section F. Drawings & Manuals**.

---

#### 4. Section G – Classification & Regulatory Data (dummy survey / class records)

For each component with no classification/regulatory entries:

1. Add **2–3 dummy survey / regulatory records** with fields like:

   * `Survey / Check Item`
   * `Regulation / Rule Ref` (e.g. “SOLAS II-1”, “Class Rules Pt.4 Ch.6”, “ISM”)
   * `Authority` (e.g. “Class – DNV”, “Flag State”, “Company Requirement”)
   * `Last Survey Date`
   * `Next Due Date`
   * `Status` (“Not Due”, “Due Soon”, “Overdue`)
   * `Remarks`

2. Make items match the component. Examples:

   **Steering Gear:**

   * “Annual Steering Gear Test – Class Requirement”
   * “Emergency Steering Drill – SOLAS II-1 Reg.29”
   * “5-Year Steering Gear Overhaul – Class Survey”

   **Rudder:**

   * “Rudder Stock NDT – 5-Year Class Survey”
   * “Underwater Inspection – Rudder & Nozzle”
   * “Rudder Clearance Check – Docking Survey”

3. Use realistic dates & due dates (e.g. last survey last year, next due within 1–4 years, depending on item).

4. Save these in the same structure the app uses for **Classification & Regulatory Data**, so they display correctly in **Section G**.

---

#### 5. Section H – Requisitions (dummy purchase/service requisitions)

Even if our Requisitions section is marked “future enhancement”, I want it to show example rows.

For each component:

1. Create **1–3 dummy requisition records** linked to the component and its spares. Use fields such as:

   * `Requisition No`
   * `Raised On`
   * `Item / Service`
   * `Related Part Code` (use existing spare part codes from Section E where possible)
   * `Qty`
   * `Status` (“Draft”, “RFQ Sent”, “PO Raised”, “Delivered On Board”)
   * `Priority` (“Normal”, “Urgent”)
   * `Remarks`

2. Link items logically to the spares:

   * For Steering Gear:

     * Requisition for “Steering Gear Oil Seal – SP-00003”
     * Requisition for “Steering Gear Actuator Piston – SP-00004”
   * For Rudder:

     * Requisition for “Rudder Actuator Seal Kit – SP-00002”
     * Requisition for “Rudder Shaft Bearing – SP-00001”

3. Ensure the Requisition records are wired to whatever data model will eventually be used by the Purchase module, but for now they only need to **display in Section H. Requisitions**.

---

#### 6. General rules

* Generate dummy data **only where these sections are empty**, so you don’t duplicate or overwrite any existing real records.
* Ensure all new records respect:

  * Correct foreign keys (`component_id`, `vessel_id`, etc.).
  * Existing UI table columns and naming.
* After seeding the dummy data, verify via the UI that:

  * Section D shows multiple realistic history rows.
  * Section F lists equipment-specific drawings and manuals.
  * Section G lists survey/regulatory items with due dates.
  * Section H shows requisitions tied to real spare part codes.

Finally, show me:

1. The seeding script / code you wrote (backend or migration file).
2. A few example records for Steering Gear and Rudder as JSON or table so I can review.
