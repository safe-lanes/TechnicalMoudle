VERIFICATION REQUEST: Backend Data Storage Analysis for New Chemical Fields

CONTEXT:
I can see that you've successfully added new sections to the Chemicals inventory frontend with all the requested fields. The form now includes:

✅ Expiry & Date Information section (Manufacture Date, Expiry Date, Batch Number, Lot Number, Shelf Life)
✅ Safety Data Sheet (SDS) section (SDS Reference, SDS Last Updated, Hazard Classification, UN Number, Flash Point)
✅ Storage & Safety section (Min/Max Storage Temp, PPE Requirements, Disposal Instructions, Emergency Contact)
✅ The Chemicals table now shows new columns: Expiry, Batch #, and Hazard

HAZARD CLASSIFICATION OPTIONS OBSERVED:
The Hazard Classification dropdown includes:
- None
- Flammable
- Toxic
- Corrosive
- Oxidizer
- Compressed Gas
- Other

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERIFICATION QUESTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. DATABASE SCHEMA:
   - Confirm which table stores these new fields (is it `stores_items`?)
   - List all the column names that were added with their exact data types
   - Confirm that these fields are nullable for backward compatibility
   - Have database indexes been added for `expiry_date`, `hazard_classification`, and `batch_number`?
   - Show the complete SQL migration script that was executed

2. API ENDPOINTS:
   - Which API endpoints were updated to handle these new fields?
   - Confirm the request/response structure for POST/PUT operations
   - Are there any validation rules implemented in the backend? (e.g., expiry_date > manufacture_date)
   - Is there an auto-calculation logic for expiry_date when manufacture_date + shelf_life_months is provided?

3. DATA STORAGE VERIFICATION:
   - Show an example of how the data is stored in the database when a chemical is created with all new fields populated
   - Provide the actual database record structure (JSON or SQL result)
   - How is the Hazard Classification stored? (as string values like "Flammable", "Toxic" or as enum/codes?)
   - Are the hazard classification options hardcoded in frontend or stored in a lookup table?

4. HAZARD CLASSIFICATION UNDERSTANDING:
   - What is your understanding of each hazard class?
   - Are there any associated safety protocols or handling instructions linked to each class?
   - Should different hazard classes have different storage requirements or warnings?
   - Are there plans to add color-coding or icons for different hazard classes in the UI?

5. EDIT & BULK UPDATE:
   - Has the Edit Chemical dialog been updated with these new fields?
   - Has the Bulk Update Chemicals page been updated to support bulk updating of expiry_date, batch_number, and sds_reference?
   - Show the current state of these features

6. EXISTING DATA:
   - How are existing chemical records (created before this update) handled?
   - Are the new fields showing as NULL or empty for existing records?
   - Is there a data migration strategy to populate existing records?

7. REPORT API ENDPOINT:
   - Has a new API endpoint been created for the Chemicals Expiry Report (e.g., GET /api/reports/chemicals-expiry)?
   - If yes, what data does it return?
   - Are there query parameters for filtering by expiry status, hazard class, vessel, etc.?
   - Are calculated fields included (days until expiry, expiry status)?

8. FILE UPLOADS:
   - Is there support for uploading SDS documents (PDF files)?
   - How are SDS documents stored? (file system, cloud storage, database BLOB?)
   - What is stored in the `sds_document_url` field? (relative path, full URL, file ID?)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TESTING REQUESTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Please create a TEST chemical record with the following data and show me:
1. The API request body that would be sent
2. The database record after it's saved
3. How it appears when retrieved via GET API

Test Chemical Data:
- Item Code: CH-TEST-001
- Item Name: Test Flammable Chemical
- Manufacture Date: 2025-01-15
- Expiry Date: 2027-01-15
- Batch Number: BT-2025-001
- Lot Number: LOT-001
- Shelf Life: 24 months
- SDS Reference: SDS-2025-TEST
- SDS Last Updated: 2025-01-20
- Hazard Classification: Flammable
- UN Number: UN1203
- Flash Point: 23°C
- Min Storage Temp: 5°C
- Max Storage Temp: 25°C
- PPE Requirements: Safety goggles, chemical-resistant gloves, apron
- Disposal Instructions: Dispose according to local hazardous waste regulations
- Emergency Contact: Safety Officer - ext. 4567

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
NEXT STEPS CLARIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After you provide the above information, I need to know:

1. Report Page Status:
   - Does the route `/pms/reports/chemicals-inventory-expiry` exist now?
   - If yes, what does it currently display?
   - If no, should we build it from scratch?

2. Calculations Logic:
   - Where should the "days until expiry" calculation be done? (frontend, backend API, or both?)
   - Should expiry status (Expired/Expiring Soon/OK) be calculated dynamically or stored?

3. Export Functionality:
   - Is there existing export functionality (PDF/Excel) that we can extend?
   - What library/approach is used for generating PDF reports?

4. Data Completeness:
   - Are there any other fields or features related to chemicals that should be added before building the report?
   - Are there any maritime industry regulations (IMO, MARPOL) that should be considered for chemical inventory?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Please provide:
1. Database schema details (table name, column names, data types)
2. SQL migration script
3. Example database record with all new fields
4. API endpoint documentation (request/response structure)
5. Current validation logic
6. Status of Edit and Bulk Update features
7. Explanation of how hazard classifications are used/stored
8. Report API status and data structure
9. Recommended approach for building the report UI

This information will help me verify that everything is working correctly and prepare for building the complete Chemicals Inventory & Expiry Report.