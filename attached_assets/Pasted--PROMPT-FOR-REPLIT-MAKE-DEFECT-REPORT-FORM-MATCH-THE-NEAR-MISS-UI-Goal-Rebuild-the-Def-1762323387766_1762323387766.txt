**PROMPT FOR REPLIT — MAKE DEFECT REPORT FORM MATCH THE “NEAR MISS” UI**

**Goal**
Rebuild the **Defect Report** screen so it **looks and behaves exactly like the Near Miss form** (same layout, spacing, colors, icons, stepper, and component patterns). Do **not** change backend logic or field names—this is a **pure UI refactor**.

---

### 1) Page shell & header (copy Near Miss)

* Use the **same page container, gutter, and max-width** as Near Miss.
* Top-right buttons: **Back • View • Save** (identical size, order, icons, and spacing).
* Page title block:

  * Title: **Defect Report**
  * Subtitle: **Part A – Describe what happened** (same style as Near Miss “Part A”).
  * Show **Report ID** on the right, same position and typography as Near Miss.

---

### 2) Left stepper (same as Near Miss)

* 3 steps with identical visuals/behavior:

  1. **Reporting**
  2. **Actions** (renamed from “Analysis” if your route is “Actions”)
  3. **Closeout**
* Active step = blue filled circle; upcoming = hollow; completed = checkmark; identical hover/focus states.

---

### 3) Card layout & grids (mirror Near Miss)

* Use **cards** with the same elevation, padding (24px), radius, and section dividers.
* **Two-column grid on desktop**, **single column on < 992px**—same breakpoints as Near Miss.
* **Top-to-bottom sections and order (in Part A)**:

  1. **Details** (left/right columns like Near Miss “Reporting”)
  2. **Description** (full-width with the same WYSIWYG toolbar used in Near Miss)
  3. **Operation Condition / Location** (same pill/radio treatment as “Vessel Location” in Near Miss)
  4. **Occurrence Type** (radio pills, same spacing)
  5. **Purchase Order** (single input row)
  6. **Class Condition (CoC)** (checkbox row)
* **Cause Analysis** (for Part B/Actions card): split into **Immediate Cause** and **Root Cause** as two equal columns with “Select” button + textarea for both, identical to Near Miss “Cause analysis”.
* **Office Closeout** (Part C) mirrors Near Miss “Office Closeout” (Reviewed checkbox, comments, Date Closed, Submit).

---

### 4) Fields, order, and exact placements (Part A card)

**Left column**

* **Vessel*** (dropdown)
* **Source** (dropdown)
* **Defect Category** (dropdown)
* **Defect Type** (dropdown)
* **Make** (dropdown)
* **Model** (dropdown)

**Right column**

* **Category** (dropdown for equipment/hardware category)
* **Date Issued*** (date picker)
* **Type** (equipment type dropdown)
* **Target Date for Closure** (date picker)
* **Responsible Role** (text/dropdown per existing)
* **Condition of Class (CoC)** (checkbox with helper text; place **below** Role—align to right column grid)

**Below the two columns**

* **Description*** (full-width). Use the **exact same editor toolbar** as Near Miss (undo/redo, bold/italic/underline, list, align, font size, link, image).
* **Operation Condition / Location** radio pills: **Sailing | Port | At Anchor**; when **Port** is selected, show **Port Name**; when **Vessel Location** needed, replicate the same two inline dropdowns pattern used in Near Miss.
* **Occurrence Type** radio pills: **Routine | Breakdown** (same pill styling).
* **Purchase Order** input (full-width).
* **Critical** checkbox on the **same row right-aligned** as Purchase Order (like Near Miss small toggles).

---

### 5) Part B – Actions (match Near Miss “Analysis”)

* Card title: **Actions**
* Subsections (each as a sub-card row, like Near Miss):

  * **B1. Immediate Cause** → “Select” button + textarea.
  * **B2. Root Cause** → “Select” button + textarea.
  * **B3. Action Status** → table identical to Near Miss (Action Type, Proposed By, Responsibility, Due Date, Date Completed, Status) with **+ Add Action** button matching size and position.

*(If you also show VIQ/SFI selectors, render them as **B4. References** row, using the same small pill dropdowns as Near Miss “B4. VIQ”.)*

---

### 6) Part C – Closeout (mirror Near Miss)

* **Reviewed** checkbox.
* **Office Comments** textarea (full-width).
* **Date Closed** date picker and **Submit** button in the same alignment as Near Miss.

---

### 7) Visual system (use Near Miss tokens 1:1)

* **Typography**: labels all-caps 12px; helper text 12px muted; input height 44px; same font as Near Miss.
* **Colors**: copy Near Miss semantic tokens (primary blue, greys, success, warning). Reuse the **CoC badge** style used elsewhere with the small rounded “CoC” chip when needed.
* **Icons**: use the exact same icon set and sizes for calendar, dropdown chevrons, eye (View), back arrow, save, plus, etc.
* **Spacing**: vertical spacing between groups = **24px**; between fields in a row = **16px**.
* **Validation**: required fields with the red asterisk and inline error text styled exactly like Near Miss.

---

### 8) Behavior parity

* **Sticky header** with View/Save like Near Miss.
* **Autosave guard**: warn on navigation away if dirty, same as Near Miss.
* **Mobile**: collapse to single column; stepper becomes compact; buttons stack as Near Miss.

---

### 9) Do NOT do

* Do **not** introduce new fields or change API keys.
* Do **not** keep the old three-panel “Basic / Equipment / Date” headers. Replace with Near Miss style **single “Reporting” card** and left stepper.
* Do **not** change the WYSIWYG to a different one—reuse the Near Miss editor instance.

---

### 10) Acceptance checklist

* [ ] Layout, spacing, colors, icons **match Near Miss** by visual diff.
* [ ] **Three-step left stepper** present and functional.
* [ ] Two-column desktop / one-column mobile grid behavior matches Near Miss.
* [ ] **CoC** checkbox sits in the **right column** under **Responsible Role**; **Purchase Order + Critical** share a row.
* [ ] **Description** editor bar is identical to Near Miss and supports images/links.
* [ ] **Actions** table and **+ Add Action** match Near Miss styling & interactions.
* [ ] **Closeout** card mirrors Near Miss including button placement.
* [ ] No misaligned controls; all labels align to the same baseline; all inputs equal height.

---

### 11) Tech notes (for the team)

* Reuse the **Near Miss React components** (Stepper, Card, FormRow, Editor, DatePicker, PillRadio, Table, Chip, Buttons).
* Import **theme tokens** from the Near Miss module to guarantee color/spacing parity.
* Keep the existing Defect form state and submit handlers; only **rebind** to the new UI components.

**Deliverable:** a PR that replaces the current Defect Report JSX/CSS with the Near-Miss-style components and passes the acceptance checklist above.
