4 fleet mapping issues found. All related to soft-delete + UNIQUE constraint conflict.

FIX 1 (Issues 1, 2, 3 + Issue 4 sub-cause B): Replace UNIQUE constraints with partial UNIQUE indexes

Create migration 078:

-- Fleet Component Mapping
ALTER TABLE fleet_component_mapping DROP CONSTRAINT IF EXISTS unique_fleet_component_mapping;
CREATE UNIQUE INDEX IF NOT EXISTS unique_fleet_component_mapping_active 
  ON fleet_component_mapping(fleet_equipment_code, vessel_code, component_code) 
  WHERE is_active = true;

-- Fleet Job Mapping
ALTER TABLE fleet_job_vessel_mapping DROP CONSTRAINT IF EXISTS unique_fleet_job_vessel_mapping;
CREATE UNIQUE INDEX IF NOT EXISTS unique_fleet_job_vessel_mapping_active 
  ON fleet_job_vessel_mapping(fleet_equipment_code, job_code, vessel_code, job_id) 
  WHERE is_active = true;

-- Fleet Spare Mapping
ALTER TABLE fleet_spare_vessel_mapping DROP CONSTRAINT IF EXISTS unique_fleet_spare_vessel_mapping;
CREATE UNIQUE INDEX IF NOT EXISTS unique_fleet_spare_vessel_mapping_active 
  ON fleet_spare_vessel_mapping(fleet_equipment_code, part_code, vessel_code, spare_id) 
  WHERE is_active = true;

Update shared/schema.ts to reflect the constraint changes.

FIX 2 (Issue 4 sub-cause A): Fix spare matching to not require fleetEquipmentCode on vessel spares

In FleetVesselMapping.tsx, the vesselSpareLookup at line 568-574:
- Currently requires vs.fleetEquipmentCode to be truthy to include the spare
- Change: Match vessel spares by partCode only. The fleetEquipmentCode context should come from the fleet side, not the vessel side.
- The key for vessel spares should be just partCode
- When building the match, use the fleet spare's fleetEquipmentCode for the mapping record

TEST after fixing:
1. Link component A to fleet X → works
2. Remove linkage → works  
3. Re-link component A to fleet X → should work (no UNIQUE error)
4. Same test for jobs and spares
5. Auto-match spares where vessel has same spare on multiple components (with or without fleetEquipmentCode) → should create multiple linkages

Commit with message: "fix(fleet): use partial UNIQUE indexes for soft-delete compatibility and fix spare matching"