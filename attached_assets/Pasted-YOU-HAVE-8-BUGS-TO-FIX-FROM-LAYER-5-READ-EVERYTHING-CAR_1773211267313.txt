YOU HAVE 8 BUGS TO FIX FROM LAYER 5. READ EVERYTHING CAREFULLY BEFORE WRITING ANY CODE.

CONTEXT: What was verified by independent testing:

Backend (T001–T004) is working correctly — the database fields, tier calculation, and superintendent API endpoints are all functional

The Superintendent page at /pms/superintendent exists and loads

The 4 test WOs are in the Pending Approval tab

However T005 and T006 (all frontend UI changes to WorkOrderFormPage.tsx and WorkOrders.tsx) were NOT implemented. The pages look identical to before Layer 5.

BUG 1 — CRITICAL (WorkOrders.tsx): "Days Late" column missing from Pending Approval tab

In client/src/pages/pms/WorkOrders.tsx, find the table rendered inside the Pending Approval tab. Add a new column called "Days Late" between the "Submitted Date" column and the "Status" column.

The value to display comes from the daysLate field that is now in the work order data (it was added to the schema in T001). Display it with this color logic:

daysLate === 0 → green text: "On Time"

daysLate >= 1 && daysLate <= 6 → yellow/amber text: "[N] days late"

daysLate >= 7 && daysLate <= 14 → orange text: "[N] days late"

daysLate > 14 → red bold text: "[N] days late ⚠️"

Also add the column header "Days Late" to the <thead> row in the correct position.

BUG 2 — CRITICAL (WorkOrders.tsx): "Approval Tier" column missing from Pending Approval tab

In the same Pending Approval tab table in WorkOrders.tsx, add another new column called "Approval Tier" directly after the "Days Late" column (before the "Actions" column).

Display a colored badge based on the approvalTier field:

approvalTier === "standard" → small green badge with text: "Standard"

approvalTier === "ce_with_justification" → small yellow/amber badge: "CE + Remarks"

approvalTier === "superintendent_notification" → small orange badge: "Supt. Notified"

approvalTier === "superintendent_locked" → small red badge: "🔒 Locked"

Also update the Actions column for this tab: if approvalTier === "superintendent_locked", replace the edit/action button(s) with a grey lock icon 🔒. On hover, show a tooltip: "Locked — Awaiting Superintendent acknowledgment". For all other tiers, keep the existing action buttons unchanged.

Also add both new column headers ("Days Late", "Approval Tier") to the <thead> of the Pending Approval table.

BUG 3 — CRITICAL (WorkOrders.tsx): Summary stat bar missing from Pending Approval tab

In WorkOrders.tsx, directly above the Pending Approval tab's table (but below the filter row), add a horizontal row of 4 stat cards. These cards must be calculated from the list of WOs currently shown in the Pending Approval tab.

Each card:

text
Card 1:
  Icon: 🔒
  Label: "Locked (Supt. Required)"
  Count: number of WOs where approvalTier === "superintendent_locked"
  Background: red (#fee2e2), border: red, count text: red (#dc2626)
  If count === 0: grey out the card (opacity 0.5)

Card 2:
  Icon: ⚠️
  Label: "Supt. Notified"
  Count: number of WOs where approvalTier === "superintendent_notification"
  Background: orange (#fff7ed), border: orange, count text: orange (#ea580c)
  If count === 0: grey out

Card 3:
  Icon: 📝
  Label: "CE Remarks Required"
  Count: number of WOs where approvalTier === "ce_with_justification"
  Background: yellow (#fefce8), border: yellow, count text: yellow (#ca8a04)
  If count === 0: grey out

Card 4:
  Icon: ✅
  Label: "Standard Approval"
  Count: number of WOs where approvalTier === "standard"
  Background: green (#f0fdf4), border: green, count text: green (#16a34a)
  If count === 0: grey out
The counts must reflect ALL WOs in the Pending Approval state, not just the current page. Fetch the total counts from the API — you may need to use the existing API endpoint that returns all pending approval WOs, or add a ?all=true query param, or derive the counts from all loaded data. Use whatever is cleanest given the existing code.

BUG 4 — CRITICAL (WorkOrderFormPage.tsx): Non-dismissible tier banner completely missing

In client/src/pages/pms/WorkOrderFormPage.tsx, find where Part B begins rendering. At the very start of Part B — before Section B1 (Risk Assessment) — add a non-dismissible colored banner when the WO's status is "Pending Approval" (or "pending_approval" — check what string the existing code uses for this status).

There must be NO close/dismiss button on this banner. It is always visible when the WO is in pending approval state.

The banner content depends on approvalTier:

text
approvalTier === "superintendent_locked":
  - Background: red (#dc2626), white text
  - Icon: 🔒
  - Title (bold, large): "APPROVAL LOCKED — SUPERINTENDENT ACTION REQUIRED"
  - Body: "This completion is [daysLate] days late. [missedCycles] cycle(s) were missed. This work order is LOCKED and cannot be approved by the Chief Engineer until the Superintendent has acknowledged it. Please notify the Superintendent immediately."

approvalTier === "superintendent_notification":
  - Background: orange (#ea580c), white text
  - Icon: ⚠️
  - Title: "HIGH LATENESS — SUPERINTENDENT HAS BEEN NOTIFIED"
  - Body: "This completion is [daysLate] days late. [missedCycles] cycle(s) were missed. The Superintendent has been automatically notified. Chief Engineer approval is permitted but DETAILED REMARKS ARE MANDATORY (minimum 20 characters)."

approvalTier === "ce_with_justification":
  - Background: #854d0e (dark yellow/brown), white text  
  - Icon: ⚠️
  - Title: "LATE COMPLETION — CE REMARKS REQUIRED"
  - Body: "This completion is [daysLate] days late. [missedCycles] cycle(s) were missed. Chief Engineer approval remarks are mandatory before this work order can be approved."

approvalTier === "standard" (or null/undefined as fallback):
  - Background: #1d4ed8 (blue), white text
  - Icon: ℹ️
  - Title: "PENDING CHIEF ENGINEER APPROVAL"
  - Body: If daysLate > 0: "This completion is [daysLate] days late. Please review and approve."
          If daysLate === 0: "This completion was submitted on time. Please review and approve."
Also update the VERY TOP banner of the page (the one that currently says "This Work Order is completed. Part B is read-only." for completed WOs). When the WO is in pending_approval status, show a matching banner there too:

superintendent_locked → red: "🔒 LOCKED — [missedCycles] missed cycle(s). Awaiting Superintendent acknowledgment before CE can approve."

superintendent_notification → orange: "⚠️ SUPERINTENDENT NOTIFIED — [daysLate] days late. CE must approve with detailed remarks (min 20 chars)."

ce_with_justification → yellow (dark text): "⚠️ REMARKS REQUIRED — [daysLate] days late. CE must provide approval remarks."

standard → blue: "ℹ️ PENDING APPROVAL — Awaiting Chief Engineer review."

BUG 5 — CRITICAL (WorkOrderFormPage.tsx): CE Approval Remarks field and Approval Tier badge missing

In WorkOrderFormPage.tsx, find where the existing "Approve" and "Reject" buttons are rendered (they are near the bottom of Part B, after B4 Spare Parts). Directly above these buttons, add the following two UI elements when the WO status is "Pending Approval":

Element 1 — Approval Tier Badge (read-only display):

text
Label: "Approval Tier"
Value: a colored badge matching the tier (same colors/labels as Bug 2 above)
This is purely informational, read-only, no input
Element 2 — CE Approval Remarks textarea:

text
Label: "CE Approval Remarks" with red asterisk if required, "(Optional)" if not
Type: textarea
Rows: 4
Placeholder: "Enter your remarks regarding this work order completion and lateness..."
Character counter below: "[N] / 500"
Helper text based on tier:
  - superintendent_locked: "🔒 Locked — cannot enter remarks until Superintendent acknowledges"
  - superintendent_notification: "Required — minimum 20 characters (completion is >14 days late)"
  - ce_with_justification: "Required — minimum 10 characters (completion is 7–14 days late)"
  - standard: "Optional"
Disabled (grey out, not editable) when approvalTier === "superintendent_locked"
This textarea's value must be wired into the approval API call — when the CE clicks Approve, send ceApprovalRemarks along with the request body. The field's ref/state variable should be called ceApprovalRemarks.

BUG 6 — CRITICAL (WorkOrderFormPage.tsx): Approve button not locked for superintendent_locked tier

In WorkOrderFormPage.tsx, find the existing "Approve" button. Add conditional logic:

text
If approvalTier === "superintendent_locked":
  → Replace the Approve button with a DISABLED grey button
  → Button text: "🔒 Locked — Awaiting Superintendent"
  → Button style: grey background, grey text, cursor: not-allowed
  → Do NOT show the original green Approve button at all
  → The Reject button can remain visible and enabled (CE can still reject)

If approvalTier === "superintendent_notification":
  → Keep the green Approve button enabled
  → But before calling the approval API, validate that ceApprovalRemarks.trim().length >= 20
  → If not: show an inline error message below the remarks field: "Detailed remarks required — minimum 20 characters (currently [N] characters)"
  → Do NOT call the API until validation passes

If approvalTier === "ce_with_justification":
  → Keep the green Approve button enabled
  → But before calling the approval API, validate that ceApprovalRemarks.trim().length >= 10
  → If not: show inline error: "Approval remarks required — minimum 10 characters (currently [N] characters)"
  → Do NOT call the API until validation passes

If approvalTier === "standard" (or null):
  → Keep the Approve button enabled as normal
  → No validation required on ceApprovalRemarks
BUG 7 — DATA: Duplicate superintendent notification for TEST-L5-LOCKED

The WO with code TEST-L5-LOCKED has two records in the superintendent_notifications table:

One record with missedCycles = 0 and approvalTier = "superintendent_notification" — this is WRONG

One record with missedCycles = 4 and approvalTier = "superintendent_locked" — this is CORRECT

Delete the incorrect record (the one with missedCycles = 0 for TEST-L5-LOCKED). You can do this by writing a migration or a direct DB cleanup script. After deleting it, the Superintendent page should show exactly 2 rows:

Row 1: TEST-L5-LOCKED (35 days late, 4 cycles, 🔒 Locked, Acknowledge button)

Row 2: TEST-L5-SUPT-NOTIFY (20 days late, 0 cycles, Supt. Notified, Info Only)

BUG 8 — DATA: "Total Pending Acknowledgment" counter is wrong

After fixing Bug 7, the counter must show 2 (both TEST-L5-LOCKED and TEST-L5-SUPT-NOTIFY are awaiting acknowledgment). Currently it shows 1. Check the API endpoint GET /api/superintendent/notifications — verify that it returns both unacknowledged records. The counter on the page reads from this endpoint. After the duplicate is deleted (Bug 7), re-check if the counter becomes 2. If the API is only returning superintendent_locked records and excluding superintendent_notification records, fix the query to return ALL records where isAcknowledged = false.

IMPLEMENTATION ORDER — DO THESE IN THIS EXACT ORDER:

Fix Bug 7 first (delete duplicate DB record via migration/cleanup)

Fix Bug 8 (verify/fix the notifications API query)

Fix Bugs 1, 2, 3 together in WorkOrders.tsx (all are in the same file/tab)

Fix Bugs 4, 5, 6 together in WorkOrderFormPage.tsx (all are in the same file)

AFTER FIXING — VERIFY THESE SPECIFIC THINGS BEFORE REPORTING BACK:

Go to /pms/work-orders → select Vessel 4 → click Pending Approval tab:

Confirm the 4-card summary bar is visible ABOVE the table

Confirm the table has "Days Late" column with correct colors

Confirm the table has "Approval Tier" column with correct colored badges

Confirm the TEST-L5-LOCKED row has a grey 🔒 icon in the Actions column instead of the edit button

Click on TEST-L5-LOCKED WO link to open it:

Confirm RED banner at top of page (very top, where "This WO is completed" normally shows)

Confirm RED non-dismissible banner at top of Part B (before B1 section)

Confirm "Approval Tier" badge shows "🔒 Locked" in red

Confirm "CE Approval Remarks" textarea is visible but DISABLED (greyed out)

Confirm the Approve button is a GREY DISABLED button saying "🔒 Locked — Awaiting Superintendent"

Confirm the Reject button is still green and clickable

Click on TEST-L5-CE-REMARKS WO link:

Confirm YELLOW banner at top and in Part B

Confirm CE Approval Remarks textarea is enabled

Try clicking Approve with empty remarks → confirm inline error appears

Try clicking Approve with only 5 characters typed → confirm error says "minimum 10 characters"

Click on TEST-L5-SUPT-NOTIFY WO link:

Confirm ORANGE banner

Try approving with 15 characters in remarks → confirm error says minimum 20 required

Click on TEST-L5-STANDARD WO:

Confirm BLUE banner

Confirm Approve button is green and enabled

Confirm remarks field shows "(Optional)"

Go to /pms/superintendent:

Confirm exactly 2 rows

Confirm "Total Pending Acknowledgment" = 2

Click "Acknowledge" on TEST-L5-LOCKED → confirm modal appears → confirm → confirm the row updates

After acknowledging, go back to the TEST-L5-LOCKED WO → confirm it now shows YELLOW banner ("Remarks Required") instead of red

**Do not report back until you have