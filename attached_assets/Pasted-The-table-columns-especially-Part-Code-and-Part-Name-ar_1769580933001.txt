The table columns (especially Part Code and Part Name) are visually merging together with no clear separation. I need to improve the visual hierarchy and spacing. Please implement the following fixes in client/src/pages/spares/SparesNew.tsx:

1. Add horizontal gap/padding between grid columns:

Modify the grid container to add column gap:

tsx
style={{ 
  gridTemplateColumns: FEATURES.IHM 
    ? 'minmax(90px, auto) minmax(130px, 1fr) minmax(150px, 1.5fr) minmax(100px, auto) minmax(80px, auto) minmax(50px, auto) minmax(50px, auto) minmax(60px, auto) minmax(100px, auto) minmax(50px, auto) minmax(140px, auto)' 
    : 'minmax(90px, auto) minmax(130px, 1fr) minmax(150px, 1.5fr) minmax(100px, auto) minmax(80px, auto) minmax(50px, auto) minmax(50px, auto) minmax(60px, auto) minmax(100px, auto) minmax(140px, auto)',
  minWidth: 'max-content',
  gap: '12px' // Add this line for both row and column gaps
}}
2. Add padding to individual grid cells:

Add horizontal padding to each cell/column to create breathing room:

tsx
className="px-3 py-2" // Add to each grid cell div
3. Increase minimum column widths for Part Code and Part Name:

Update gridTemplateColumns to:

tsx
gridTemplateColumns: FEATURES.IHM 
  ? 'minmax(110px, auto) minmax(150px, 1fr) minmax(180px, 1.5fr) minmax(120px, auto) minmax(80px, auto) minmax(60px, auto) minmax(60px, auto) minmax(80px, auto) minmax(120px, auto) minmax(60px, auto) minmax(160px, auto)' 
  : 'minmax(110px, auto) minmax(150px, 1fr) minmax(180px, 1.5fr) minmax(120px, auto) minmax(80px, auto) minmax(60px, auto) minmax(60px, auto) minmax(80px, auto) minmax(120px, auto) minmax(160px, auto)'
Changes:

Part Code: 90px → 110px (+20px)

Part Name: 130px → 150px (+20px)

Component: 150px → 180px (+30px)

Part Number: 100px → 120px (+20px)

Stock: 60px → 80px (+20px)

Location: 100px → 120px (+20px)

Actions: 140px → 160px (+20px)

4. Optional: Add subtle vertical borders between columns:

Add to the grid cell class:

tsx
className="px-3 py-2 border-r border-gray-200 last:border-r-0"
5. Reduce font size slightly if text is still too cramped:

Add to grid cells:

tsx
className="px-3 py-2 text-sm" // Use text-sm (14px) instead of default
Recommended Implementation Order:

First add gap: '12px' to the grid style

Then add px-3 py-2 padding to cells

If still cramped, increase the minmax widths as specified

Optionally add text-sm to reduce font size

Optionally add border-r border-gray-200 for vertical separators

Expected Result:

Clear visual separation between Part Code and Part Name columns

Adequate breathing room between all columns

Better readability without text merging

Professional table appearance with proper spacing

Please implement these changes and verify the visual separation is clear.