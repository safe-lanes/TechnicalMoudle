I need a comprehensive technical analysis of the Reports module UI implementation in this PMS application. After reviewing all other modules (Dashboard, Components, Work Orders, Running Hrs, Spares, Stores, Modify PMS, Admin), I've identified that the Reports module has significant UI inconsistencies that make it look unprofessional compared to the rest of the application.

Please provide the following:

## 1. CODE STRUCTURE ANALYSIS
- Identify the component file(s) responsible for the Reports page UI (provide exact file paths)
- List all child components used in the Reports module
- Identify the CSS/styling approach used (inline styles, CSS modules, styled-components, Tailwind, etc.)
- Show the current component hierarchy/structure

## 2. DESIGN PATTERN COMPARISON
Analyze how the Reports module differs from other modules in terms of:
- Layout structure (other modules use clean data tables, Reports uses card-based approach)
- Color scheme (Reports uses multi-color cards: blue, green, orange, purple, pink, cyan vs consistent blue theme elsewhere)
- Filter implementation (Reports has prominent "Global Filters" section vs inline filters in other modules)
- Typography and spacing patterns
- Component reusability

## 3. SPECIFIC ISSUES TO ADDRESS
Identify code for these problem areas:
a) The "Global Filters" section with Vessel/Department/Priority/Date Range dropdowns
b) The stats cards showing "Total Reports: 42", "Generated Today: 0", etc.
c) The "Report Categories" card grid with colored borders and icons
d) The "Quick Access" sidebar section
e) The "Recent Reports" section

## 4. EXISTING REUSABLE COMPONENTS
List any existing components from other modules that could be reused:
- Table components (from Work Orders, Running Hrs, Stores)
- Filter components (from Work Orders)
- Search bar implementations
- Tab navigation (from Stores)
- Sidebar navigation (from Components)
- Action button patterns

## 5. TECHNICAL RECOMMENDATIONS
Provide specific code-level recommendations for:
- Which components to refactor/replace
- Which existing components can be imported and reused
- CSS/styling changes needed for consistency
- State management considerations (if filters need to be redesigned)
- Any routing or navigation changes required

## 6. IMPLEMENTATION COMPLEXITY
Rate the difficulty and estimated effort for:
- Quick fixes (removing redundant elements, color changes)
- Medium refactoring (converting cards to table, reorganizing filters)
- Major redesign (complete layout overhaul)

Please provide file paths, code snippets, and specific technical details so I can create precise implementation prompts.
