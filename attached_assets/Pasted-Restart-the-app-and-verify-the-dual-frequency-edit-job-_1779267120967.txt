Restart the app and verify the dual-frequency edit-job fix is 
deployed. Branch: feature/replit-work, expected HEAD at or 
above commit eb23356b1.

═══════════════════════════════════════════════════════════════════
STEP 1 — RESTART
═══════════════════════════════════════════════════════════════════
1. Restart the Replit app so it picks up the new code 
   (migrations auto-run on startup).

2. Confirm the commit is deployed:
     git log --oneline | grep eb23356b1
   Expected: shows "fix(pms): Dual Frequency D3 edit path - read 
   rhCounterType from jobContext..."

3. Confirm server is healthy in the console — no startup errors.

═══════════════════════════════════════════════════════════════════
STEP 2 — HARD REFRESH BROWSER (CRITICAL)
═══════════════════════════════════════════════════════════════════
This fix changes both server AND client code. The old client 
bundle in the browser cache will NOT have the new logic. 
A normal page reload is NOT enough.

Do a HARD refresh:
   - Windows / Linux: Ctrl + Shift + R  (or Ctrl + F5)
   - Mac: Cmd + Shift + R

Or open DevTools → right-click the refresh button → "Empty 
Cache and Hard Reload."

═══════════════════════════════════════════════════════════════════
STEP 3 — TEST: edit-job Dual Frequency (the failing case)
═══════════════════════════════════════════════════════════════════
T1. Open an existing job for a component whose RH Counter Type 
    is Master or Inherited.
    - Change Maintenance Basis to "Dual Frequency"
    - Fill in calendar interval and Running Hours interval
    - Click Save
    Expected: SAVES SUCCESSFULLY, no red warning, no "Dual 
    Frequency not allowed" toast.

T2. Negative regression — try the same on an existing job 
    whose component has RH Counter Type = NOT_RH_DRIVEN.
    Expected: BLOCKED with the warning and toast (the rule 
    still works correctly on bad components).

T3. Calendar / Running Hours regression — open a Calendar job 
    and a Running Hours job and confirm they look and save 
    exactly as before.

T4. Network tab check (DevTools → Network):
    - Open a job in edit mode
    - Confirm there is NO request to 
      /technical/api/components/details/* anymore
    - The component data should come from the jobContext call 
      only
    This proves the fix took effect (not stale bundle).

═══════════════════════════════════════════════════════════════════
REPORT
═══════════════════════════════════════════════════════════════════
- Commit deployed (yes/no)
- T1 result with screenshot if it fails
- T2, T3 results
- T4: confirm the /components/details/ request is gone