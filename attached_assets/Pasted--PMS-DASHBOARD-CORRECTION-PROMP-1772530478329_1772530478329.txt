```
=============================================================
PMS DASHBOARD — CORRECTION PROMPT (ISSUE 6 ONWARDS)
Continuing from previously provided Issues 1–5.
Do NOT change data, APIs, routing, or business logic.
ONLY change visual styles, chart config, and CSS.
=============================================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 6 — 6-MONTH TREND LINE CHART (COMPLETELY BROKEN)
File: client/src/pages/pms/Dashboard.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHAT IS WRONG:
- Y-axis shows "12522%", "7000%", "3500%" — absurd values, chart is
  completely unusable
- The red Overdue line rockets off the chart
- Green Completed and Orange Outstanding lines are flat at 0%
- Root cause: the percentage calculation is wrong. Values are likely
  being computed as (count / someWrongTotal) * 100 producing values
  way above 100

STEP 1 — FIX THE DATA:
Find the array/function that builds trend chart data (likely called
trendData or maintenanceTrend or similar). For each monthly entry,
the values for "completed", "overdue", and "outstanding" MUST be
plain percentage integers between 0 and 100.

Use this formula for each month:
  const TOTAL = totalWorkOrders  // e.g. 2618

  completedPct  = Math.min(100, Math.round((completedCount  / TOTAL) * 100))
  overduePct    = Math.min(100, Math.round((overdueCount    / TOTAL) * 100))
  outstandingPct = Math.min(100, Math.round((outstandingCount / TOTAL) * 100))

If per-month breakdown data does not exist from the API and only
the overall snapshot totals exist (e.g. completed=1275, overdue=1127,
total=2618), then build static representative monthly data that
progresses logically toward the final value. Example:

const trendData = [
  { month: 'Oct', completed: 30, overdue: 43, outstanding: 4 },
  { month: 'Nov', completed: 35, overdue: 43, outstanding: 4 },
  { month: 'Dec', completed: 38, overdue: 43, outstanding: 4 },
  { month: 'Jan', completed: 42, overdue: 43, outstanding: 4 },
  { month: 'Feb', completed: 46, overdue: 43, outstanding: 4 },
  { month: 'Mar', completed: 49, overdue: 43, outstanding: 4 },
]
// 49% = actual completion rate, 43% = actual overdue rate

STEP 2 — FIX THE CHART COMPONENT:
Replace whatever chart component is currently used (AreaChart or
broken LineChart) with the following exact Recharts JSX:

import {
  LineChart, Line, XAxis, YAxis, CartesianGrid,
  Tooltip, Legend, ResponsiveContainer
} from 'recharts';

<ResponsiveContainer width="100%" height={220}>
  <LineChart
    data={trendData}
    margin={{ top: 8, right: 16, left: 0, bottom: 0 }}
    style={{ backgroundColor: '#ffffff' }}
  >
    <CartesianGrid strokeDasharray="3 3" stroke="#f0f4f8" vertical={false} />
    <XAxis
      dataKey="month"
      tick={{ fontSize: 11, fill: '#6b7280' }}
      axisLine={{ stroke: '#e5e7eb' }}
      tickLine={false}
    />
    <YAxis
      domain={[0, 100]}
      ticks={[0, 25, 50, 75, 100]}
      tickFormatter={(v) => `${v}%`}
      tick={{ fontSize: 11, fill: '#6b7280' }}
      axisLine={false}
      tickLine={false}
      width={38}
    />
    <Tooltip
      formatter={(value) => [`${value}%`]}
      contentStyle={{
        backgroundColor: '#ffffff',
        border: '1px solid #e5e7eb',
        borderRadius: '6px',
        fontSize: '12px'
      }}
    />
    <Legend
      iconType="circle"
      iconSize={8}
      wrapperStyle={{ fontSize: '12px', paddingTop: '8px' }}
    />
    <Line
      type="monotone"
      dataKey="completed"
      name="Completed %"
      stroke="#16a34a"
      strokeWidth={2}
      dot={{ r: 4, fill: '#16a34a', strokeWidth: 0 }}
      activeDot={{ r: 5 }}
    />
    <Line
      type="monotone"
      dataKey="outstanding"
      name="Outstanding %"
      stroke="#f59e0b"
      strokeWidth={2}
      dot={{ r: 4, fill: '#f59e0b', strokeWidth: 0 }}
      activeDot={{ r: 5 }}
    />
    <Line
      type="monotone"
      dataKey="overdue"
      name="Overdue %"
      stroke="#e74c3c"
      strokeWidth={2}
      dot={{ r: 4, fill: '#e74c3c', strokeWidth: 0 }}
      activeDot={{ r: 5 }}
    />
  </LineChart>
</ResponsiveContainer>

STEP 3 — REMOVE OLD CHART ARTIFACTS:
- Delete any <defs> gradient linearGradient blocks
- Delete any <Area> components
- Delete any <ReferenceArea> colored bands (green/orange/red zones)
- Delete any AreaChart import if it is no longer used
- The chart wrapper div must have: backgroundColor: '#ffffff',
  borderRadius: '8px', padding: '8px 4px'


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 7 — STATUS DISTRIBUTION DONUT CHART
File: client/src/pages/pms/Dashboard.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHAT IS WRONG:
- The donut ring is too THICK — takes up too much visual area
- There is not enough white space in the center hole
- The overall chart card may have a gray background tint

EXACT FIXES:
1. Find the <Pie> component for Status Distribution.
   Change to:
   innerRadius="70%"
   outerRadius="90%"
   paddingAngle={2}
   This gives a thin ring with a large white center hole.

2. The chart wrapper div:
   backgroundColor: '#ffffff'
   padding: '0'

3. Segment colors — keep exactly as they are:
   Overdue      → '#e74c3c'  (red)
   Due          → '#f59e0b'  (amber/orange)
   Pending Approval → '#3b82f6'  (blue)
   Completed    → '#16a34a'  (green)

4. The legend below the donut — ensure it uses:
   fontSize: '12px', color: '#374151'
   iconType="circle", iconSize={10}

5. The "STATUS DISTRIBUTION" label above:
   fontSize: '13px', fontWeight: 600, color: '#1a6eb5',
   textTransform: 'uppercase', letterSpacing: '0.5px'

6. The "Click segments to filter" subtitle:
   fontSize: '11px', color: '#9ca3af', fontStyle: 'normal'
   — must be GRAY, not blue


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 8 — SPARES STOCK STATUS DONUT CHART
File: client/src/pages/pms/Dashboard.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHAT IS WRONG:
- Same issue as Status Distribution — ring is too thick
- The center number "527" label is visible — that is fine, keep it
- Chart background has a slight tint

EXACT FIXES:
1. Find the <Pie> component for Spares Stock Status.
   Change to:
   innerRadius="65%"
   outerRadius="88%"
   paddingAngle={2}

2. Segment colors — keep exactly:
   OK     → '#16a34a'  (green)
   At Min → '#f59e0b'  (amber)
   Low    → '#e74c3c'  (red)

3. Chart wrapper div:
   backgroundColor: '#ffffff'

4. "SPARES STOCK STATUS" label:
   fontSize: '11px', fontWeight: 700, color: '#1a6eb5',
   textTransform: 'uppercase', letterSpacing: '0.8px'

5. Legend below:
   fontSize: '12px', iconType: 'circle', iconSize: 8


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 9 — OVERDUE WORK ORDERS TABLE
File: client/src/pages/pms/Dashboard.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHAT IS WRONG:
- The STATUS column is being cut off — shows only "ST" instead of
  "Overdue" badge. The column is too narrow.
- The table rows look cramped

EXACT FIXES:
1. Table container:
   width: '100%'
   overflowX: 'auto'
   borderRadius: '8px'
   border: '1px solid #f1f5f9'

2. Table header row (WORK ORDER | EQUIPMENT | STATUS):
   backgroundColor: '#1a2b4a'
   color: '#ffffff'
   fontSize: '11px'
   fontWeight: 600
   textTransform: 'uppercase'
   letterSpacing: '0.5px'
   padding: '10px 12px' per cell

3. Column widths — set explicitly:
   WORK ORDER column: minWidth: '160px'
   EQUIPMENT column: minWidth: '200px', flex: 1
   STATUS column: minWidth: '100px', width: '100px'

4. STATUS badge in each row:
   backgroundColor: '#fee2e2'
   color: '#dc2626'
   fontSize: '11px'
   fontWeight: 500
   borderRadius: '4px'
   padding: '3px 8px'
   whiteSpace: 'nowrap'
   display: 'inline-block'

5. Table data rows:
   fontSize: '12px'
   color: '#374151'
   padding: '9px 12px' per cell
   borderBottom: '1px solid #f1f5f9'
   backgroundColor alternating: '#ffffff' and '#fafafa' (zebra stripe)

6. "OVERDUE WORK ORDERS" section label:
   fontSize: '11px', fontWeight: 700, color: '#1a6eb5',
   textTransform: 'uppercase', letterSpacing: '0.8px'

7. "View All (1127)" link:
   fontSize: '12px', color: '#1a6eb5', fontWeight: 500,
   textDecoration: 'none'
   On hover: textDecoration: 'underline'

8. "Showing top 5 of 1127 total" sub-text:
   fontSize: '11px', color: '#9ca3af'


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 10 — INVENTORY & FLEET ANALYSIS KPI LIST
File: client/src/pages/pms/Dashboard.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHAT IS WRONG:
- The numbers (6678, 1820, 410 etc.) look correctly styled as plain
  bold text now — this part is mostly OK
- BUT the row separators between items may be missing or too faint
- The card background may not be pure white

EXACT FIXES:
1. Each KPI row (e.g. "Total Spares    6678"):
   display: 'flex', justifyContent: 'space-between', alignItems: 'center'
   padding: '10px 12px'
   borderBottom: '1px solid #f1f5f9'
   backgroundColor: '#ffffff'

2. Label text (e.g. "Total Spares"):
   fontSize: '13px', color: '#374151', fontWeight: 400

3. Value text — based on type:
   Total Spares:      fontSize: '14px', fontWeight: 700, color: '#1a2b4a'
   Low Stock:         fontSize: '14px', fontWeight: 700, color: '#e74c3c'
   Critical Low Stock: fontSize: '14px', fontWeight: 700, color: '#e74c3c'
   Total Components:  fontSize: '14px', fontWeight: 700, color: '#1a2b4a'
   Stores Inventory:  fontSize: '14px', fontWeight: 700, color: '#1a2b4a'

4. Remove ALL background-colored badge wrappers from numbers —
   numbers must be PLAIN BOLD TEXT only, no pill/badge background


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 11 — VESSEL / GROUP ANALYSIS MATRIX
File: client/src/pages/pms/Dashboard.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHAT IS WRONG:
- The table scrollbar is visible and looks heavy
- The matrix dots use colors that don't match reference well
- Table overflows and is partially cut off

EXACT FIXES:
1. Table wrapper:
   overflowX: 'auto'
   scrollbarWidth: 'thin'
   Add CSS: '::-webkit-scrollbar { height: 3px }'
            '::-webkit-scrollbar-thumb { background: #cbd5e1; borderRadius: 2px }'
            '::-webkit-scrollbar-track { background: transparent }'

2. Metric label column (first column):
   minWidth: '110px'
   fontSize: '12px', color: '#374151', fontWeight: 500
   padding: '8px 8px'

3. Each vessel column header:
   minWidth: '55px', textAlign: