You are an AI assistant helping me design and integrate an AI chatbot into my existing Planned Maintenance System (PMS) dashboard web app running on Replit.
​

The PMS is a vessel maintenance control center with modules and data such as:
​

Top navigation: Technical, Dashboard, Cert. & Surveys, Defects, PMS, Admin.
​

User role selector (e.g., “Sail Admin”).
​

Vessel selector (e.g., “Vessel 1”).
​

KPI cards: Overdue Work Orders, Due Work Orders, Pending Approval, Completed, Total Work Orders (all with counts and “View…” actions).
​

Charts: Work Order Status Distribution; Outstanding Tasks as % of Monthly Planned Maintenance; Spares Stock Status; Stores Inventory Breakdown (all interactive).
​

Lists: Overdue Work Orders with IDs, titles, status; Low Stock Spares with item name, figure/part no, ROB (remaining on board) and Min; plus summary tiles for Components, Spares Inventory (with low stock count), Stores Inventory (with low stock count).
​

Quick Actions: navigation to sub‑modules like Work Orders, Components, Spares, Reports.
​

Your task is to ask me, step by step, for all the information you need to:

Define the chatbot’s purpose and capabilities for this PMS dashboard.

Design its integration points in the UI and backend.

Specify data access, security, and context windows.

Choose a technical architecture suitable for Replit (front‑end + back‑end + model access).

Plan prompts, tools/functions, and example conversations.

Outline an implementation roadmap with concrete steps and files to create/edit in Replit.

Ask questions in small, clear batches, and always wait for my answers before proceeding. Start by asking me:

What are the primary use‑cases I want from this PMS chatbot (e.g., answering questions about work orders, generating reports, suggesting maintenance priorities, finding low stock spares, navigating to specific screens, etc.)?

Who are the target users (e.g., ship staff, office technical team, superintendent, admin), and what permissions/roles exist today in the PMS?

Whether the chatbot should just read existing PMS data, or also perform actions (e.g., create/update work orders, acknowledge overdue items, create spares requisitions, open certain dashboard filters).

After I answer, continue asking whatever further details you need (data models, API structure, authentication method, stack used in Replit, etc.) until you have enough information to produce:

A high‑level architecture description.

A list of required endpoints / functions for the chatbot tools.

Prompt templates and example user queries for each use‑case.

A file‑by‑file implementation plan for the Replit project.