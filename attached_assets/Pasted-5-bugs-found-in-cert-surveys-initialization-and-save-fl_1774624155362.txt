5 bugs found in cert-surveys initialization and save flow. Fix all:

BUG 1 & 2: Initialization skips already-initialized vessels
Files: certAdminService.ts:310-311, surveyAdminService.ts:281-282

Current: if (existingRecords.length > 0) return "already initialized, skip"
Problem: New company certs added AFTER initialization never reach the vessel.

Fix: Instead of skipping, MERGE new entries. Compare existing applicability records against current company certificates. If any company cert doesn't have an applicability record for this vessel, create one. Keep existing records untouched.

Something like:
- Get existing applicability masterId list for this vessel
- Get current company certificates list
- Find missing = company certs NOT in existing applicability
- Insert applicability records only for missing ones
- Return all records (existing + newly added)

BUG 3 & 4: getCompanyCertificates/Surveys filters too strictly
Files: certAdminRepository.ts:186, surveyAdminRepository.ts:145

Current: WHERE applicableToCompany = true
Fix: For initialization purposes, include ALL entries that are either:
- applicableToCompany = true (master entries marked for company), OR
- master_id LIKE 'CMP-%' (company-added entries — always include regardless of flag)

BUG 5: Save auto-propagation uses vessels from applicability, not vessels table
File: certAdminService.ts:67-72

Current: allVessels = getDistinctVessels() from applicability table
Fix: allVessels should come from the vessels table directly, not from existing applicability records. This ensures new/uninitialized vessels also receive company certs when saved.

Change to: allVessels = await storage.getVessels() or equivalent

TEST after fixing:
1. Initialize Vessel 1 with current company certs (should work)
2. Add a new company cert "Test Post-Init Cert" 
3. Go to Vessel 1's vessel tab — "Test Post-Init Cert" should now appear
4. Add a new vessel (Vessel 99) that was never initialized
5. Save a company cert — Vessel 99 should also get it
6. Clean up test data after verification

Commit with message: "fix(cert-surveys): merge new company entries into initialized vessels and fix propagation"