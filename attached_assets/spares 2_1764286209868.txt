Q1 – Location display in UI (A / B / C)

For each spare you have Location A + ROB A + Location B + ROB B.

How should the Inventory screen show this? it should show total rob. we can have a small arrow to open a small dialogue showing location A and b with the rob in them. 

Option A – 1 main row + expandable panel

Table shows: Part Code, Part Name, Total ROB, etc.

Clicking the row expands to show:

Location A, ROB A

Location B, ROB B

Option B – 4 columns in table

Columns: Location A, ROB A, Location B, ROB B (always visible)

Option C – Compact row + “breakdown” icon

Table shows Total ROB + Location A name.

A small icon opens a modal/popup showing full breakdown for this spare.

👉 Which one do you want? (A, B or C) Option A

Q2 – One spare linked to multiple components: how to show in list?

If Fuel Injector is linked with AE1, AE2, AE3:

Option 1: Show ONE row only, and in “Component” column list all: AE1 / AE2 / AE3

Option 2: Show THREE rows, one per link:

SP-ME-001 – AE1

SP-ME-001 – AE2

SP-ME-001 – AE3

Which behaviour do you prefer for the Inventory list? option 1

Q3 – Consumption when spare stored in 2 locations

Example:
Fuel Injector – Location A = 5, Location B = 3, Total = 8.
WO consumes 7.

What is the rule?

a) Reduce Location A first, then B (A→0, B→1)

b) Reduce Location B first, then A

c) Ask user which location to consume from (dropdown in WO) yes ask him

d) Some other fixed rule?

Q4 – Part code uniqueness & format (just confirm)

From template + your text, I understand:

Format ≈ VESSELCODE-0000 (e.g., V001-0001)

Unique per vessel (same part code won’t be reused for two different items on same ship)

The same physical spare used by AE1, AE2, AE3 will share one common part code.

Please confirm: ✅ Yes, this understanding is correct / ❌ No, change is… YES those will be handled while uploading the excel sheet. If manually adding, look at the pattern and provide next running number

(No need to rewrite format; just confirm or correct.)

Q5 – Deletion vs Inactivation

If a spare is “deleted” but it already has history / WO usage:

What should actually happen?

a) Soft delete / Inactive flag – spare disappears from normal lists but history remains. yes

b) Hard delete – remove completely (risky). no

Which do you want as standard rule? soft delete always

Q6 – Decimal quantities

Can ROB / Min Qty ever be decimal (e.g., 0.5 drum, 1.25 L, etc.)? no , always integer to keeep it simple

If No → all integer.

If Yes → how many decimals (1 / 2 / 3)?

Q7 – Stock-take / Reconciliation (now or later?)

Do you want a simple “Stock Take” mode in this version:

Export current ROB → user checks physically → import corrected ROB via Excel → system logs differences.

Or keep this for future phase? no- future phase

Answer: “Yes, include basic stock-take now” or “No, future feature”.

Q8 – Critical spare ROB = 0 alert , yes it must be red color status in the spares dashboard, also in alerts sub-module. also in the main dashboard of app will have the status of critical spares. 

Earlier you said no alerts for low stock generally.
But specifically for Critical = Yes & ROB = 0:

Do you want at least a warning/flag in the UI (red badge), even if no email/notification? yes definitely

Yes, show a strong red badge in list (no email).

No, same behaviour as normal spares.