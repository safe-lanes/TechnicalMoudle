CRITICAL ISSUES TO FIX - Due Jobs (7 Days) Report:

ISSUE #1: ONLY 1 JOB SHOWING - QUERY LOGIC IS TOO RESTRICTIVE

The report is only showing 1 job. This means the query filtering is wrong. You need to DEBUG and FIX:

ROOT CAUSE ANALYSIS NEEDED:
1. Check what TODAY's date is being calculated as
2. Check the actual dueDate values in the work_orders table
3. Check if you're using the correct date comparison logic

CORRECTED QUERY LOGIC:

For CALENDAR-BASED jobs, include if ANY of these conditions are met:
a) dueDate is within next 7 days: (dueDate >= TODAY AND dueDate <= TODAY + 7 days)
b) Job is ALREADY OVERDUE: (dueDate < TODAY)
   - These are HIGH PRIORITY and must be included!

For RUNNING HOURS-BASED jobs:
a) Due within 168 hours: (nextDueReading - currentCumulativeRH) <= 168 AND > 0
b) ALREADY OVERDUE in RH: currentCumulativeRH >= nextDueReading
   - These are HIGH PRIORITY and must be included!

EXCLUSIONS (Only exclude these):
- status = 'Completed' (already done)
- status = 'Postponed' (delayed officially)

DO NOT EXCLUDE:
- Overdue jobs (these are MOST IMPORTANT!)
- Jobs with dueDate in the past
- Jobs where current RH exceeds next due RH

CORRECTED FILTERING CODE:

// Calendar-based filter
const calendarFilter = {
  maintenanceBasis: { $in: ['Calendar', 'Calendar+RH'] },
  $or: [
    // Due in next 7 days
    { 
      dueDate: { 
        $gte: new Date(), 
        $lte: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000) 
      } 
    },
    // Already overdue
    { dueDate: { $lt: new Date() } }
  ],
  status: { $nin: ['Completed', 'Postponed'] }
};

// For RH-based: You'll need to calculate in code after fetching
// because currentCumulativeRH is in components table

DEBUG OUTPUT REQUIRED:
Before generating Excel, console.log:
1. Total work orders fetched from database
2. Today's date
3. Date range being queried (today to today+7)
4. How many jobs filtered out and why
5. Sample of the dueDate values from the database

---

ISSUE #2: EXCEL FORMAT NOT PROFESSIONAL

The format needs significant improvement for a professional maritime report.

REQUIRED CHANGES:

A. REPORT HEADER SECTION (Above the data table):
Row 1: "PLANNED MAINTENANCE SYSTEM" (Merge A1:P1, Font 18, Bold, Center)
Row 2: "DUE JOBS REPORT (NEXT 7 DAYS)" (Merge A2:P2, Font 14, Bold, Center)
Row 3: Empty
Row 4: "Vessel: [Vessel Name]" in A4 (Bold)
Row 4: "Report Date: [DD-MMM-YYYY HH:MM]" in N4 (Bold, Right-aligned)
Row 5: "Generated By: [Username]" in A5
Row 5: "Total Jobs: [Count]" in N5 (Right-aligned)
Row 6: Empty
Row 7: Start of table headers

B. TABLE HEADER ROW (Row 7):
Background: #1F4E78 (Dark Blue, not light blue)
Font: White, Bold, Size 11
Text: Centered vertically and horizontally
Add thin white borders between columns
Row Height: 25

C. DATA ROWS (Starting Row 8):
- Alternate row colors: White (#FFFFFF) and Light Gray (#F2F2F2)
- All borders: Thin gray (#D3D3D3)
- Text alignment:
  - Numbers (S.No, Days, RH values): Right-aligned
  - Dates: Center-aligned
  - Text: Left-aligned
- Row Height: 20

D. CONDITIONAL FORMATTING (Keep existing but enhance):
- OVERDUE JOBS (dueDate < today OR currentRH > nextDueRH):
  - Background: #FFC7CE (Light Red)
  - Text: #9C0006 (Dark Red)
  - Font: Bold
  - Add "⚠" symbol before job title
  
- URGENT (Due in ≤2 days or ≤48 RH):
  - Background: #FFF4CE (Light Yellow)
  - Text: #9C6500 (Dark Orange)
  - Font: Bold
  
- CRITICAL PRIORITY or CRITICAL EQUIPMENT:
  - Priority column: Red text (#FF0000), Bold
  - Critical column: Red text (#FF0000), Bold, "⚠ YES"

E. SUMMARY SECTION (After data, skip 2 rows):
Row X: Empty
Row X+1: Border top (thick black line across A to P)
Row X+2: 
  - A: "SUMMARY:" (Bold, Size 12)
Row X+3:
  - A: "Total Jobs Due:"
  - B: [Count] (Bold)
Row X+4:
  - A: "Overdue Jobs:"
  - B: [Count of overdue] (Bold, Red if > 0)
Row X+5:
  - A: "Critical Priority Jobs:"
  - B: [Count of critical] (Bold)
Row X+6:
  - A: "Critical Equipment Jobs:"
  - B: [Count of critical equipment] (Bold)

F. COLUMN ADJUSTMENTS:

Change column order for better readability:
A: S.No (Width: 6)
B: Priority (Width: 10) - Move this up front for visibility
C: Status Indicator (Width: 8) - New column: "Overdue", "Urgent", "Due", "OK"
D: Work Order No (Width: 22)
E: Job Title (Width: 40)
F: Component Code (Width: 15)
G: Component Name (Width: 30)
H: Department (Width: 12)
I: Due Date (Width: 15)
J: Days Remaining (Width: 12)
K: Next Due RH (Width: 14)
L: Current RH (Width: 14)
M: RH Remaining (Width: 12)
N: Frequency (Width: 18)
O: Assigned To (Width: 20)
P: Critical (Width: 10)

G. NEW CALCULATED COLUMN - "Status Indicator" (Column C):
Logic:
- If dueDate < today OR currentRH > nextDueRH: "OVERDUE"
- If daysRemaining <= 2 OR rhRemaining <= 48: "URGENT"
- If daysRemaining <= 7: "DUE"
- Else: "ACTIVE"

H. DATE & NUMBER FORMATTING:
- Due Date: Format as "DD-MMM-YYYY" (e.g., "11-Feb-2026")
- Days Remaining: Format as number, no decimals
- RH values: Format as number with thousand separator (e.g., "5,000")
- Frequency: Show as "3 Months" or "500 Hours"

I. FREEZE PANES:
- Freeze Row 7 (header row) so it stays visible when scrolling
- Freeze Column C (keep S.No and Priority visible when scrolling right)

J. PAGE SETUP FOR PRINTING:
- Orientation: Landscape
- Paper: A4
- Print area: A1:P[LastRow]
- Fit to: 1 page wide
- Repeat header row (Row 7) on every page
- Add footer: "Page [Page] of [Pages]" (Center), "Printed: [DateTime]" (Right)

K. AUTO-FILTER:
- Enable auto-filter on header row (Row 7)
- This allows users to sort and filter in Excel

---

ISSUE #3: FILE NAMING

Current filename format is good, but standardize:
Format: "PMS_DueJobs7Days_[VESSELNAME]_[YYYYMMDD]_[HHMM].xlsx"
Example: "PMS_DueJobs7Days_Vessel4_20260204_1145.xlsx"

---

IMPLEMENTATION CHECKLIST:

Before you code, confirm:
□ You understand why only 1 job is showing (debug the query first)
□ You have the correct date comparison logic
□ You will include OVERDUE jobs (they are most important!)
□ You will add the professional header section
□ You will add the status indicator column
□ You will add the summary section with counts
□ You will implement alternate row coloring
□ You will add freeze panes
□ You will enable auto-filter

After implementation:
□ Test with multiple jobs (should show more than 1)
□ Test with overdue jobs (they should appear at top in red)
□ Test with critical jobs (red highlighting works)
□ Test printing layout (fits on page)
□ Verify all columns are visible and readable

NOW: First, debug and fix the query to show ALL due jobs (including overdue). Show me the console output with job counts. Then implement the professional formatting.
