FIXES REQUIRED - SUPERINTENDENT NOTIFICATIONS & APPROVAL WORKFLOW
ISSUE 1: Vessel Name Bug (CRITICAL - Data Corruption)
Problem: Both workOrderService.ts (line 55) and anomalyDetectionService.ts (line 167) store wo.vesselId (UUID) in the vesselName field instead of the actual vessel name.

Fix Required:

Update notification creation code in both files to properly resolve vessel name from vesselId before storing:

text
// Instead of: vesselName: wo.vesselId || ''
// Should be: vesselName: await getVesselNameFromId(wo.vesselId) || ''
Data Migration Script to fix existing records in superintendent_notifications table:

Query all records where vessel_name looks like a UUID pattern

Lookup actual vessel name from vessels table using the UUID

Update vessel_name column with correct vessel name

Log how many records were updated

Verification: After fix, dashboard should show correct counts when filtering by specific vessel

ISSUE 2: Threshold Mismatch - Approval Tier System (CRITICAL - Business Logic Error)
Problem: Current approval tier system uses incorrect thresholds that don't match Layer 4B and Layer 5 specifications.

Current Implementation (WRONG):

superintendent_locked: Missed cycles ≥ 1

superintendent_notification: Days late > 14 (and 0 missed cycles)

ce_with_justification: Days late 7–14

Missing: Backdating detection entirely

Correct Implementation (Layer 4B & Layer 5 Spec):

Condition	Tier	CE Remarks	Superintendent Action
HIGH SEVERITY: Missed cycles ≥ 3 OR Days late ≥ 21 OR Backdating ≥ 7 days	superintendent_locked	Mandatory (min 20 characters)	MUST acknowledge before CE can approve (Hard block)
MEDIUM SEVERITY: Missed cycles = 2 OR Days late 14-20 OR Backdating 3-6 days	superintendent_notification	Mandatory (min 20 characters)	Notification created for awareness (No block, CE can approve)
LOW SEVERITY: Missed cycles = 1 OR Days late 7-13 OR Backdating 1-2 days	ce_with_justification	Mandatory (min 10 characters)	No superintendent notification
STANDARD: No issues	standard	Optional	No notification
Fix Required in workOrderService.ts:

Update the approval tier calculation logic to:

typescript
// Pseudo-code for correct logic
const missedCycles = calculateMissedCycles(wo);
const daysLate = calculateDaysLate(wo);
const backdatingDays = calculateBackdatingDays(wo); // NEW - must be added

let approvalTier: string;
let superintendentNotificationRequired: boolean;

// HIGH SEVERITY - Superintendent LOCKED (hard block)
if (missedCycles >= 3 || daysLate >= 21 || backdatingDays >= 7) {
  approvalTier = 'superintendent_locked';
  superintendentNotificationRequired = true;
  ceRemarksRequired = true;
  ceRemarksMinLength = 20;
}
// MEDIUM SEVERITY - Superintendent NOTIFICATION (awareness only)
else if (missedCycles === 2 || (daysLate >= 14 && daysLate < 21) || (backdatingDays >= 3 && backdatingDays < 7)) {
  approvalTier = 'superintendent_notification';
  superintendentNotificationRequired = true;
  ceRemarksRequired = true;
  ceRemarksMinLength = 20;
}
// LOW SEVERITY - CE Justification only
else if (missedCycles === 1 || (daysLate >= 7 && daysLate < 14) || (backdatingDays >= 1 && backdatingDays < 3)) {
  approvalTier = 'ce_with_justification';
  superintendentNotificationRequired = false;
  ceRemarksRequired = true;
  ceRemarksMinLength = 10;
}
// STANDARD - No issues
else {
  approvalTier = 'standard';
  superintendentNotificationRequired = false;
  ceRemarksRequired = false;
}

// Create superintendent notification if required
if (superintendentNotificationRequired) {
  await createSuperintendentNotification({
    workOrderId: wo.id,
    workOrderCode: wo.code,
    jobTitle: wo.jobTitle,
    componentName: wo.componentName,
    vesselName: await getVesselNameFromId(wo.vesselId), // FIX from Issue 1
    daysLate: daysLate,
    missedCycles: missedCycles,
    backdatingDays: backdatingDays, // NEW field
    approvalTier: approvalTier
  });
}
Additional Requirements:

Add backdating_days column to superintendent_notifications table (integer, default 0)

Update validation error messages to reflect new thresholds

Update frontend UI to show correct severity messages

ISSUE 3: Missed Cycles Justification Field
Problem: Current code requires "Skipped Cycles Justification" (30 characters) for missed cycles ≥ 1, separate from CE Remarks.

Clarification Needed:

Is this "Skipped Cycles Justification" a separate field from CE Remarks?

Or should CE Remarks be the only field required (with varying min lengths based on severity)?

Proposed Unified Approach:

Use CE Remarks field only (single field for all justifications)

Minimum character requirements based on severity tier:

HIGH/MEDIUM severity: 20 characters

LOW severity: 10 characters

STANDARD: Optional (0 characters)

Remove separate "Skipped Cycles Justification" field to simplify UX

Please confirm which approach is correct:

Keep separate "Skipped Cycles Justification" (30 char) + "CE Remarks" (20 char)?

Use unified "CE Remarks" field with variable min length?

ISSUE 4: Anomaly Detection System Alignment
Current State: anomalyDetectionService.ts uses correct Layer 5 thresholds (HIGH: ≥3 cycles, ≥21 days late, ≥7 days backdating).

Required Action:

Ensure anomaly detection and approval tier systems use IDENTICAL thresholds

Both should trigger on HIGH: ≥3 cycles OR ≥21 days late OR ≥7 days backdating

Verify no duplicate notifications are created (one from approval tier, one from anomaly detection)

Question: Should anomaly detection notifications be separate from approval tier notifications, or should they be unified into a single notification system?

DELIVERABLES REQUESTED:
Code Fix for workOrderService.ts:

Fix vessel name resolution (Issue 1)

Update approval tier thresholds to Layer 4B/5 spec (Issue 2)

Add backdating detection to approval tier logic (Issue 2)

Update CE remarks validation logic (Issue 3)

Code Fix for anomalyDetectionService.ts:

Fix vessel name resolution (Issue 1)

Ensure threshold alignment with approval tier system (Issue 4)

Database Migration Script:

Fix existing vessel_name data (UUID → actual name)

Add backdating_days column to superintendent_notifications table

Database Schema Update:

Add backdating_days INTEGER DEFAULT 0 to superintendent_notifications

Verification Steps:

Test with a work order that has 5 missed cycles → Should trigger superintendent_locked

Test with 21+ days late → Should trigger superintendent_locked

Test with 7+ days backdating → Should trigger superintendent_locked

Test dashboard with Vessel 3 selected → Should show correct notification counts

Test CE approval blocking when tier is superintendent_locked

PRIORITY: HIGH - These are critical business logic errors that affect the core anti-fraud features (Layers 4B and 5).

Please provide the updated code files and migration script, or confirm you need any clarifications before proceeding with the implementation.