REQUIREMENT: Add Running Hours History Feature to Frontend

CONTEXT:
Currently, the Running Hours page lacks a historical view showing when running hours were updated, what values were entered, and by which user. This historical data is critical for audit trails and fraud detection.

BEFORE IMPLEMENTATION - IMPORTANT:
1. First, check if running hours audit/history data is ALREADY being stored in the backend database
2. Identify the existing table name and schema (likely running_hours_audit or similar)
3. DO NOT create new tables if historical data is already being captured
4. Provide the existing table structure for review

REQUIREMENT DETAILS:

UI/UX REFERENCE:
Implement the EXACT SAME UI pattern as the Spares and Stores modules' "History" feature:
- Same button styling and placement
- Same modal/page design
- Same table layout and formatting
- Same interaction patterns

FEATURE STRUCTURE:

1. MAIN RUNNING HOURS PAGE:
   - Add a "History" button (similar to Spares/Stores)
   - Keep the same list of machinery/components currently displayed
   - When user clicks on any component → open history page/modal

2. HISTORY PAGE/MODAL:
   - Show tabular historical records for the selected component
   - Columns to include:
     * Date/Time of entry (when the RH was recorded)
     * Running Hours value entered
     * Previous RH value (for comparison)
     * Difference/Change in hours
     * Updated by (user name/rank)
     * Remarks/Notes (if any)
   
3. REQUIRED FEATURES (same as Spares/Stores):
   - Pagination (10/25/50/100 items per page)
   - Export facility (CSV/Excel)
   - Date range filter
   - Sort by date (ascending/descending)
   - Search functionality
   - Responsive design matching existing UI

4. DATA SOURCE:
   - First check what table currently stores this historical data
   - Query should retrieve:
     * All historical entries for the selected component
     * User information who made the entry
     * Timestamps of when entries were made
     * Any audit trail information

IMPLEMENTATION STEPS:

Step 1: Database Investigation
- Search for existing running hours history/audit tables
- Document the schema
- Confirm if data is already being captured
- If not, identify where running hours updates are currently stored

Step 2: Backend API
- Create endpoint: GET /api/running-hours/history/:componentId
- Add pagination, filtering, sorting parameters
- Add export endpoint: GET /api/running-hours/history/:componentId/export

Step 3: Frontend Implementation
- Copy UI components from Spares/Stores history feature
- Maintain consistent styling with existing codebase
- Add History button to Running Hours page
- Create history table component with same layout
- Implement pagination using existing pagination component
- Add export functionality using same export utility

Step 4: Testing Requirements
- Verify history shows for all components
- Test pagination with large datasets
- Verify export works correctly
- Test date filtering
- Verify user information displays correctly

CRITICAL NOTES:
- DO NOT create new database tables without confirming existing structure
- Reuse existing UI components from Spares/Stores for consistency
- Match the exact visual design and user experience
- Ensure proper indexing for performance
- Add proper error handling

Please start by:
1. Investigating the database schema for running hours history
2. Showing me the existing table structure and columns
3. Confirming what data is already being captured
4. Then we can proceed with implementation

Do NOT implement anything yet - just provide the database investigation results first.
