PROMPT FOR REPLIT AI TO IMPLEMENT UI CHANGES:
text
Based on your analysis, please implement the UI standardization for all report detail pages. Here are the specific changes needed:

SCOPE:
Update these files to match Dashboard.tsx and JobsFormPage.tsx patterns:
- client/src/pages/reports/MaintenanceRecords.tsx
- client/src/pages/reports/RunningHours.tsx
- client/src/pages/reports/SparesReports.tsx
- client/src/pages/reports/StoresReports.tsx
- client/src/pages/reports/IHMReports.tsx
- client/src/pages/reports/ModifyPMS.tsx
- client/src/pages/reports/PMSAdmin.tsx

IMPLEMENTATION REQUIREMENTS:

1. HEADER SECTION:
   - Remove the large colored icon box completely
   - Replace with simple layout:
     ```tsx
     <div className="flex items-center gap-4 mb-6">
       <Button variant="ghost" onClick={() => navigate('/pms/reports')}>
         <ArrowLeft className="h-4 w-4 mr-2" /> Back to Reports
       </Button>
       <div>
         <h1 className="text-2xl font-bold text-gray-900">Maintenance & Work Orders</h1>
         <p className="text-sm text-gray-500">10 reports for maintenance tracking</p>
       </div>
     </div>
     ```

2. STATISTICS CARDS ROW:
   - Convert to Dashboard's border-l-4 pattern
   - Use gap-4 instead of gap-6
   - Smaller icons (w-4 h-4 instead of w-8 h-8)
   - Example:
     ```tsx
     <Card className="border-l-4 border-l-blue-500 bg-white">
       <CardHeader className="pb-2">
         <CardDescription className="flex items-center gap-1">
           <FileText className="w-4 h-4 text-blue-500" />
           Total Reports
         </CardDescription>
         <CardTitle className="text-3xl">10</CardTitle>
       </CardHeader>
     </Card>
     ```

3. CONVERT CARD GRID TO TABLE:
   - Replace the 2-column card grid with a clean table layout
   - Table structure:
     ```tsx
     <div className="rounded-lg border border-gray-200 overflow-hidden bg-white">
       <table className="w-full">
         <thead>
           <tr className="bg-gray-50 border-b border-gray-200">
             <th className="text-left py-3 px-4 font-semibold text-sm text-gray-700">Report Name</th>
             <th className="text-left py-3 px-4 font-semibold text-sm text-gray-700">Frequency</th>
             <th className="text-left py-3 px-4 font-semibold text-sm text-gray-700">Priority</th>
             <th className="text-left py-3 px-4 font-semibold text-sm text-gray-700">Est. Time</th>
             <th className="text-left py-3 px-4 font-semibold text-sm text-gray-700">Actions</th>
           </tr>
         </thead>
         <tbody className="divide-y divide-gray-200">
           {/* Report rows */}
         </tbody>
       </table>
     </div>
     ```

4. TABLE ROW DESIGN:
   - Each report as a table row
   - Report Name: Bold text with description below
   - Frequency: Badge with variant="outline"
   - Priority: Colored Badge (bg-red-100 text-red-800 for HIGH, bg-yellow-100 text-yellow-800 for MEDIUM, bg-gray-100 text-gray-800 for LOW)
   - Est. Time: Small gray text (text-xs text-gray-500)
   - Actions: Icon buttons only
   - Add hover:bg-gray-50 cursor-pointer to rows
   
   Example row:
   ```tsx
   <tr className="hover:bg-gray-50 cursor-pointer">
     <td className="py-3 px-4">
       <div>
         <div className="font-medium text-gray-900">Due Jobs (7 days)</div>
         <div className="text-sm text-gray-500">Upcoming work orders due in the next 7 days</div>
       </div>
     </td>
     <td className="py-3 px-4">
       <Badge variant="outline">Daily/Weekly</Badge>
     </td>
     <td className="py-3 px-4">
       <Badge className="bg-red-100 text-red-800">HIGH</Badge>
     </td>
     <td className="py-3 px-4">
       <span className="text-xs text-gray-500">&lt; 1 min</span>
     </td>
     <td className="py-3 px-4">
       <div className="flex items-center gap-1">
         <Button size="icon" variant="ghost" title="Preview">
           <Eye className="h-4 w-4" />
         </Button>
         <Button size="icon" variant="ghost" title="Download PDF">
           <FileText className="h-4 w-4" />
         </Button>
         <Button size="icon" variant="ghost" title="Download Excel">
           <Sheet className="h-4 w-4" />
         </Button>
       </div>
     </td>
   </tr>
HIDE EXCESSIVE METADATA:

Remove "Purpose", "Key Fields", and "Outputs" sections from the table view

These can be shown in a tooltip or modal when clicking on the report row

Keep the table clean and scannable

FILTERS & SEARCH:

Keep the search and filter dropdowns at the top

Use the same inline flex layout as current implementation

Ensure consistent styling with other pages

LAYOUT & SPACING:

Container: p-6 bg-white (remove bg-[#fafafa] if present)

Stats grid: grid-cols-4 gap-4

Table margin-top: mt-6

Consistent padding throughout

ADDITIONAL NOTES:

Maintain all existing functionality (filtering, searching, actions)

Keep the same data structure and props

Use Shadcn components consistently: Button, Badge, Card, Input, Select

Import icons from lucide-react

Ensure responsive design works on mobile

Please implement these changes across all 7 report detail page files while preserving all current functionality.

text

***

This prompt provides complete implementation details for Replit AI to standardize your Reports section UI. It includes:
- Exact code examples
- Specific styling classes
- Component structure
- All visual specifications from the analysis