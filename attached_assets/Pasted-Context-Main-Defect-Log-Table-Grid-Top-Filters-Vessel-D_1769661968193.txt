Context:
Main Defect Log – Table Grid (Top Filters: Vessel, Due/Overdue, Period, Clear button)

Issue Summary:
None of the top filters in the Main Defect Log table grid are functioning correctly.
Filtering logic and filter state reset behavior are inconsistent and incorrect.

1) Vessel Filter Issue
Current Behavior:
- Selecting any Vessel results in an empty table, even though defect records exist for the selected vessel.

Expected Behavior:
- Selecting a Vessel should correctly filter and display all defect records linked to the selected vessel.
- No records should be hidden if valid data exists.

2) Due / Overdue Filter Issues
Current Behavior:
- Selecting "Overdue" does not display all overdue defect reports
  (e.g., only 5 records are shown when 7 overdue reports exist).
- Records with "Verified" status are incorrectly included in the Overdue list.

Expected Behavior:
- Overdue filter should display all overdue defect reports based on due date logic.
- Records with Verified status must be excluded from Overdue results.
- Filtering must be accurate and consistent.

3) Clear Button – Filter Reset Issue
Current Behavior:
- Clicking the Clear button resets the table data to show all records.
- However, filter dropdowns (Due/Overdue, Period, etc.) still display the previously selected values.

Expected Behavior:
- Clicking Clear must:
  - Reset the table data to show all records
  - Reset all filter selection fields to the default value "All"
  - Clear any internal filter state or query parameters

Constraints (Strict):
- Do NOT change backend APIs, database schema, or business logic unless strictly required.
- Do NOT modify unrelated screens, tables, or filters.
- Do NOT refactor existing working logic.
- Do NOT introduce new validations, statuses, or filter options.
- Scope must remain limited to Main Defect Log top filters only.

Implementation Guidelines:
- Apply minimal and localized changes only.
- Fix filtering logic and UI filter state together.
- Ensure filters work correctly both individually and in combination.
- Follow existing filtering and state management patterns.
Confirm Understanding & Ask questions if in doubt
