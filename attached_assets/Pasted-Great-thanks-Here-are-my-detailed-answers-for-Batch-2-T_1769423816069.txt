Great, thanks. Here are my detailed answers for Batch 2 – Tech Stack and Project Structure.

Please read all of this, then:

Acknowledge anything that looks sub‑optimal for adding an AI chatbot.

Tell me what you need clarified or adjusted before we design endpoints/tools.

Now here are my answers in your requested structure:

Frontend Stack
Frontend tech

The PMS dashboard frontend is built with:

[Describe it explicitly: e.g. React + Vite, or plain HTML/CSS/JS, or Next.js, etc.]

Entry point file(s):

[e.g. src/main.jsx, index.html with inline JS, etc.]

Dashboard structure

The dashboard is structured as:

[Choose one]

A single page with conditional rendering.

Multiple routes handled on the frontend (e.g. React Router).

Server‑rendered templates per page.

Current PMS dashboard page(s):

[List main route(s) or filenames for the PMS dashboard, e.g. /dashboard, /pms, Dashboard.jsx, etc.]

Component library

UI libraries used:

[e.g. Tailwind, Bootstrap, Material UI, none, etc.]

Any existing modal/side panel components:

[Yes/No. If yes, name the component/file where they live.]

Backend Stack
Backend framework

The backend for this PMS project uses:

[e.g. Node + Express, Flask, FastAPI, Django, etc.]

Backend location

The backend is:

[Choose one]

In the same Replit project as the frontend (monolith).

In a separate Replit project/service.

API exposure

APIs are currently exposed as:

[Choose all that apply]

REST endpoints (JSON).

Server‑side rendered pages only.

WebSocket endpoints.

Main backend entry file:

[e.g. index.js, app.js, main.py, etc.]

Project Structure
Top‑level folder/file tree

Here is my top‑level project structure (output of tree -L 2 or equivalent):

text
[PASTE YOUR tree -L 2 OUTPUT HERE]
Existing API/route folders

Existing API / routes / controllers structure:

[e.g. src/routes, routes/api, api, controllers, etc.]

Where I currently define REST endpoints:

[Name the main files, e.g. routes/workorders.js, api/spares.py, etc.]

Where you think new chatbot endpoints should ideally live:

[If you have a preference, state it; otherwise say you’re open to their suggestion.]

Authentication & Sessions
Authentication method

Users authenticate via:

[e.g. username/password login form with session cookies, or JWT stored in localStorage, etc.]

Brief description of auth flow:

[1–2 sentences on how login works and how auth is checked for each request.]

Current user role and vessel selection

How the app knows the current user:

[e.g. user info stored in session, in JWT, or fetched from /me endpoint.]

How the app knows the current role and selected vessel:

[e.g. stored in session, in Redux/store, URL param, context, localStorage, etc.]

How many roles and vessels a user can have:

[e.g. one role per user, multiple vessels; or multiple roles; etc.]

Role‑based access middleware

Existing middleware / mechanism enforcing role‑based access:

[Yes/No. If yes, name the file and briefly describe how it works.]

If there’s no dedicated middleware, explain how access control is currently implemented:

[e.g. checks inside each route handler, or no checks yet.]

After you process this, please:

Confirm if this stack is suitable for integrating an AI chatbot with tool‑calling style APIs.

Point out any structural changes you recommend (e.g., adding a dedicated api/chatbot route, refactoring routes, adding auth middleware).

Then move on to Batch 3 – Data Models & Database, asking me exactly the information you need there.