You have completed the investigation, but now I want you to stop recommending your own product interpretations.

From this point onward, treat the below as confirmed requirement, not as something to re-decide.

The Reports module currently shows separate top-level categories such as “Critical Equipment” and “LSA/FFA Equipment,” and the screen message explicitly says users should expand a category and click a report to view it. Follow that existing report-tree interaction model exactly for the new category.

Confirmed Requirement — Do Not Reinterpret
1. Tree Structure
Create a new top-level category in Reports:

Class Items

It must be placed below LSA/FFA Equipment in the left-side tree.

It is a separate category, not under LSA/FFA.

2. Report Structure Inside Category
Since current Reports behavior is category expansion + report click, keep the same pattern.

So:

add Class Items as a top-level category

inside it, create the report item:

Class Items Master List

Do not ask again whether category click itself should open the table.
Use the existing tree behavior already followed in Reports.

3. Primary Data Logic
This report must list:

all jobs where Job.Class Related = Yes

This is the primary filter logic.

Do not shift the main logic to:

component class item

both flags together

alternative regulatory interpretations

You may include component class item only as an additional display field if already available, but it is not the primary filter condition.

4. What the Table Must Show
The report table must combine:

A. Section C style job listing
Meaning the report should show job-oriented columns similar to the jobs listing visible in Section C of the Components submodule, such as:

Job Code

Job Title

Task Type

Frequency

Last Done Date

Next Due Date

B. Section G component classification/regulatory details
Meaning the report must also show the Classification & Regulatory Data fields from Section G of the Components submodule, such as:

Classification Society

Certificate No.

Last Class Survey

Next Class Survey

Survey Type

Class Requirements

Class Code

Information

The goal is:

primary row basis = class-related jobs

enriched with linked component identity

enriched with Section G regulatory/classification details

Important Direction Changes
Do not do these:
Do not compare this report to LSA/FFA

Do not propose “similar to LSA/FFA”

Do not tell me “copy pattern from LSA/FFA”

Do not re-open business logic questions already decided above

Do not invent optional UX interpretations unless technically necessary

Do this instead:
Treat requirement as frozen

Translate it into exact implementation scope

Identify where code changes are needed

Identify where schema gaps exist, if any

Tell me exactly what to build

Specific corrections to your previous response
You previously added assumptions like:

one row per job × class record

include jobs even when no component link exists

drop or repurpose Class Code / Information

maybe one child node is required as recommendation

Do not make such product decisions on your own.

Instead, now do the following:

1. Confirm current technical structure only
If current Reports tree requires a child report node, then state that as current implementation requirement, not as a recommendation.

2. Confirm exact mapping for Section G fields
I want a precise table:

| UI Label in Section G | Exact DB Column | Table Name | Already Available in API? |

Especially for:

Classification Society

Certificate No.

Last Class Survey

Next Class Survey

Survey Type

Class Requirements

Class Code

Information

If “Class Code” and “Information” do not exist as actual stored backend fields, say that clearly and identify whether:

they are only UI labels

they are mapped from another field

or they require schema change

Do not silently substitute them.

3. Confirm exact row-grain options without deciding for me
Tell me what the backend currently allows if a component has multiple Section G records:

one row per job

one row per component-job-link

one row per component-job-link-regulatory-record

But do not finalize one without instruction. Just state the technical consequence of each.

4. Confirm exact orphan behavior possibilities
If a class-related job has no linked component:

can it still be returned from jobs table? yes/no

can Section G be shown? yes/no

what would each implementation option mean technically?

Again, do not choose behavior unless current code already enforces one.

Now give me the output in this format
Locked Requirement Restatement

Reports Tree Implementation Requirement

Confirmed Primary Filter Logic

Section C Job Fields Available

Section G Field Mapping Table

Join Structure and Possible Row Grain

API / Backend Changes Required

Frontend Changes Required

Schema Gaps Requiring Decision

Exact Build Scope Without Assumptions

Final Instruction
This is no longer an investigation for open interpretation.

This is now a translation of confirmed product requirement into exact implementation scope.

Do not propose alternatives unless there is a real technical blocker.