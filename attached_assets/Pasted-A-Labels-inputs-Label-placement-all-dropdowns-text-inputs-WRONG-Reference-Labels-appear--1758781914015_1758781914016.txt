A) Labels & inputs

Label placement (all dropdowns & text inputs) — WRONG

Reference: Labels appear inside the inputs as grey uppercase placeholders (e.g., VESSEL, SOURCE, DEFECT CATEGORY, DEFECT TYPE, CATEGORY, TYPE, MAKE, MODEL, DATE ISSUED, TARGET DATE, DATE COMPLETED).

Replit: Labels are outside/above the fields in black text.

Fix: Move labels back inside the inputs as placeholders; remove the above-field labels.

“Purchase Order” row — WRONG

Reference: Small blue subheading “Purchase Order” above a single input PO REF; Critical checkbox inline to the right of this row.

Replit: “Purchase Order” looks like a blue link; Critical checkbox is pushed to the far right, misaligned vertically.

Fix: Style the “Purchase Order” text as a section caption (not link). Place PO REF input left; put Critical checkbox in the same row, left-aligned right next to PO REF (as in the reference).

B) Section headers, dividers & containers

Section styling — WRONG

Reference: Sections (“Details”, “Cause Analysis”, “Actions”) use thin blue dividers; the content sits in a flat, light background (no card borders).

Replit: Each section is wrapped in a rounded card with darker borders; blue dividers are missing.

Fix: Remove card borders/rounded boxes; restore the thin blue horizontal divider under each section title.

Sub-section headings (“Basic / Equipment / Hardware / Date”) — WRONG

Reference: Compact blue sub-headings centered above each 3-column group; consistent spacing below.

Replit: Black text headings; spacing to the fields is larger and uneven.

Fix: Use blue sub-headings and tighten the top/bottom spacing to match reference.

C) Grid & alignment

Field grid alignment — WRONG

Reference: Three equal columns across:
Column 1 (Basic): Vessel, Source, Defect Category, Defect Type
Column 2 (Equipment/Hardware): Category, Type, Make, Model
Column 3 (Date): Date Issued, Target Date, Date Completed

Replit: Same fields but vertical spacing and baselines are off; “Purchase Order” line breaks alignment.

Fix: Use a strict 12-column grid; each field is the same width and shares a common baseline per row.

Defect ID & top-bar controls — WRONG

Reference: Defect ID (e.g., DR/007/21/1243/V) shown in blue on the top right; Back / View as small pills; Save (blue) on far right.

Replit: Defect ID is left below the title; Back and Save use a different style.

Fix: Place ID at top right in blue; Back & View small pills on the right; Save solid blue button at the far right.

D) Description & severity row

Severity control — WRONG

Reference: Left-aligned green button 2 - Minor with built-in caret; to its right: VIQ VER, VIQ REF, SFI CODE REF; Save button at the far right of that same row.

Replit: A small green “Minor” pill (badge, not a control); Save is pushed below the description on the right.

Fix: Implement a large green dropdown button labeled 2 - Minor (or current level). Keep VIQ/SFI pickers on the same row; put Save on the far right of that row.

E) Cause Analysis block

Immediate vs. Root Cause layout — WRONG

Reference: Two rows:
Row 1 → Immediate Cause (left) + Further Explanation (right)
Row 2 → Root Cause (left) + Further Explanation (right)

Replit: Immediate Cause and Root Cause are side by side in a single row.

Fix: Stack as two rows exactly like the reference.

“Select” buttons & labels — WRONG

Reference: Small Select pill under the small left caption (“Immediate Cause” / “Root Cause”), then the textarea with the same caption inside as placeholder.

Replit: “Select” is beside the heading; headings are bold black and repeated above textareas.

Fix: Match reference: caption (light) → Select pill below it → textarea with the placeholder text (IMMEDIATE CAUSE, ROOT CAUSE, FURTHER EXPLANATION).

F) Actions table & footer controls

“+ Add Action” button — WRONG

Reference: Small outline pill on the right of the Actions header row.

Replit: Grey solid pill sitting lower than header.

Fix: Use the outline style, align to top-right of the Actions section.

Status cell (“Close”) — WRONG

Reference: Status is text in the last column (e.g., Close) in table font.

Replit: Dark rounded chip labeled Close.

Fix: Replace the chip with plain table text styling.

Upload / View buttons — WRONG

Reference: Small blue outline buttons with icons.

Replit: Grey/white icon buttons with different sizing.

Fix: Use blue outline pill buttons with the same size as reference.

G) Color / typography

Color palette — WRONG

Reference:

Blue dividers & headings

Inputs with light grey borders and grey placeholder text

Buttons: solid blue (primary), green severity control.

Replit:

Section cards with dark borders

Black labels above inputs

Mixed button styles (grey/white chips).

Fix: Remove card borders; use blue dividers and placeholder labels; standardize primary buttons (solid blue) & severity (green).

Font weight hierarchy — WRONG

Reference: Section titles (bold), sub-section headings (blue), placeholders (light grey uppercase), table headers (medium).

Replit: Many headings/labels are black and heavy; placeholders are not used.

Fix: Restore placeholder labels and the weight hierarchy exactly as in the reference screenshots.