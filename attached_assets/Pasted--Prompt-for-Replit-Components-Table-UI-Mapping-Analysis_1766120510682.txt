🟦 Prompt for Replit — Components Table ↔ UI Mapping (Analysis Only)
I want you to explain how the Components UI sub-module is backed by the database, specifically focusing on the components table and any directly related tables.
This is an analysis and documentation request only.
❌ Do NOT modify code
❌ Do NOT run migrations
❌ Do NOT fix issues yet
🎯 Objective
Help me clearly understand how data shown in the Components UI is sourced, filtered, and structured at the database and API level, so I can verify correctness and debug issues confidently.
🔍 What You Must Explain
1️⃣ Components Table Overview
Explain the components table in detail:
Purpose of the table in the PMS system
Key columns and what each represents in the UI (e.g. component_code, sfi_code, vessel_id, parent_component_id, fec, criticality, etc.)
Which columns are mandatory vs optional
How hierarchy (parent → child) is represented
2️⃣ UI → API → DB Flow (Components Module)
Describe the full flow when a user:
Selects a vessel from the dropdown
Views the left-side component tree
Clicks a specific component to open the right-side detail panel
Include:
Which API endpoints are called (with example URLs)
Which service files handle the request
How queries filter data by:
vessel
component hierarchy
component code / ID
3️⃣ Component Tree Construction Logic
Explain:
How the SFI-based tree is built from DB rows
Whether this is done:
fully in SQL
fully in service logic
or a hybrid approach
How parent–child relationships are resolved
4️⃣ Relationship to Other Sub-Sections in UI
Briefly explain how the selected component links to:
Jobs
Running Hours
Spares
Work Orders
Maintenance History
Focus on which foreign keys or identifiers are used to fetch related data when a component is selected.
5️⃣ Common Failure / Mismatch Points
From your implementation, identify:
What could cause components to:
appear under the wrong vessel
show data not belonging to the selected component
load but not reflect related jobs/spares correctly
Any assumptions in the code that the UI depends on
📄 Output Requirements
Produce a single Markdown file
Suggested name:
COMPONENTS_TABLE_UI_MAPPING.md
Use clear sections, diagrams (text-based if needed), and examples
This is documentation and explanation only
⛔ Rules
Do NOT change code
Do NOT fix bugs
Do NOT introduce new logic
Stop after producing the Markdown document
I will review this and then give explicit instructions for any fixes if needed.