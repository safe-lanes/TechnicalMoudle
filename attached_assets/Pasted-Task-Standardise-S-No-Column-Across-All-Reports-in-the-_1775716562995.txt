Task: Standardise S.No Column Across All Reports in the Reports Module

Problem:
There is a structural inconsistency in the Reports module — 17 out of 26 reports have an S.No column as the first column, but 9 reports do not. This makes the UI look inconsistent and unprofessional. The decision is to add S.No as the first column to all reports that are currently missing it.

Reports currently missing S.No (9 reports to fix):

Due Jobs (7 days) — MaintenanceReports.tsx → currently starts with PRIORITY

Monthly Maintenance Summary — MaintenanceReports.tsx → KPI/summary table, starts with METRIC

Crew Workload Distribution — MaintenanceReports.tsx → starts with RANK

Stores Inventory Status Report — StoresReports.tsx → StoresInventoryStatusReport.tsx → starts with Item Code

Lubricants & Oil Analysis Report — StoresReports.tsx → starts with Item Code

Chemicals Inventory & Expiry Report — StoresReports.tsx → ChemicalsExpiryReport.tsx → starts with Item Code

Change Requests Status & Tracking — ChangeRequestReports.tsx → starts with ID

Work Priority Performance (hidden report) — MaintenanceReports.tsx → starts with Priority

Man-Hours Planned vs Actual (hidden report) — MaintenanceReports.tsx → starts with WO Number

What to do for each:

For all tabular/row-based reports (reports 1, 3, 4, 5, 6, 7, 8, 9):

Add an S.No column as the first column in the UI table header and rows

Value = index + 1 (1-based sequential row number)

Column header label = S.No

Apply consistently to both the UI table AND the PDF/Excel export if one exists

For Monthly Maintenance Summary (report 2 — KPI/summary format with METRIC / VALUE / PERCENTAGE rows):

This is a summary report, NOT a row-based tabular report

Do NOT add S.No to this report — it would be meaningless on a metric summary table

Instead, add a subtle row number or section indicator only if grouping sections (Executive Summary / Priority Breakdown / Department Breakdown) need to be distinguished — otherwise leave as-is

Implementation rules:

The S.No column should be narrow — fixed width of approximately 60px or w-16 in Tailwind

It should be non-sortable (no sort icon)

It should re-number from 1 on every page if pagination is active (i.e., page 2 row 1 = index 51 if 50 rows per page, not restart at 1)

In PDF export, S.No should appear as the first column

In Excel export, S.No should appear in column A

Do NOT change:

The existing column order of all other columns — only prepend S.No

Any data, filtering logic, API calls, or business logic

The 17 reports that already have S.No

Test after implementation:

Open each of the 9 affected reports and confirm S.No appears as the first column

Confirm S.No is 1, 2, 3... sequentially

Confirm no other columns shifted or broke layout

Confirm PDF and Excel exports also show S.No as first column where exports exist