Excellent plan! Your approach is spot-on. Proceed with all 6 steps with these refinements:

✅ Approved Implementation Plan
Step 1: Fix get_components Critical Filter
Check executeTool function in chatbotService.ts:

typescript
case "get_components":
  let components = await storage.getComponents(args.vesselId);
  
  // Ensure critical filter works
  if (args.critical !== undefined) {
    components = components.filter(c => c.critical === args.critical);
  }
  
  // Ensure search works (case-insensitive)
  if (args.search) {
    const searchLower = args.search.toLowerCase();
    components = components.filter(c => 
      c.name?.toLowerCase().includes(searchLower) ||
      c.componentCode?.toLowerCase().includes(searchLower)
    );
  }
  
  return components;
Also add: Limit results to prevent overwhelming responses:

typescript
// Add limit parameter to prevent returning 200+ components
const limit = args.limit || 50;
return components.slice(0, limit);
Step 2: Enhanced System Prompt Formatting
Update the system prompt with these exact formatting instructions:

typescript
## Response Formatting Rules

### For Components/Equipment Lists:
Format each component clearly with key details:
- Component name and code
- Running hours (if relevant)
- Critical status
- Location/department

Example:
🔧 **Main Engine** (ME-001)
- Running Hours: 45,234 RH
- Status: Critical
- Location: Engine Room

### For Work Orders:
Show priority, overdue days, and actionable info:
⚠️ **WO-1234** - ME Piston Inspection
- Component: Main Engine
- Due: 2026-01-10 (15 days overdue)
- Priority: High

### For Spares:
Highlight shortage and urgency:
🔴 **Fuse FCF2-10A**
- ROB: 5 | Min: 10 | Short: -5
- Component: Electrical Panel
- ⚠️ Order immediately

### Critical Rules:
1. NEVER show raw pipe-delimited data or unformatted text
2. Parse tool results into readable markdown
3. Use bullet points and clear hierarchy
4. Include relevant numbers (counts, dates, hours)
5. Always end with 2-3 actionable suggestions
6. Use emojis sparingly: ⚠️ (urgent), 🔧 (equipment), 🔴 (critical), ✅ (ok)

### Summary Format:
When asked for "status" or "summary":
**PMS Status for Vessel 1**
- ⚠️ Overdue: 8 work orders (immediate attention needed)
- 📋 Due: 151 work orders
- ✅ Completed: 6 this period
- 🔴 Low Stock: 215 spares (87 at minimum)

Would you like me to:
• Show overdue work order details
• Navigate to low stock spares page
• Draft a superintendent briefing
Step 3: Enhanced Context Awareness
Current page detection - Add this helper in ChatButton.tsx:

typescript
const getCurrentPageContext = () => {
  if (location.includes('/pms/components')) {
    return {
      page: 'Components',
      hint: 'User is viewing the components list. Prioritize component and equipment queries.'
    };
  }
  if (location.includes('/pms/workorders')) {
    return {
      page: 'Work Orders',
      hint: 'User is viewing work orders. Prioritize maintenance and work order queries.'
    };
  }
  if (location.includes('/pms/spares')) {
    return {
      page: 'Spares Inventory',
      hint: 'User is viewing spares. Prioritize spare parts and stock level queries.'
    };
  }
  if (location.includes('/pms/stores')) {
    return {
      page: 'Stores Inventory',
      hint: 'User is viewing stores. Prioritize consumables and stores queries.'
    };
  }
  if (location.includes('/pms/running-hours')) {
    return {
      page: 'Running Hours',
      hint: 'User is viewing running hours. Prioritize equipment hours and maintenance timing queries.'
    };
  }
  return {
    page: 'PMS Dashboard',
    hint: 'User is on the main dashboard. Provide overview and summary information.'
  };
};
And update the system prompt to include:

text
## Current Context
- Page: {{currentPage}}
- Context Hint: {{pageHint}}

Use this context to:
- Prioritize relevant queries
- Suggest contextual actions
- Offer to navigate to related pages
Step 4: Conversation Intelligence
Add to system prompt:

typescript
## Conversation Memory & Follow-ups

You have access to conversation history. Use it to:

1. **Handle References**:
   - "show me more" → refer to last query's results
   - "the first one" → refer to first item in previous list
   - "details on that" → get details of previously mentioned item
   - "what about [other component]?" → apply same query pattern to new entity

2. **Track Query Context**:
   - If you just listed work orders, remember the list
   - If user asks "show me number 3", use the work order ID from position 3
   - If user asks about a component by name after a list, find its ID

3. **Smart Drill-Down**:
   User: "Show overdue work orders"
   Bot: [lists 8 items]
   User: "details on the Main Engine one"
   Bot: [calls get_work_order_detail for the ME work order from previous list]

4. **Cross-Reference Queries**:
   User: "Show me Main Engine work orders"
   Bot: [shows work orders]
   User: "what spares are low for it?"
   Bot: [calls get_spares_by_component for Main Engine]
Important: Make sure the conversationHistory is being passed correctly in the useChat hook.

Step 5: Maritime NLU Enhancement
Add this section to system prompt:

typescript
## Maritime Terminology & Query Understanding

Understand these maritime terms and patterns:

**Equipment Aliases**:
- "Main Engine" / "ME" / "prime mover" → Main Engine components
- "Generators" / "Gen" / "DG" → Generator components
- "Aux Boiler" / "Boiler" → Auxiliary Boiler
- "Steering Gear" / "SG" → Steering system
- "Emergency Generator" / "E/G" / "EG" → Emergency generator

**Maintenance Terms**:
- "class items" / "survey items" / "class work" → work orders with classRelated = Yes
- "running hours based" / "RH jobs" → maintenanceBasis = "Running Hours"
- "time based" / "calendar based" → maintenanceBasis = "Calendar"
- "overdue" → status = "Overdue"
- "due soon" / "coming up" → status = "Due" within next 7-30 days
- "postponed" → check postponement status

**Spares Terms**:
- "low stock" / "below minimum" → ROB < Min
- "critical spares" → critical = true AND ROB < Min
- "out of stock" / "zero stock" → ROB = 0
- "reorder" / "requisition" → spares needing attention

**Time Ranges**:
- "this week" → next 7 days
- "this month" → next 30 days
- "urgent" / "immediate" → overdue or critical
- "coming up" / "soon" → next 7-14 days

**Priority Questions**:
- "what needs attention" → overdue WOs + critical low stock spares
- "what should I prioritize" → overdue + critical + high priority items
- "status" / "summary" → KPI overview with counts
- "briefing" / "report" → formatted summary for superintendent
Step 6: Dynamic Follow-Up Suggestions
In ChatPanel.tsx, add this logic after messages render:

typescript
// Add this function
const getDynamicFollowUps = (lastMessage: Message): string[] => {
  if (!lastMessage || lastMessage.role !== 'assistant') return [];
  
  const content = lastMessage.content.toLowerCase();
  const toolsUsed = lastMessage.toolsUsed || [];
  
  // For work orders
  if (content.includes('overdue') || content.includes('work order')) {
    return [
      'Show details of the most critical one',
      'Which system has most overdue items?',
      'What spares do I need for these?',
      'Take me to work orders page'
    ];
  }
  
  // For spares
  if (content.includes('spare') || content.includes('low stock')) {
    return [
      'Show only critical items',
      'Group by component',
      'Draft requisition list',
      'Navigate to spares page'
    ];
  }
  
  // For components
  if (content.includes('component') || content.includes('equipment')) {
    return [
      'Show maintenance for this equipment',
      'Check running hours',
      'Related spares status',
      'View in components page'
    ];
  }
  
  // For summaries
  if (content.includes('status') || content.includes('summary')) {
    return [
      'Show overdue details',
      'Low stock breakdown',
      'Draft superintendent briefing',
      'What needs priority?'
    ];
  }
  
  // Default suggestions
  return [
    'Show overdue work orders',
    'Check low stock spares',
    'PMS status summary'
  ];
};

// Render after the last assistant message
{messages.length > 0 && messages[messages.length - 1].role === 'assistant' && (
  <SuggestedPrompts
    prompts={getDynamicFollowUps(messages[messages.length - 1])}
    onSelect={handlePromptClick}
    label="Quick follow-ups"
    disabled={isLoading}
  />
)}
Testing Checklist After Implementation:
Test these queries in order:

✅ "show me critical components" → Should list critical equipment clearly formatted

✅ "can you list the critical equipments" → Same as above, should work now

✅ "show overdue work orders" → Should format with dates, priorities

✅ "low stock spares" → Should show ROB/Min comparison

✅ "PMS status summary" → Should call multiple tools and summarize

✅ "what needs attention this week?" → Multi-tool smart query

✅ "show me Main Engine maintenance" → NLU test for equipment alias

✅ Follow-up test: "show overdue" → then "details on the first one"

✅ Context test: Go to Components page, ask "what's here?"

✅ Deep link test: "take me to low stock spares"

Implementation Order:
Step 1 → Fix get_components (5 min)

Step 2 → Enhanced formatting in system prompt (10 min)

Step 5 → Add NLU to system prompt (5 min)

Step 4 → Add conversation intelligence to system prompt (5 min)

Step 3 → Enhance context detection in ChatButton (10 min)

Step 6 → Add dynamic follow-ups to ChatPanel (15 min)

Total: ~50 minutes

Proceed with implementation now. Test after each step and let me know if any issues arise.