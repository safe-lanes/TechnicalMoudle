Please revise the PMS Reports page again. The left-side tree navigation is now present, which is good, so keep that tree-based structure. However, the right-side panel is still not behaving as required. Right now, when a report is selected, the right pane still shows extra summary UI such as four KPI/stat tiles and a report information row before the actual report content. That is not what is needed. I want the selected report to open directly in report view mode in the right pane, with only two export buttons at the top: PDF and Excel.

This is an adjustment to the current implementation, not a fresh redesign. Do not remove the tree navigation. Do not go back to the old tile-based report layout. Keep the current master-detail structure with the report tree on the left and the report display panel on the right.

The main correction required is this: when the user clicks a report name in the left tree, the system should immediately load that actual report in the right pane. It should not first show a report summary dashboard, a report descriptor card, KPI counters, a statistics strip, or an intermediate report metadata section. Those elements are unnecessary for this screen and should be removed from the selected report display area.

Specifically, remove the extra top content that appears above the actual report preview when a report is selected. This includes the four summary tiles/cards such as Total Reports, High Priority, Daily Reports, Work Orders, or any equivalent summary counters. Also remove the report information row or mini table that shows report name, frequency, priority, estimated time, and action icons in the body of the right pane. Those items belong to the old report browsing design and should not appear in the new direct-view model.

The right pane should be simplified into a clean report viewer area. The desired behavior is:

User clicks a report name in the left tree.

The selected report opens directly in the right pane.

At the top right or top area of that pane, show only two action buttons: Export PDF and Export Excel.

Below that, show the actual report content/preview itself.

Nothing else should sit between the report selection and the report preview.

Do not change the report generation logic. Do not change the underlying report data logic. Do not change API calls, filters, conditions, export functions, or backend behavior. Reuse the same existing report rendering functions and same export actions that already work. This task is only to clean up the UI flow so the selected report goes directly into view mode instead of showing unnecessary wrapper UI.

Think of the right pane as a report viewer, not as a report catalog card. Once a report is clicked, the user should feel like they are already inside that report. The UI should not repeat report metadata in a separate card if the actual report title and content are already visible in the report itself.

Please keep the current filters that belong to the PMS Reports screen, such as vessel, search, and date range, if they are already functioning at the top level of the page. Those can remain in the page header/top toolbar. But inside the report viewing pane, do not add extra summary widgets, extra cards, or duplicate action rows. The report viewer should stay focused.

The empty state is fine if no report is selected, but once a report is selected, the placeholder should disappear completely and the selected report should occupy the right panel directly. There should not be any assistant-like messaging, report chooser text, or generic helper block once a real report has been clicked.

Please preserve the current tree interaction on the left side:

Top-level categories remain expandable and collapsible.

Child reports remain visible under each category.

Clicking a child report highlights it as selected.

The selected report should remain visibly active in the tree.

The tree structure itself should not be replaced.

Also keep Maintenance Planner hidden from the visible report navigation if it is already removed or hidden. Do not reintroduce it.

The final intended UX is very simple:

Left side = report tree.

Right side = actual selected report in direct preview mode.

Only two top actions for the selected report = PDF export and Excel export.

No KPI tiles.

No summary cards.

No report metadata strip.

No duplicate report listing inside the right pane.

No extra actions except the two export buttons.

Please verify the final output against this checklist before considering the work complete:

The left tree remains present and functional.

Clicking a report name directly opens that report in the right pane.

The four tiles/stat cards are removed from the right pane.

The report metadata/info row is removed from the right pane.

Only two export buttons remain for the selected report: PDF and Excel.

The actual report preview/content is shown directly below those buttons.

No backend logic or report logic is changed.

The page remains consistent with the PMS application style.

Important: this is not a request to rebuild the reporting engine. It is only a request to remove leftover summary UI and make the selected report open directly in clean view mode.