I want to restructure the KPI cards at the top of my PMS dashboard into three clear groups (bands) to improve information hierarchy and readability.
Do not change any backend logic or calculations, only reorganize and slightly restyle the UI using the existing KPI values.

Overall goal:

Group related KPIs into 3 visual bands:

Work-order health

Maintenance effectiveness

Inventory health

Make it obvious at a glance what each group represents.

Identify existing KPI cards (do not rename their meaning):

Work-order related KPIs like: Overdue, Due, Pending approval, Completed, Total work orders (or similar).

Maintenance effectiveness KPIs like: Outstanding tasks / percentage, 6‑month trend summary, etc.

Inventory-related KPIs like: Spares inventory, Components count, Stores inventory, Low stock items, etc.
Use the existing cards that are currently at the top of the dashboard or in the overview section.

Create three horizontal “bands” (sections) for these KPIs:
a) Band 1: “Work-order health”

Place all work-order status KPIs here: Overdue, Due, Pending approval, Completed, Total WOs (or whatever variants exist).

Add a small section title at the top left of this band: “Work-order health”.

Inside the band, arrange the cards in a single row (or two rows on smaller screens) with consistent spacing.

b) Band 2: “Maintenance effectiveness”

Place KPIs that reflect the effectiveness or backlog of maintenance (e.g., Outstanding tasks, % completed vs total, 6‑month trend summary card).

Add a small section title: “Maintenance effectiveness”.

Keep these cards visually grouped and separated from Band 1 and Band 3 with spacing or a subtle divider.

c) Band 3: “Inventory health”

Place inventory-related KPIs: Spares inventory, Components, Stores inventory, Low stock counts, etc.

Add a small section title: “Inventory health”.

Arrange these cards in a neat grid/row inside this band.

Visual and layout rules for all three bands:

Each band should feel like a section:

Title text (slightly smaller than the main page title, consistent style across all three).

Under the title, the related KPI cards in a row/grid.

Use consistent spacing between bands (vertical margin) and between cards (horizontal gap).

Do NOT change the numbers or labels on each card, only how they are grouped and laid out.

Use the same card component style that already exists (same border radius, background, shadows, fonts).

Responsiveness:

On desktop / wide screens: aim for cards in each band to appear on 1 row if possible (wrapping to 2 rows only if necessary).

On tablet/small screens: allow cards to wrap to 2+ rows but keep their band title visible above them.

Ensure bands stack vertically in the order: Work-order health, then Maintenance effectiveness, then Inventory health.

Clear ordering inside each band:

For Work-order health, sort cards in a logical order, for example: Overdue → Due → Pending approval → Completed → Total.

For Inventory health, group similar things next to each other (e.g., Components next to Spares, then Stores, then Low stock).

Keep label text exactly as it is now unless spacing requires very minor adjustments.

Do not:

Do not remove any existing KPI card.

Do not introduce new dummy KPIs.

Do not alter the underlying calculation or data source for any KPI.

At the end, briefly describe:

Which KPI cards you placed into each of the 3 bands.

Where in the component/layout file you defined these bands so I can easily adjust the grouping later if needed.