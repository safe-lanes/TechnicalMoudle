Replit Prompt: Noon Report Module (Isolated Section)
________________________________________
TASK: Add a fully isolated "Noon Report & Fuel Management" module to the existing PMS.
________________________________________
⚠️ PRIME DIRECTIVE — READ BEFORE WRITING ANY CODE
This is an existing, live PMS application. You must NOT:
•	Modify any existing files, routes, components, models, or database tables
•	Import or reference anything from the new module inside existing code
•	Rename, refactor, or restructure anything already in the project
•	Touch package.json, app.js / server.js, index.js, or any existing route/model files except for two specific, clearly marked single-line additions described below
The new module must be 100% self-contained. All its routes, components, models, logic, and styles live inside its own dedicated folder. The only connection to the host application is:
1.	One import line in the main server/router file to mount the module's router
2.	One sidebar nav link entry (clearly commented) in the existing navigation component
Both additions must be wrapped in a comment block so they can be found and removed instantly:
js
// ====== NOON REPORT MODULE — START (remove these 2 lines to disable) ======
// ... single import/mount line here ...
// ====== NOON REPORT MODULE — END ======
To completely hide and disable the entire module, a developer must only toggle one flag:
js
// In /noon-report-module/config.js
const NOON_MODULE_ENABLED = true; // Set to false to hide from nav and disable all routes
When NOON_MODULE_ENABLED = false:
•	The sidebar nav link disappears
•	All /noon-report/* routes return 404
•	Nothing else in the app is affected
________________________________________
FOLDER STRUCTURE
Create the following folder structure. Do not place any file outside this folder:
text
/noon-report-module/
│
├── config.js                   ← Master enable/disable flag lives here
├── index.js                    ← Module entry point; exports the Express router
│
├── routes/
│   ├── noonEntry.routes.js
│   ├── fuelDashboard.routes.js
│   ├── bunkerManagement.routes.js
│   ├── alerts.routes.js
│   ├── reports.routes.js
│   └── fleetOverview.routes.js
│
├── controllers/
│   ├── noonEntry.controller.js
│   ├── fuelDashboard.controller.js
│   ├── bunkerManagement.controller.js
│   ├── alerts.controller.js
│   ├── reports.controller.js
│   └── fleetOverview.controller.js
│
├── models/
│   ├── NoonReport.model.js
│   ├── FuelROB.model.js
│   ├── BunkerRecord.model.js
│   ├── NoonAlert.model.js
│   └── VoyageLeg.model.js
│
├── views/ (or components/ if React/frontend framework)
│   ├── NoonEntryForm/
│   │   ├── TabNavigation.jsx (or .ejs/.html per your stack)
│   │   ├── TabNavigation.jsx
│   │   ├── Tab1_Navigation.jsx
│   │   ├── Tab2_Weather.jsx
│   │   ├── Tab3_Machinery.jsx
│   │   ├── Tab4_Emissions.jsx
│   │   └── Tab5_Cargo.jsx
│   ├── FuelDashboard/
│   │   ├── ROBStatusCards.jsx
│   │   ├── ConsumptionChart.jsx
│   │   ├── CIIGauge.jsx
│   │   ├── BunkerPlanningCard.jsx
│   │   └── SpeedConsumptionChart.jsx
│   ├── BunkerManagement/
│   │   ├── BunkerEntryForm.jsx
│   │   └── BunkerCostSummary.jsx
│   ├── Alerts/
│   │   └── AlertsPanel.jsx
│   ├── Reports/
│   │   └── ReportsExport.jsx
│   └── FleetOverview/
│       └── FleetDashboard.jsx
│
├── services/
│   ├── calculationEngine.js    ← All auto-calculations (ROB, CII, EEOI, averages)
│   ├── alertEngine.js          ← Alert generation and severity logic
│   └── reportGenerator.js      ← PDF/Excel generation
│
├── middleware/
│   └── noonAuth.middleware.js  ← Role-based access for this module only
│
└── utils/
    ├── fuelConversionFactors.js ← IMO CO2 factors, safety margins, thresholds
    └── validators.js            ← All form validation logic
________________________________________
MODULE MOUNTING (only changes to existing files)
In the main server file (e.g., app.js or server.js), add only these lines — nothing else:
js
// ====== NOON REPORT MODULE — START (remove these 2 lines to disable) ======
const noonReportModule = require('./noon-report-module/index');
app.use('/noon-report', noonReportModule);
// ====== NOON REPORT MODULE — END ======
In the existing sidebar navigation component, add only this block — nothing else:
jsx
{/* ====== NOON REPORT MODULE NAV LINK — START (remove to disable) ====== */}
{NOON_MODULE_ENABLED && (
  <SidebarItem
    icon={<FuelIcon />}
    label="Noon Report"
    path="/noon-report"
    subItems={[
      { label: "Daily Entry", path: "/noon-report/entry" },
      { label: "Report History", path: "/noon-report/history" },
      { label: "Fuel Dashboard", path: "/noon-report/fuel-dashboard" },
      { label: "Bunker Management", path: "/noon-report/bunker" },
      { label: "Reports & Export", path: "/noon-report/reports" },
      { label: "Fleet Overview", path: "/noon-report/fleet" }, // office role only
    ]}
  />
)}
{/* ====== NOON REPORT MODULE NAV LINK — END ====== */}
(Use the same SidebarItem / nav component pattern that already exists in this project. Do not create a new one.)
________________________________________
DATABASE ISOLATION
•	All new database models/tables must have the prefix nr_ (e.g., nr_noon_reports, nr_fuel_rob, nr_bunker_records, nr_alerts, nr_voyage_legs)
•	This prefix ensures zero collision with any existing tables
•	No existing model/table should be modified, joined, or referenced from within this module
•	If the module needs vessel names or user data, it should read-only query from existing tables using a thin adapter in /noon-report-module/utils/existingDataAdapter.js — this adapter is the only permitted read bridge and must contain no writes
________________________________________
UI/UX RULE — STRICTLY FOLLOW
Do not create a new design system. Inspect the existing PMS pages and replicate:
•	The exact same color palette, font sizes, and spacing
•	The same card/panel component style
•	The same table style (headers, row striping, hover states)
•	The same button styles (primary, secondary, danger)
•	The same badge/status pill styles (colors for Active, Warning, Critical, Resolved)
•	The same modal/dialog pattern
•	The same form input, label, and validation error style
•	The same sidebar sub-item indentation pattern
Every page of the new module must be indistinguishable in visual style from existing PMS pages.
________________________________________
WHAT TO BUILD (Summary of Features)
Build the following features inside /noon-report-module/ — full spec is detailed separately but at a high level:
1.	Daily Noon Entry Form — 5-tab form (Navigation, Weather, Machinery & Fuel, Emissions, Cargo/Remarks) with inline validation, draft auto-save, and submission lock
2.	Auto-Calculation Engine — ROB tracking per fuel type, rolling 3/7-day averages, endurance in days and NM, CII/AER, EEOI, bunker planning figures
3.	Fuel Dashboard — ROB status cards, consumption trend chart, CII gauge, bunker planning card, speed vs. consumption scatter chart
4.	Alerts Panel — Auto-triggered alerts for high consumption, low ROB, AE hour spikes, CII band drops — with severity badges and acknowledgement workflow
5.	Bunker Management — BDN-level bunkering records, auto ROB update on bunkering, cost tracking per voyage
6.	Reports & Export — Auto-generated Noon Report PDF, Monthly Fuel Excel, Voyage Performance Report, CII/EEOI Compliance Report — all with company header/footer
7.	Fleet Overview Dashboard — Multi-vessel table (office/admin only) with ROB status, endurance, CII rating, and active alerts per vessel
________________________________________
ROLES & PERMISSIONS (module-internal only)
Handle role checks inside /noon-report-module/middleware/noonAuth.middleware.js using session/JWT already present in the existing app. Do not create a new auth system.
Role	Access
Master / Officer	Daily Entry, Report History, Fuel Dashboard (own vessel)
Chief Engineer	Machinery & Fuel tabs, Fuel Dashboard, Bunker Management
Shore Office	All read + Alerts acknowledgement + Fleet Overview + Export
Admin	Full access including config (thresholds, safety margins)
________________________________________
DEVELOPMENT PHASE ORDER
Build in this order to keep things testable at each stage:
1.	Folder scaffold + config.js enable flag + module mounting (verify nav link appears/disappears)
2.	Database models (nr_ prefix tables)
3.	Daily Noon Entry Form (all 5 tabs) + validation + draft save + submission
4.	Calculation Engine (auto-calculations on submission)
5.	Fuel Dashboard (charts + ROB cards)
6.	Alerts Engine + Alerts Panel
7.	Bunker Management
8.	Reports & Export (PDF/Excel generation)
9.	Fleet Overview Dashboard
Do not proceed to the next phase until the current phase works without errors.
________________________________________
FINAL CHECKLIST BEFORE WRITING ANY CODE
•	Have you confirmed you will NOT touch any existing file except the two marked additions?
•	Have you confirmed all new files go inside /noon-report-module/?
•	Have you confirmed all new DB tables use the nr_ prefix?
•	Have you confirmed the NOON_MODULE_ENABLED = false flag completely hides and disables the module?
•	Have you confirmed the UI will match the existing PMS theme exactly?
If all five are confirmed, begin with Phase 1
