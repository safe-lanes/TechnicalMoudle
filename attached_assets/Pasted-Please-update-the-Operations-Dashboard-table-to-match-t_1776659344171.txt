Please update the Operations Dashboard table to match the Work Orders module table design and behavior shown in the reference, and do not change anything else outside this scope. The current Operations Dashboard table is missing the Assigned to column, and its table styling/filter behavior does not match the Work Orders module layout shown in the screenshots and current PMS Work Orders page.

Scope
Implement AG Grid in the Operations Dashboard table only, using the same table structure, filter row behavior, visual styling, and color scheme as the Work Orders (W.O) module table. Also add the missing Assigned to column in the Operations Dashboard table. Do not modify cards, KPIs, tabs, toggles, data logic, navigation, API behavior, or any other module outside this exact table enhancement scope.

Required changes
Replace the current Operations Dashboard table with AG Grid, aligned visually and functionally with the Work Orders module table.

Match the header styling from Work Orders:

blue header background,

white column titles,

same header height/alignment,

same clean bordered grid look,

same spacing/padding and compact professional PMS styling.

Add column-level filters in the header row exactly in the same spirit as the Work Orders module, with filter icons / filter capability available per column.

Add the missing Assigned to column in the Operations Dashboard table in the correct position consistent with Work Orders, ideally after Job Title and before date/status-related columns unless the existing dashboard data structure requires the nearest equivalent placement.

Preserve the Operations Dashboard data context, but present it in the same AG Grid format as the Work Orders table.

Operations Dashboard table requirements
Please configure the Operations Dashboard table columns to include, wherever data is available:

Component

Work Order No

Job Title

Assigned to ← this must be added

Due Date

Status

Criticality

Actions

If any existing dashboard column names differ slightly in code, map them carefully without changing underlying business logic or data contracts unnecessarily.

AG Grid behavior
Implement AG Grid with behavior matching the Work Orders module as closely as possible:

sortable columns,

per-column filtering,

proper header filter UI,

clean row rendering,

responsive horizontal handling if needed,

consistent pagination behavior with the existing dashboard expectations,

no visual regressions,

no change to current row actions except to keep them working inside the AG Grid table.

If the Work Orders module already has a reusable AG Grid configuration, shared column styling, theme wrapper, header class, filter config, badge renderer, or action-cell renderer, reuse the same implementation/patterns instead of rebuilding a separate styling approach. Keep code centralized and consistent with the Work Orders module.

Color and UI consistency
Match the Work Orders module table color scheme exactly or as close as possible using existing project styles:

blue header bar,

white header text,

same filter/header icon treatment,

same row borders and table density,

same badge treatment for status,

same clean PMS table appearance,

same visual hierarchy for actions column.

For Status and Criticality, keep the same badge/chip style logic already used in the dashboard unless the Work Orders module already has a reusable renderer that should be shared. Prefer reuse over duplication.

Important implementation instructions
Do not change anything outside the Operations Dashboard table.

Do not redesign the dashboard cards, KPI tiles, toggle, tabs, page layout, spacing outside the table block, or any other module.

Do not change backend logic unless absolutely required to supply the missing Assigned to field.

If Assigned to data is already available in the source, bind and display it directly.

If Assigned to is not currently returned in the dataset, extend the minimum required query / mapping only to populate this one field, without altering anything else.

Keep all existing table actions functional.

Maintain current permissions, role visibility, and page behavior.

Avoid unnecessary refactoring. Make the smallest clean change that brings the Operations Dashboard table in line with the Work Orders module.

Expected result
After the change, the Operations Dashboard table should visually and functionally look like the Work Orders module table, including AG Grid-based column filters and the missing Assigned to column, while everything else on the page and in the system remains unchanged.