I am building a Planned Maintenance System (PMS) dashboard web app (for vessels) in Replit, and I want to add an integrated AI chatbot to it.
​

The PMS dashboard currently has:
​

Top navigation: Technical, Dashboard, Cert. & Surveys, Defects, PMS, Admin.

Role selector: e.g., “Sail Admin”.

Vessel selector: e.g., “Vessel 1”.

KPI cards: Overdue Work Orders (8), Due Work Orders (151), Pending Approval (0), Completed (6), Total Work Orders (189), each with “View …” actions.

Charts:

Work Order Status Distribution (Overdue / Due / Completed).

Outstanding Tasks as % of Monthly Planned Maintenance (e.g., 138 of 142 tasks outstanding, 97%).

Spares Stock Status (OK / At Min / Low).

Stores Inventory Breakdown (Stores / Lubes / Chemicals / Others).

Lists:

Overdue Work Orders with IDs, titles, and status (e.g., ME Piston And Rings, Inspection – 500H; Provision Ref. Compressors, Inspection – 15000H).

Low Stock Spares with item name, figure/part no, ROB and Min, including a “Critical” flag for some (e.g., Fuse FCF2-10A, ROB 5 / Min 10).

Summary tiles: Components (270 active), Spares Inventory (1113, 302 low stock), Stores Inventory (40, 38 low stock).

Quick Actions: Work Orders, Components, Spares, Reports.

Your job is to act as a senior full‑stack/LLM architect and:

Design an AI chatbot for this PMS dashboard.

Ask me all the questions you need (in batches) to fully understand:

Business use‑cases and workflows.

Data models and relationships (work orders, components, spares, stores, running hours, vessels, users/roles).

Current tech stack in Replit (front‑end framework, back‑end, database, auth).

What APIs or server routes already exist, and what can be added.

Security and role‑based access control requirements.

Deployment and scaling constraints.

Then propose:

A high‑level architecture for the chatbot (front‑end UI + back‑end + LLM + tools/functions).

Exact APIs / functions (“tools”) that the chatbot should be able to call (with request/response shapes).

Prompt structure and context strategy (how it will use dashboard data, filters, role, vessel, etc.).

Example conversations and test cases.

A step‑by‑step implementation plan for this specific Replit project (file‑by‑file where possible).

Start by asking me these questions (you can rephrase/extend them as you like):

Primary use‑cases

What are the main tasks I want the chatbot to help with? For example:

Answering questions about work orders (overdue, due, completed, by system/component/date/vessel).

Summarizing maintenance status for a vessel, date range, or equipment group.

Suggesting maintenance priorities based on overdue / critical / running hours.

Finding low‑stock or critical spares and suggesting what to reorder.

Navigating/opening specific dashboard views or filters.

Drafting reports, emails, or briefings based on dashboard data.

Anything else I have in mind.

Target users & roles

What user roles exist in my system (e.g., Sail Admin, vessel crew, office technical team, superintendent, admin)?

For each role, what should the chatbot be allowed to see and do? (Read‑only vs. can perform actions.)

Do some roles see only one vessel, and others see a fleet?

Read vs. write capabilities

Should the chatbot only read data (answer questions, summarize, highlight issues)?

Or also perform actions, such as:

Create/update work orders.

Change work order status (e.g., acknowledge overdue, mark completed).

Create or suggest spares requisitions.

Apply dashboard filters or deep‑link the user to specific screens.

For any write actions, what approvals or confirmations are required?