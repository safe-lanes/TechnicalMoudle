Issues #1-5 are fixed. Now let's fix Issues #6 and #7 together - both relate to Unplanned Work Orders:

Issue #6: When adding an unplanned Work Order, the system should list all active components in the filter/selector so user can select the component.

Issue #7: An Unplanned Work Order is neither getting saved nor submitted, even after entering all required data.

Current behavior:
1. Try to create an unplanned work order
2. Component selector may not show all active components
3. Even after filling all fields, save/submit fails

Please investigate and fix:

1. Find the Unplanned Work Order form - likely in:
   - client/src/components/UnplannedWorkOrderForm.tsx
   - Or client/src/components/NewWorkOrderForm.tsx
   - Or within client/src/pages/pms/WorkOrders.tsx

2. For Issue #6 (Component selector):
   - Find where components are loaded for the selector
   - Should fetch from `/api/components/${vesselId}`
   - Filter should show only isActive === true components
   - Add logging: console.log('[UNPLANNED_WO] Components loaded:', components?.length);

3. For Issue #7 (Save not working):
   - Find the save/submit handler for unplanned WO
   - Add logging of the payload:
   console.log('[UNPLANNED_WO] Submitting:', JSON.stringify(formData, null, 2));
   
   - Check what fields are required for unplanned WO
   - Common missing fields: componentId, vesselId, workOrderType, status

4. In server/routes.ts, find the unplanned WO creation endpoint:
   - Add logging:
   console.log('[API_UNPLANNED_WO] Received:', req.body);
   - Check validation requirements
   - Log any validation failures

5. Unplanned WOs might need different required fields than planned WOs:
   - No jobId required (it's unplanned)
   - But componentId is still required
   - Status should default to 'pending' or 'open'

Test by:
1. Navigate to Work Orders page
2. Click "Add Unplanned Work Order" or similar
3. Check if component dropdown shows all active components
4. Fill all fields and submit
5. Check server logs for any errors
6. WO should save successfully