Your previous response was cut off before completing. Please answer the following, picking up where you stopped. Do not write any code — answers only.

Fix Required — Alert Badge Location:
The alert badge must NOT be placed on the "Fuel Dashboard" sidebar icon. Add a dedicated "Alerts" item to the sidebar between "Fuel Dashboard" and "Bunker Mgmt" — the full updated sidebar order is:

Label	URL Path	Icon
Daily Entry	/noon-report/entry	FileEdit
Report History	/noon-report/history	History
Fuel Dashboard	/noon-report/fuel-dashboard	Gauge
Alerts	/noon-report/alerts	Bell
Bunker Mgmt	/noon-report/bunker	Fuel
Reports & Export	/noon-report/reports	FileDown
Fleet Overview	/noon-report/fleet	Ship
The unacknowledged alert count badge (red pill) sits on the Bell / Alerts sidebar item. Confirm this change.

Fix Required — Report History Columns:
Remove "Position" and "Distance (NM)" from the Report History table. These are not useful as list columns. Replace with:
Date | Voyage No. | Route (Port From → Port To) | Status | Submitted By | Actions
Confirm this change.

Complete Section 3 — UI Components:

List every shadcn/UI component you will use across the module — e.g. <Card>, <Tabs>, <Badge>, <Dialog>, <AlertDialog>, <Select>, <Input>, <RadioGroup>, <Table>, etc.

Confirm the exact chart library being used for the Fuel Dashboard charts — is it the same library already used in the existing PMS? Name it specifically.

Confirm whether AG Grid is used for Fleet Overview — and if yes, confirm it uses the same columnDefs / rowData pattern as DefectsLogWithTabs.tsx.

For the CII Gauge — is this a custom SVG component, a chart library gauge, or a simple color-banded progress bar?

Complete Section 4 — State & Behaviour:

When NOON_MODULE_ENABLED = false, list every UI element that disappears — nav tab, sidebar items, page routes, and API routes.

Draft auto-save — is there a visible "Saved as draft at HH:MM" timestamp shown to the user? Where on the form does it appear?

After submission and lock — confirm fields are disabled (not hidden), the Submit button is hidden, and the form is still readable.

Does the alert badge counter update in real time (websocket), on a polling interval, or only on page load/navigation?

Complete Section 5 — ASCII Wireframes:

Draw ASCII wireframe layouts for:

Fuel Dashboard page

Fleet Overview page

Daily Noon Entry Form (showing tab bar + one tab's form fields + sticky footer)

Use this format:

text
┌─────────────────────────────────────────────────┐
│  Top Menu Bar                                   │
├──────────┬──────────────────────────────────────┤
│ Sidebar  │  Content Area                        │
└──────────┴──────────────────────────────────────┘
Again — no code. Answers and wireframes only. All answers will be reviewed before any code is written.

