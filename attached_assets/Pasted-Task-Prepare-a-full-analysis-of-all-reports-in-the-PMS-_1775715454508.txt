Task: Prepare a full analysis of all reports in the PMS Reports module before we standardize report structure

Background
I have reviewed the first report, Due Jobs (7 days), and I can already see inconsistency in table design and data logic. For example, this report currently starts with PRIORITY as the first column, and it does not have an S.No. column. Also, the report is showing values like Priority = Low / Medium even though this priority was not intentionally configured by me and I need to understand exactly where that value is coming from and how it is being derived. The current visible headers for this report are: PRIORITY, STATUS, WO NUMBER, TITLE, COMPONENT, DUE DATE, DAYS LEFT, ASSIGNED TO.

Objective
Before making any UI or structure changes, I want you to perform a complete report-by-report analysis of the entire Reports module and give me a structured table for every report.

Important instruction
Do not change any code yet. Do not modify report layout, columns, filters, styling, or logic yet.
This task is only to analyze and document the current implementation in detail so that we can later make all reports uniform.

What I want you to analyze
For every single report in the Reports module, prepare a structured analysis covering:

Report category

Example: Maintenance & Work Orders, Running Hours & Condition, Inventory - Spares, Inventory - Stores/Lubes/Chemicals, IHM, Modify PMS, Critical Equipment, LSA/FFA Equipment.

Report name

Example: Due Jobs (7 days), Overdue Jobs, Completed Jobs Register, Monthly Maintenance Summary, Critical Equipment Status, Unplanned/Breakdown Jobs, Job Postponement Log, Crew Workload Distribution, etc.

Current table headers / visible columns

List the headers exactly as currently rendered in UI.

Mention column order exactly as implemented.

Example from the current Due Jobs report: PRIORITY, STATUS, WO NUMBER, TITLE, COMPONENT, DUE DATE, DAYS LEFT, ASSIGNED TO.

Column source mapping
For each column, identify:

the frontend label,

the actual backend/database field or computed source,

the file/component where it is defined,

whether the value is:

directly fetched,

transformed,

derived,

hardcoded,

defaulted,

or fallback-generated.

Value calculation / derivation logic
For each report, explain:

how each column value is calculated,

where the calculation happens,

whether it comes from API response, frontend memo/filter, or backend service,

whether any business rule is applied.

Specific example:

For PRIORITY in Due Jobs, identify exactly:

from which field it comes,

whether it is stored in DB or inferred,

whether there is a default fallback,

and why Low/Medium values are appearing.

Missing structural consistency
For each report, mention:

whether S.No. exists or not,

whether the first column is appropriate,

whether the report starts with a logical identifying column,

whether the column order is consistent with other reports.

Filters currently affecting the report
For each report, document:

whether Vessel filter affects it,

whether Period filter affects it,

whether Search affects it,

whether internal report-specific filters exist,

whether PDF/Excel export uses the same filtered dataset.

Data path
For each report, trace:

frontend component,

API endpoint,

controller/service,

query/data transformation,

final rendered table.

Issues / observations
For each report, identify:

columns that look unnecessary,

columns that look derived incorrectly,

fields that are unclear,

reports with inconsistent headers,

reports where values may be coming from fallback logic instead of actual configured data.

Required output format
I want the output in table format, not loose paragraphs.

Table 1: Report Inventory
Create one master table with these columns:

| Category | Report Name | Component/File | Has Table? | Has S.No.? | Current First Column | Period Applied? | Export Matches UI? |

Table 2: Per-Report Column Analysis
For each report, create a separate table with these columns:

| Column Order | UI Header | Source Field | Source File / Function | Value Type (Direct/Derived/Hardcoded/Fallback) | How Value Is Calculated | Notes / Concerns |

Table 3: Report Logic Summary
For each report, create another summary table with:

| Report Name | Data Source API | Backend Service/Controller | Frontend Filtering Logic | Current Filters Applied | Known Inconsistency |

Specific focus
Pay special attention to:

Due Jobs (7 days) first, because it already shows structural inconsistency:

no S.No.

first column is PRIORITY

unclear source of priority values

need full trace of how each visible value is populated.

Deliverables
A complete analysis of all reports in the Reports module.

Proper tables for each report.

Exact source mapping for every header/value.

A clear note on which reports are structurally inconsistent.

No code changes yet — analysis only.

Strict instruction
Do not implement any fixes yet. Do not standardize anything yet. Do not add/remove columns yet.
First produce the analysis only. After that, we will decide how to make all reports uniform.