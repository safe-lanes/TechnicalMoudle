✅ SPARES MODULE – COMPLETE CLARIFICATION QUESTION BANK

These are grouped by sections and screens, exactly like your PMS structure.

Answer one-by-one, short answers are enough.

SECTION A — GENERAL MODULE LOGIC
A1. Which spares should appear in Spares Inventory list?

✓ All spares linked to current vessel- Yes
But clarify further: All spares linked to components

Should list include:

Only spares linked to components? Yes, no spare can be without the component. 

Also standalone spares not linked to any component? No

Should spares with ROB = 0 still appear? yes the must still appear. 

A2. Critical Spares

How is “Critical” decided? they will me marked as critical when uploaded using an excel sheet. and also in app the will be marked as critical. 

From Excel upload? yes

Or editable by office? office can also decide

Or flag comes from each linked job? not necessarily. critical spares are not fully linked to critical components. 

A3. “Min Qty” Logic

When ROB < Min Qty:

Should status automatically become “LOW”? yes

Should alert be generated? (Yes/No) no

Should WO be blocked if spare is 0? (Yes/No) no, it must give warning message. 

A4. Linking Spare to Component

When adding a spare:

You have:

Part Code

Part Name

Linked Component (dropdown search)

Qty / Min Qty

Critical

Location

Question:

Can one spare be linked to multiple components? yes if the component is of same type. every spare will have a unique part code eg Vessel Code - 0000Running Numbers

Or strictly one spare → one component? similar equipments like same engines will have same spares. so linking will be using the part code and component code. eg 3 auxiliary engines will not have different spares, they will share same inventory. 

A5. Linking Spare to Multiple Locations

Your excel template shows:

Location A

Location B

Location B ROB

Location A ROB

etc.

Question:

Should one spare item have:

Single location (your UI seems single location only)

Multiple locations (like template suggests)

UI simplified to "Location A / B" but backend still supports multi-location? - the spares may be stores in more than one location that is why i have given location A and B so that user can atleast have 2 places entered where the spares are stored. 

SECTION B — INVENTORY SCREEN (MAIN)
B1. Left Side Component Tree

Is this just for filtering, or does selecting a component:

Filter spares linked to that component only? yes

Show spares in the right table?
yes
Allow quick navigation to component profile?
yes
B2. Table Columns

Current columns:

Part Code

Part Name

Component

Critical

ROB

Min

Stock Status (OK / Low / Critical)

Location

Questions:

Should “Stock Status” be colour-coded only? yes always color coded. 

Do you want reorder level, supplier name, last received date, etc.?no

Should type (Consumable / Rotable / Spare) be included?
no
B3. Sorting & Filters

Filter row has:

Criticality dropdown

Stock dropdown

Component dropdown

Apply / Clear

Questions:

Should filters be sticky per user session? no

Should search box search across:

Part Code

Part Name

Component

Location

Serial No (if any)

Maker

Drawing No

Position No

Should filters auto-apply or require “Apply” button? auto apply smart filter to be used which should be able to search using anything like name,code

SECTION C — ADD SPARE (MODAL)
C1. Part Code Auto-Generation

Your template shows:

SP-ME-001

Should Part Code:

Be auto-generated (SP-XXXX format)?

Or manually entered?

Or auto with ability to override?

C2. Maker / Drawing No / Position No

UI currently does NOT show these fields, but Excel template DOES.

Do you want:

Simplified UI (only critical fields)?

Full detailed UI with all fields? yes full detailed Ui

Different UI for ship vs office?
no will be same. for office they can use the vessel selector dropdown. at ship's view only that specific spares will be visible, no vessel selector dropdown, in any of the sub-module. 
C3. Multiple Spare Entry Rows

Your modal allows:

+ Add Spare multiple rows. nono, it was just demo. only one blank field row  will appear. user can add more blank rows by clicking + icon

Question:

Should “Save” save ALL rows in one transaction? yes

C4. Validation Rules

For each row:

Mandatory fields? question not clear

Duplicate part code allowed? (Probably no) no

Linked Component required? yes always

Min Qty < Qty? not necessary

Location required? not mandatory

SECTION D — INVENTORY TRANSACTIONS

There are TWO types:

Consumption (during WO)

Receipt (during stores receiving)

For each, clarify:

D1. When WO consumes spares

During Work Order Part B (major maintenance):

Consumed qty reduces ROB

Should consumption auto-log a history entry? yes

Should WO form allow adding new spare manually? (you answered Yes earlier) yes

D2. On manual consumption (Inventory screen)

Is there a way to manually consume spares without WO? yes there is + - icons under the action column and also can be done under bulk update. 

Should this exist? yes

Or consumption only via WO? no

D3. Receiving Spares

Your “Bulk Update Spares” modal allows:

Received Date

Received Place

Comments

Received Qty

Questions:

Should Received Qty increase ROB? obviously

Should New ROB be auto-calculated? obviously

Should we store supplier name? yes if given 

Should we log “Inspection result”? (Pass/Fail)no

SECTION E — HISTORY SCREEN

Your screenshot shows:

Date

Part Name

Type (Consumed / Received)

Qty

Reference (WO no.)

Comment

Questions:

E1. Should history include:

User who performed action? yes

Stock location? no

Previous ROB → New ROB difference? yes

E2. Should history be immutable? yes
E3. Should ship edit history? (Probably no) no
SECTION F — BULK UPDATE SPARES

Your modal shows:

Received Date

Received Place

Comments

Consumed

Received

New ROB

Questions:

F1. Should negative consumption be allowed? (NO?) no
F2. Should consumption & receipt be allowed in same row? no
F3. Should blank rows = no update? yes
F4. Should Bulk Update also allow editing “Location”? yes
SECTION G — EXCEL UPLOAD LOGIC

You provided the master Excel template.

Clarify:

G1. Upload applies at:

Vessel level ONLY? vessel level

Or Fleet-wide? no the codes will be share though but spares will be uploaded to a particular vessel as per vessel code

G2. What happens if Part Code already exists?

Update existing? increase rob after confirmation pop-up showing the clash

Reject? no

Create new version?no

G3. Mandatory columns?

From the yellow fields, which are truly required? all are required only

G4. How many locations do you want to support in UI?

Template allows:

Location A ROB

Location B ROB

Location C ROB

But UI only supports one. the ui will have a expandable pane showing location A and location B. 

SECTION H — PERMISSIONS
H1. Who can:

Add Spare - vessel users via "change-pms" module, we can add directly as we are developer

Edit Spare - codes only we can change. 

Delete Spare - ship after office approval

Change Critical flag - ship after office approval


Change Min Qty-  ship after office approval


Change ROB - all users

Bulk Update - all users

Upload Excel - client's office and us

Export - all users

See History - all users

Your answer will define role-based access. 

SECTION I — LINKAGE WITH OTHER MODULES
I1. Components Module

Should clicking component in left tree:

Filter spares linked to that component - yes

Highlight those rows

Scroll to them? open them in right pane

Open component profile? no

I2. Work Orders Module

When WO consumes spares:

Should New ROB immediately reflect in Inventory table? yes

Should spares unavailable (ROB < required) block WO completion? no must show warning

I3. Running Hours Module

No direct linkage unless spares consumption affects maintenance planning. Confirm: No link.
ya no link to running hours
SECTION J — ADVANCED LOGIC
J1. Expired Spares

Do you want expiry date tracking for: no

Filters

Alerts

Blocking consumption

J2. IHM (Inventory of Hazardous Materials)

If spare has IHM flag “Yes”:

Should separate UI or filter appear? yes

Should disposal instructions be included? yes if available

Should location tracking be separate? yes definitely

J3. Rotables

Should system support spares that: no

Have serial numbers

Can be repaired and returned

Have life cycle

Need tracking per part (not just qty)?

J4. Attachment Fields

Do you need upload of:
can do this , this needs to be developed.
Maker Certificates

Test Reports

Store Receipt files