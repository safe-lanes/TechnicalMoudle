STEP 6 — RETURN skippedCyclesJustification IN API RESPONSES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: server/modules/work-orders/services/workOrderContextService.ts

The GET work order endpoint returns the full work order context to 
the frontend. Ensure the following fields are included in the response:

  - missedCycles (already added in Layer 1)
  - originalDueDate (already added in Layer 2)
  - skippedCyclesJustification  ← ADD THIS (new in Layer 4B)

Add it to the work order context mapping:
  skippedCyclesJustification: workOrder.skippedCyclesJustification || null

This is needed so the frontend can:
1. Show the justification field on Pending Approval WOs 
   (to know if missed_cycles >= 1)
2. Show the stored justification text on Completed WOs (read-only)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 7 — DO NOT CHANGE ANYTHING ELSE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Do NOT change Layer 1, 2, or 3 logic
- Do NOT change how missed_cycles is calculated
- Do NOT change the existing approverRemarks field — keep it 
  exactly as it is. The new skippedCyclesJustification is a 
  SEPARATE field, shown separately, stored separately
- Do NOT block the "Reject" approval action under any circumstances
- Do NOT apply this to Running Hours based jobs (the check is 
  only on missed_cycles which is already 0 for RH jobs)
- Do NOT change any other pages or features

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WORKED EXAMPLE — VERIFY YOUR IMPLEMENTATION AGAINST THIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scenario:
  Job: FO Purifier Inspection - 1W (Weekly, Calendar-based)
  Scheduled due date: 01-Jan-2026
  Completion date entered by crew: 22-Jan-2026
  missed_cycles = 3 (3 weekly cycles skipped)
  WO Status: Pending Approval

WHAT CE SEES when opening this WO for approval:

  [At bottom of page, in approval panel]

  ┌──────────────────────────────────────────────────────┐
  │ 🚨 MANDATORY ACKNOWLEDGEMENT REQUIRED                │
  │                                                      │
  │  This work order has 3 skipped maintenance cycles.   │
  │  The scheduled due date was 01-Jan-2026.             │
  │  The work was completed on 22-Jan-2026.              │
  │                                                      │
  │  As Chief Engineer, you must provide a written       │
  │  justification explaining why these maintenance      │
  │  cycles were missed before you can approve this      │
  │  work order.                                         │
  └──────────────────────────────────────────────────────┘

  Justification for Skipped Cycles *
  ┌──────────────────────────────────────────────────────┐
  │ Required: Explain why 3 maintenance cycles were      │
  │ missed...                                            │
  │                                                      │
  │                                                      │
  └──────────────────────────────────────────────────────┘
  0 / 500 (minimum 20 characters required)  ← shown in red

  [Existing field]
  Approver Remarks (optional)
  ┌──────────────────────────────────────────────────────┐
  │                                                      │
  └──────────────────────────────────────────────────────┘

  [APPROVE - greyed out, disabled]  [REJECT - active]

AFTER CE types justification (e.g., "Engine overhaul in progress, 
all non-critical jobs deferred by CE order ref: LOG-2026-001"):

  0 / 500 → 75 / 500 (minimum 20 characters required) ← now grey

  [APPROVE - now active, blue]  [REJECT - active]

WHAT IS STORED IN DB after approval:
  work_orders.skipped_cycles_justification = 
    "Engine overhaul in progress, all non-critical jobs 
     deferred by CE order ref: LOG-2026-001"
  work_orders.approver_remarks = (whatever CE typed separately)
  work_orders.status = 'Completed'
  work_orders.missed_cycles = 3 (unchanged)

WHAT CE SEES on completed WO (read-only view):
  [In approval/signature section at bottom of page]

  CE Justification for Skipped Cycles
  ┌──────────────────────────────────────────────────────┐
  │ Engine overhaul in progress, all non-critical jobs   │
  │ deferred by CE order ref: LOG-2026-001               │
  └──────────────────────────────────────────────────────┘
  [shown with light red background]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SUMMARY OF ALL FILES TO MODIFY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Backend:
1. shared/schema.ts
   → Add skipped_cycles_justification TEXT column to workOrders table
   → Run ALTER TABLE migration

2. server/modules/work-orders/services/workOrderService.ts
   → In approveWorkOrder(): check if missed_cycles >= 1 and block 
     approval if skippedCyclesJustification is not provided
   → Save skippedCyclesJustification to the work_orders record

3. server/modules/work-orders/services/workOrderBulkService.ts
   → Same validation for bulk approval path

4. server/modules/work-orders/routes/ or controllers/
   → Add skippedCyclesJustification as accepted field in approval 
     request body

5. server/modules/work-orders/services/workOrderContextService.ts
   → Return skippedCyclesJustification in GET work order response

Frontend:
6. client/src/pages/pms/WorkOrderFormPage.tsx
   → In the approval panel at bottom of page (visible only on 
     Pending Approval status WOs):
     a. Add red mandatory acknowledgement alert box when 
        missedCycles >= 1
     b. Add mandatory justification textarea when missedCycles >= 1
     c. Disable Approve button until 20+ chars typed
     d. Send skippedCyclesJustification in API call on approve
   → In the completed (read-only) view:
     a. Show stored skippedCyclesJustification in a read-only 
        light-red field when it is not null

Please implement all of this now, run the migration, restart the 
app, and confirm no compilation errors. Then provide a brief summary 
of every file changed and what was changed in each file.
