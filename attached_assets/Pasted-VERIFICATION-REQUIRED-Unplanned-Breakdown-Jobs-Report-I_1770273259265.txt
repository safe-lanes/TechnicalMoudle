VERIFICATION REQUIRED: Unplanned/Breakdown Jobs Report Inconsistency

The PDF and Excel reports are showing different data and structure. I need you to verify and fix:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 1: Different Data Filtering
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PDF Report Query: Shows 1 job (filtered by status = 'Completed')
Excel Report Query: Shows 5 jobs (includes Active + Completed)

REQUIREMENT: Both reports MUST show the SAME data.
For unplanned/breakdown jobs analysis, we want COMPLETED jobs only.

EXPECTED QUERY (both PDF and Excel):
```sql
WHERE wo.vessel_id = $1
  AND (wo.work_order_type = 'Unplanned' OR wo.task_type ILIKE '%Unplanned%')
  AND wo.status = 'Completed'  -- THIS LINE IS CRITICAL
  AND DATE(wo.created_at) BETWEEN $2 AND $3
ACTION: Update Excel query to match PDF query (add status = 'Completed' filter)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 2: Excel Has Wrong Column Structure
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current Excel Columns (18 columns - WRONG):
S.No, Work Order No, Job Code, Job Title, Comp Code, Component Name, Dept,
Priority, Status, Due Date, Last Done, Days Left, Days Overdue, Next Due RH,
Current RH, RH Remaining, Assigned To, Critical Equip

These columns are for PLANNED maintenance, not unplanned/breakdown jobs.

REQUIRED Excel Columns (11 columns - MATCH PDF):

S.No

Work Order No

Comp. Code

Component Name

Job Title

Description (brief_work_description)

Created Date

Completed Date

Performed By

Hours (total_time_taken_hours)

Manhours

Department (optional, can be 12th column)

REMOVE these columns (not applicable for unplanned jobs):
❌ Job Code
❌ Due Date
❌ Last Done
❌ Days Left
❌ Days Overdue
❌ Next Due RH
❌ Current RH
❌ RH Remaining
❌ Critical Equip

ACTION: Update Excel column structure to match PDF exactly

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERIFICATION STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Show me the CURRENT Excel generation query (full SQL)

Show me the CURRENT Excel column mapping code

Compare with PDF generation query

Fix Excel to match PDF exactly:

Same WHERE clause (including status = 'Completed')

Same column structure (11-12 columns)

Same data fields

EXPECTED RESULT:

PDF: Shows 1 completed unplanned job (or more if available)

Excel: Shows SAME 1 completed unplanned job (or more if available)

Both have identical columns and data

Generate both reports again after fix and confirm they match.