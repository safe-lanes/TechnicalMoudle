You are a senior backend engineer designing PostgreSQL migrations for a production system.

I already have two tables (possibly created via earlier migrations or manual DB push):

1. adm_available_ranks
2. adm_vessel_org_chart

Your task is to create a SINGLE migration script that:

-----------------------------------
CORE OBJECTIVE
-----------------------------------
1. Ensures tables exist before inserting data
2. Inserts initial seed data from Excel
3. Runs safely in ALL scenarios:
   - Fresh DB
   - DB already containing data
   - DB partially populated
   - DB pushed manually without migration history

-----------------------------------
STEP 1: ENSURE TABLE EXISTENCE
-----------------------------------
1. Check if tables exist:
   - adm_available_ranks
   - adm_vessel_org_chart

2. If NOT exists:
   → Create them with proper schema (columns, PK, FK)

3. If already exists:
   → DO NOT modify structure
   → Continue to data seeding

Use:
   CREATE TABLE IF NOT EXISTS

-----------------------------------
STEP 2: TRANSACTION SAFETY
-----------------------------------
Wrap entire migration in:

BEGIN;
...
COMMIT;

-----------------------------------
STEP 3: IDEMPOTENT MASTER DATA INSERT
-----------------------------------
Insert into:
   adm_available_ranks

Rules:
1. Use ON CONFLICT DO NOTHING
2. Conflict target:
   - ar_uuid (preferred)
   OR fallback to unique name

3. Handle:
   - UUID properly
   - Boolean fields
   - NULL values

4. DO NOT assume table is empty

-----------------------------------
STEP 4: SAFE HIERARCHY INSERT (CRITICAL)
-----------------------------------
Insert into:
   adm_vessel_org_chart

Rules:

1. NEVER hardcode IDs
2. Resolve IDs dynamically:

   rank_id:
     SELECT id FROM adm_available_ranks WHERE name = 'X'

   parent_rank_id:
     SELECT id FROM adm_available_ranks WHERE name = 'Y'

3. Insert in hierarchy-safe way:
   - First rows where parent_rank_id IS NULL
   - Then child rows

OR

Use SELECT-based inserts that only execute when both parent & child exist

-----------------------------------
STEP 5: HANDLE PARTIAL DATA
-----------------------------------
Ensure:
1. If some ranks already exist → skip inserting duplicates
2. If some hierarchy already exists → skip duplicates
3. Use:
   ON CONFLICT DO NOTHING
   OR WHERE NOT EXISTS

-----------------------------------
STEP 6: OPTIONAL SAFETY CHECK (RECOMMENDED)
-----------------------------------
Before seeding, optionally check:

IF data already exists (example: count > 0)
→ Skip full seed
OR
→ Allow partial insert (preferred)

-----------------------------------
STEP 7: INDEXING (IF NOT EXISTS)
-----------------------------------
Ensure indexes exist:

- adm_available_ranks(ar_uuid)
- adm_vessel_org_chart(rank_id)
- adm_vessel_org_chart(parent_rank_id)

-----------------------------------
STEP 8: DO NOT BREAK FK CONSTRAINTS
-----------------------------------
1. DO NOT disable FK checks
2. Ensure parent exists before child
3. Use SELECT-based inserts to guarantee FK validity

-----------------------------------
OUTPUT REQUIREMENT
-----------------------------------
Return a complete PostgreSQL migration script that includes:

- CREATE TABLE IF NOT EXISTS
- FK constraints
- Transaction block
- Idempotent inserts
- Safe hierarchy insertion
- Index creation

-----------------------------------
IMPORTANT RULES
-----------------------------------
DO:
✔ Make migration re-runnable safely
✔ Support unknown DB state
✔ Maintain referential integrity

DO NOT:
✖ Assume empty DB
✖ Hardcode IDs
✖ Disable constraints
✖ Use TRUNCATE

-----------------------------------

Return ONLY SQL migration script. No explanation.