🚨 READ-ONLY TEST MODE — DO NOT WRITE ANY CODE 🚨
You are being asked to test the current state of the application on the revert/orgchart-builder branch. This is a verification task only. You are strictly forbidden from:

Writing, modifying, or deleting any code
Creating, editing, or deleting any files
Making any commits
Pushing to any branch
Merging this branch into any other branch
Rebasing this branch
Creating new checkpoints
"Fixing" anything you notice during testing
Implementing, refactoring, or improving anything
Suggesting changes by making them — only describe in text

Your only allowed actions are:

Running npm install to install dependencies
Running npm run dev to start the server
Reading console and server logs
Navigating the running app in the browser (or equivalent)
Running read-only git commands (git status, git log, git branch) if needed
Producing a text report in this chat

Reply with "Acknowledged — read-only test mode" as your first line.

Context
The Vessel Org Chart builder feature (Tasks #119, #120, #121, #123) has been removed because management changed the requirements. Task #122 (Me/My Team dashboard scope filtering) has been intentionally preserved. You are currently on branch revert/orgchart-builder at commit 4e5e80bc. This branch has NOT been merged into replit_dev yet — that will only happen after your test confirms everything works.

Pre-flight checks (do these first)

Run git branch --show-current — confirm you are on revert/orgchart-builder. If not, STOP and tell me.
Run git log -1 --oneline — confirm HEAD is at commit 4e5e80bc. If not, STOP and tell me.
Run git status — confirm working tree is clean. If anything is modified, STOP and tell me.


Testing steps
Step 1: Install dependencies
npm install
Report any errors or warnings. Expected: clean install with no new dependency issues.
Step 2: Start the dev server
npm run dev
Let it run for at least 30 seconds. Watch the console output for:

Startup errors or stack traces
Any errors referencing: ranks, VesselOrgChart, vessel-org-chart, resolveHierarchyScope, hierarchy-scope, getActiveRoles, getNodesByRoleRuid, roleRuid, nodeLayer
Database connection failures
Migration errors
Port binding issues
Module resolution errors (Cannot find module, Module not found)

Report the full startup output.
Step 3: Manual smoke test in the browser
Open the running app and perform the following checks. Report yes/no for each:
3a. Admin → Ranks page

Navigate to the Admin → Ranks page
Is the page loading successfully? (yes/no)
Is the "Vessel Org Chart" button GONE from this page? (yes/no — it should be gone)
Any console errors on this page? (yes/no — describe if yes)

3b. Direct URL navigation to removed page

Type /admin/vessel-org-chart directly in the URL bar
Does the old builder page render? (yes/no — it should NOT render)
What does the app show instead? (e.g., redirect, 404, blank page, fallback)
Any console errors? (describe)

3c. Dashboard → Me/My Team (Task #122 — must still work)

Navigate to the Dashboard
Is the Me/My Team toggle visible? (yes/no)
Does toggling between "Me" and "My Team" change the displayed data? (yes/no)
Any console errors or network failures? (describe)
Does the Dashboard load KPIs, work orders, or whatever it normally shows? (yes/no)

3d. Other modules (confirm nothing else broke)
Click around these pages and confirm they load normally:

Spares
Certificates / Surveys
Defects
Components
Work Orders
Any other modules you normally use

Report any unexpected errors, missing data, or broken UI.
3e. Browser console review

Open DevTools → Console tab
Report any red errors, 404s, 500s, or warnings that appear during normal navigation
Specifically look for errors referencing org chart, ranks, hierarchy, or role binding

Step 4: Server log review
After the smoke test, check the server console output. Report:

Any 500 errors?
Any unhandled promise rejections?
Any errors mentioning ranks, vessel-org-chart, or hierarchy-scope?


Final report format
Please provide a report in this exact format:
## Test Report — revert/orgchart-builder branch

**Pre-flight:**
- Branch: [confirmed / mismatched]
- Commit: [confirmed 4e5e80bc / mismatched]
- Working tree: [clean / dirty]

**npm install:** [clean / warnings / errors — describe]

**npm run dev:** [boots cleanly / errors — describe]

**Admin → Ranks page:**
- Loads: yes/no
- "Vessel Org Chart" button gone: yes/no
- Console errors: yes/no (describe)

**Direct URL /admin/vessel-org-chart:**
- Builder renders (should NOT): yes/no
- Fallback behavior: [describe]
- Console errors: [describe]

**Dashboard → Me/My Team:**
- Toggle visible: yes/no
- Toggle works: yes/no
- Dashboard loads normally: yes/no
- Console errors: [describe]

**Other modules:**
- Spares: [works / broken — describe]
- Certificates: [works / broken — describe]
- Defects: [works / broken — describe]
- Components: [works / broken — describe]
- Work Orders: [works / broken — describe]

**Browser console (full session):** [clean / errors — list all]

**Server logs (full session):** [clean / errors — list all]

**Overall verdict:** [PASS — safe to merge / FAIL — do not merge, issues listed above]

Hard rules reminder

Do NOT modify any code
Do NOT commit, push, rebase, or merge
Do NOT fix any issues you find — only report them
Do NOT create checkpoints
If the server fails to boot, STOP and report — do not attempt to fix
If you find a bug, STOP and report — do not attempt to fix

Begin with the pre-flight checks.