Full Analysis — What's Wrong vs What the Audit Dashboard Has
Issue 1 — Overview / Management tabs are NOT functioning
Clicking "Overview" does nothing — same content shows. Clicking "Management" alone does nothing either. The only way "Management" content appears is when you click "All Vessel" toggle, which is completely wrong UX logic. These two controls are completely tangled — the tab and the toggle are triggering each other's content accidentally. They need to be separated and work independently.

Issue 2 — All Vessel / My Vessel toggle is confused and clumsy
"All Vessel" click is actually switching the tab to Management AND loading fleet benchmarking — this should not happen

The "All Vessel" toggle in Audit simply scopes the data shown in the same layout — it does NOT switch tabs

The vessel dropdown next to it is redundant when "All Vessel" is already selected — it should only show when "My Vessel" is active

The dropdown also does nothing visible when clicked

Issue 3 — Status Distribution + Progress bar duplication
The donut chart already shows Overdue / Due / Pending / Completed visually

Below it there is a "Pending — 0%" progress bar floating alone doing the same job

The donut chart legend also repeats the same info

This is triple duplication of the same data in the same card — confusing and wasteful

Issue 4 — 6-Month Maintenance Trend is a BAR chart, NOT a LINE chart
The Audit dashboard uses smooth area/line charts for trend data

The bar chart here is heavy, aggressive looking, and does NOT match the elegant line chart style of the Audit module

A line chart with shaded zone bands (Healthy/Watch/Backlog) is more professional and matches the reference design

Issue 5 — Dot matrix is ALL GREEN — completely useless
Every single dot in the Vessel/Fleet Analysis matrix is the same green color

No differentiation at all — all vessels show identical dots for Work Orders, Overdue WOs, Low Stock, Spares, Running Hrs

In the Audit module the dots have real color variation (grey/amber/red) based on actual data per vessel

The dots need to be colored based on actual per-vessel thresholds — red dots for vessels with high overdue, amber for moderate, green for low

Issue 6 — Gauge charts have NO section sub-labels / stats below them (unlike Audit)
In the Audit dashboard, below each gauge you see "Ins: 14 Obs: 87" — small contextual secondary stats

The PMS gauges show "38% of total / Due: 101 / Pending: 1" but these are too compressed, not formatted clearly like Audit

Also the gauge title font style doesn't match — Audit uses centered, clean title above each gauge, PMS uses mixed styling

Issue 7 — Column 1 has NO section sub-dividers between gauges
In the Audit dashboard, each KPI (SIRE 2.0, NON-SIRE, CDI, Terminal, PSC) has a clean horizontal line separating it

In PMS, the three gauges (Overdue WOs, Completion Rate, Outstanding Tasks) are just stacked with white space and a faint card border — no clear visual separator

Issue 8 — "Pending" progress bar is broken / orphaned
There is a lone "Pending — 0%" bar below the donut chart legend with no label context, no styling alignment — it looks like a rendering bug

Issue 9 — Column 3 dot matrix column headers are MISSING
The vessel names/columns in the dot matrix have no visible column headers

In the Audit module, column headers (TS MG, TS MOH, TS MSM etc.) are clearly shown above the dot columns

The PMS dot matrix just shows unlabeled columns with dots, making it unreadable

Issue 10 — Vessel dropdown is still showing alongside "All Vessel / My Vessel" toggle
When "My Vessel" is selected, the dropdown makes sense

When "All Vessel" is selected, the dropdown "All Vessels" is completely redundant and confusing

The dropdown should only be visible when "My Vessel" is active

Replit Correction Prompt
Here is the full prompt to fix all 10 issues:

I want you to fix specific problems in the PMS Dashboard. Do NOT rebuild everything — only fix the issues listed below. Do not touch any data or calculations.

FIX 1 — Overview and Management tabs must work independently
Right now clicking Overview and Management tabs does nothing on their own. Fix this:

"Overview" tab: when active, show the current 3-column layout (Work Order KPIs gauges | Status Distribution + Trend + Table | Inventory + Dots + Watchlist). This is the existing layout — make it the default active view.

"Management" tab: when clicked, replace the 3-column layout with the Fleet Comparison Vessel Benchmarking table that currently only shows when "All Vessel" is clicked. Move that table to be the content of the Management tab.

These two tabs must work completely independently of the "All Vessel / My Vessel" toggle. The tabs control WHAT view you see. The toggle controls WHICH vessels data is scoped to. They are separate controls.

On page load: default to "Overview" tab active + "My Vessel" toggle active.

FIX 2 — All Vessel / My Vessel toggle must only change data scope, NOT switch tabs
Fix the All Vessel / My Vessel pill toggle so that:

Clicking "All Vessel": keeps the current active tab (Overview or Management) exactly as is, but filters all KPI data to show fleet-wide aggregated numbers. It does NOT switch to Management tab.

Clicking "My Vessel": keeps the current tab active, but shows only the selected vessel's data. When "My Vessel" is active, show the vessel dropdown. When "All Vessel" is active, hide the vessel dropdown completely (because "All Vessels" is already the scope).

The vessel dropdown should only appear and be visible when "My Vessel" toggle is active. When "All Vessel" is active, remove the dropdown from the UI entirely — do not just disable it, hide it.

FIX 3 — Remove the orphaned "Pending 0%" progress bar
In the middle column (Work Order Status and Trends), below the donut chart and its legend, there is a lone "Pending — 0%" progress bar with no context. Remove it completely. The donut chart already shows all status data visually. Do not replace it with anything.

FIX 4 — Change 6-Month Maintenance Trend from bar chart to line/area chart
Replace the current bar chart in the "6-Month Maintenance Trend" section with a line chart (or area chart with fill):

X axis: months (Sep, Oct, Nov, Dec, Jan, Feb) — keep as is

Y axis: percentage 0% to 100% — keep as is

The data line: one smooth line showing the outstanding task % per month — same data, different chart type

Add three shaded horizontal zone bands behind the line:

Green zone: 0% to 30% labeled "Healthy"

Amber/orange zone: 30% to 60% labeled "Watch"

Red zone: 60% to 100% labeled "Backlog"

The line itself: solid, colored by current zone (if value is in Backlog zone, line is red; Watch zone, amber; Healthy zone, green)

Legend below: Healthy | Watch | Backlog with colored rectangles — same as before but matching line chart style

Keep the same card container and title "6-MONTH MAINTENANCE TREND"

FIX 5 — Fix the Vessel/Fleet Analysis dot matrix
The dot matrix currently shows all green dots with no column headers. Fix both problems:

Add column headers:

Above the dots, add the vessel names as column headers — use the vessel names from the existing vessel data (the same names that appear in the Overdue Work Orders table or vessel dropdown)

Column headers: small text, grey, centered above each column of dots

Row labels on the left: Work Orders | Overdue WOs | Low Stock | Spares | Running Hrs — keep as is

Color the dots based on actual data thresholds:

For each vessel, calculate:

Work Orders dot: green if vessel has WOs, grey if no data

Overdue WOs dot:

Red if overdue % is above 30% of that vessel's total WOs

Amber if overdue % is 10%–30%

Green if overdue % is below 10%

Low Stock dot:

Red if low stock items above 100

Amber if 50–100

Green if below 50

Spares dot: green if spares data exists, grey if not

Running Hrs dot: green if running hours updated in last 30 days, amber if 30–90 days, red if more than 90 days

Use the existing data you already have for these calculations — do not create new dummy data. If vessel data is not available for a metric, show a grey dot (not green).

FIX 6 — Clean up the gauge chart secondary stats formatting
Below each semicircle gauge in Column 1, format the secondary stats consistently to match the Audit dashboard style (Ins: 14 Obs: 87 format):

Overdue WOs gauge: below the gauge arc, show on one line: "Due: 101 · Pending: 1" — centered, small grey font, using a centered dot separator

Completion Rate gauge: show "Total: 2397 · Active: 122" — same format

Outstanding Tasks gauge: show "Tasks: 61/92 · vs last month: -24%" — same format

Each gauge item should have:

Clear title above the gauge arc (bold, dark, centered) — e.g., "Overdue WOs"

The arc + center number

The main descriptor below center — e.g., "38% of total" in small grey

The two secondary stats in the format above

A horizontal divider line 1px solid #e8e8e8 below each gauge item before the next one starts

FIX 7 — Add horizontal dividers between gauge items in Column 1
Between each of the 3 gauge sections in Column 1 (Overdue WOs, Completion Rate, Outstanding Tasks), add a clean horizontal separator line:

Color: #e8e8e8

Width: full column width

No margin above or below the line — let the padding of each gauge section create the spacing

CONSISTENCY RULES — Apply to all fixes
Use the exact same dark navy blue (#1a3261 or whatever exact color the column header bars currently use) for all section title bars — do not introduce any new colors

All badges/pills: same border radius, same font size, same padding — make them identical to each other

Do not change any working functionality — the donut chart click-to-filter, the Watch List links, the View All buttons must all remain working

At the end tell me:

Which files you changed

Confirm the Overview/Management tab logic is now separated from All Vessel/My Vessel toggle logic

Confirm the dot matrix now has column headers and colored dots based on real data