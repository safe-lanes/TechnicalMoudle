TASK: FIX LAYOUT OVERFLOW – ADD / EDIT FLEET COMPONENT SCREEN

Objective:

Ensure the **Add / Edit Fleet Component screen** is properly contained **within module boundaries** and does not overflow beyond **horizontal or vertical headers**, aligning its size with the **Component Edit page**.

--------------------------------------------------

1. Current Issue

• Screen is overflowing outside:

  - Horizontal module header  
  - Vertical module header (sidebar)  

• Layout is not properly contained within viewport  
• Causes UI misalignment and poor usability  

--------------------------------------------------

2. Expected Behavior

• Screen must:

  - Fit completely within module layout  
  - Respect header and sidebar boundaries  
  - Maintain consistent width similar to Component Edit page  

--------------------------------------------------

3. Root Cause Analysis

Investigate:

• Container width set to 100vw instead of available space  
• Missing max-width constraint  
• Incorrect margin/padding  
• Overflow settings not controlled  
• Sidebar/header offset not considered  

--------------------------------------------------

4. Corrective Plan

A. Container Width Fix

• Replace full viewport width usage:

  ❌ width: 100vw  
  ✔ width: 100%  

• Ensure container respects parent layout  

--------------------------------------------------

B. Apply Max Width

• Match Component Edit page:

  max-width: <same as component edit container>  
  margin: auto  

--------------------------------------------------

C. Layout Containment

• Wrap screen inside main layout container:

  - Should align with module content area  
  - Must not overlap sidebar/header  

--------------------------------------------------

D. Overflow Handling

• Add:

  overflow-x: hidden  

• Ensure no horizontal scroll due to layout overflow  

--------------------------------------------------

E. Vertical Alignment

• Ensure:

  - Proper top spacing below header  
  - No content hidden behind header  

--------------------------------------------------

F. Responsive Behavior

• Ensure screen adapts to:

  - Different resolutions  
  - Smaller screen widths  

--------------------------------------------------

5. UI Consistency

• Match Component Edit page for:

  - Width  
  - Alignment  
  - Padding  
  - Content centering  

--------------------------------------------------

6. Validation

Check:

• No horizontal scroll  
• No overlap with sidebar  
• No content hidden behind header  
• Layout identical to Component Edit page width  

--------------------------------------------------

Expected Result:

• Add / Edit Fleet Component screen fits within module layout  
• No overflow outside headers or sidebar  
• Screen width matches Component Edit page  
• Clean and consistent UI layout  