✅ Perfect observation — the **Condition of Class (CoC)** checkbox currently breaks visual alignment because it’s inserted *between* the “Date Issued / Target Date / Responsible Role” block. That placement disrupts the horizontal symmetry of the Defect Form.  

Here’s the corrected **Replit prompt** to fix this layout issue while keeping everything functional and professional.

---

## 🧩 Replit Prompt — Fix “Condition of Class (CoC)” Checkbox Placement

### 🎯 Objective
Reposition the **Condition of Class (CoC)** checkbox in the **Defect Report form** to make the layout clean, aligned, and professional — while keeping its data binding (`is_coc`) and behavior intact.

---

### 🧱 Form Layout Update
**Where to move it:**
- Move the **CoC checkbox block** *below* the entire “Date” section.  
  This means it should appear **after** the “Date Completed” field.  
- It must remain within the same column alignment (right-hand side column), but visually separated by a small vertical gap (about `16px`).

**Design specification:**
```
Date Issued      [    ☐   ]
Target Date for Closure [    ☐   ]
Responsible Role [ dropdown ]
Date Completed   [    ☐   ]

──────────────────────────────
☑ Condition of Class (CoC)
   Tick if this defect is a Class Condition.
──────────────────────────────
```

**CSS / Spacing adjustments:**
- Use standard top padding or margin (e.g., `margin-top: 12px;`) before rendering the CoC block.
- Align its left margin to match input labels (not indented).
- Label font-weight same as other form field labels (`font-weight: 600`).

---

### ⚙️ Behavior (Keep same as before)
- Field name: `is_coc`
- Default: `false`
- On form load: display existing saved value.
- On save: persist to backend (POST/PUT /defects).
- On view mode: show read-only line → **Condition of Class: Yes / No**

---

### 📊 Sidebar Integration (unchanged)
- Sidebar “CoC (Condition of Class)” button still filters defects with `is_coc = true`.
- Active/Resolved tabs inside CoC section remain functional:
  - **Active CoC** → `status=open && is_coc=true`
  - **Resolved CoC** → `status=closed && is_coc=true`

---

### ✅ Test after fix
1. Open defect form → visually confirm CoC checkbox appears below date fields.
2. Save a defect with CoC checked → visible in main list with CoC badge.
3. Click sidebar CoC → only CoC defects appear.
4. Edit defect → checkbox retains correct state.
5. Export/Reports → still show CoC column.

---

Would you like me to add a **horizontal divider line** above the checkbox (“–––––––––––––––––”) to visually separate it from the date fields for a cleaner sectioned look (like a sub-block)? It’ll make it appear more intentional and less “floating.”