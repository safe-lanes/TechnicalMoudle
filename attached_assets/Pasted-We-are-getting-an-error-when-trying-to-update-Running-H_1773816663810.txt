We are getting an error when trying to update Running Hours for a component in the Running Hours module. The error says: "Same-day update already performed. Only one update per day." The error response also shows maxAllowedIncrease as 0, requestedIncrease as 130, a date from August 2015, and requiresAdminOverride as true.

The user was trying to enter a backdated RH reading. The component currently shows 870 hours, and the user was trying to set it to 1000 hours with a date of 25-02-2015.

We believe the system is confusing two completely different scenarios. The first is multiple updates on the same actual current date, which should be restricted. The second is entering a backdated historical RH entry for a past date, which must always be allowed because engineers regularly need to record past readings.

Please investigate the code and answer the following questions without making any changes yet:

Find the exact file and function where the same-day validation is implemented. What does the rule check?

Why is maxAllowedIncrease showing as 0? Walk through the calculation with the actual values.

What date does the system use to check for same-day duplicates? Is it comparing the user-entered date against other entries on that same date, or is it using today's actual date, or the last recorded update date?

Is the system incorrectly treating a backdated entry (date in 2015) as a same-day violation? Explain exactly how the logic flows for a backdated date entry.

What data currently exists in the database for FO PURIFIER NO.2, component 702.005.02? Show all RH history entries with their dates and values, and what the last update date field says.

Does the validation behave differently for Set Total mode versus Add Delta mode?

What is requiresAdminOverride and is there any way in the UI to bypass it?

List every validation check that runs in order when a user submits an RH update, and what triggers each one to fail.

Do not fix anything yet. Just investigate and explain clearly what is happening and why. Share the findings so we can decide the correct fix