=======================================================
PMS DASHBOARD — FULL UI CORRECTION PROMPT
Target: Make PMS Dashboard look EXACTLY like the reference at dev.sl-sail.com/audit/dashboard/summary
Do NOT change any data, routing, API calls, or business logic. ONLY change styles/CSS/JSX styling.
=======================================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 1: TOP NAVIGATION BAR (TopMenuBar.tsx)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CURRENT STATE:
- The entire top navbar has a white background — this part is OK
- The active tab "PMS" has a blue bottom underline — this part is OK
- BUT: The active tab icon (wrench/spanner SVG) and the active tab label text "PMS" 
  are CURRENTLY showing in a slightly different shade or the icon is not colored blue
- The inactive tabs (Technical, Cert. & Surveys, Defects, Admin) currently show 
  icons and text in dark gray — this is correct

REFERENCE STATE:
- Navbar background: pure white #ffffff
- Active tab (Dashboard in reference): icon is filled/colored BLUE #1a6eb5, 
  text is BLUE #1a6eb5, and there is a solid 3px blue bottom border ONLY on the active tab
- Inactive tabs: icon and text are dark gray #4b5563
- No background color on any tab (active or inactive)
- Bottom of navbar has a 1px solid #e5e7eb border separating it from page content

EXACT FIXES:
1. Find the top navbar container div. Set: backgroundColor: '#ffffff', 
   borderBottom: '1px solid #e5e7eb'
2. For the ACTIVE tab item: 
   - Remove any background color class (remove bg-[...] from active tab)
   - Add: borderBottom: '3px solid #1a6eb5', paddingBottom applied correctly
   - Set text color: color: '#1a6eb5'
   - Set icon color/stroke/fill: '#1a6eb5'
3. For INACTIVE tab items:
   - No border bottom
   - Text color: '#4b5563'
   - Icon color: '#4b5563'


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 2: LEFT SIDEBAR (SideMenuBar.tsx / TechnicalModule.tsx)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CURRENT STATE:
- Sidebar background is dark navy (approximately #16569e or similar dark blue)
- The active item "Dashboard" has a BRIGHT CYAN/TEAL highlight background (#52baf3) 
  which looks like a bright pill/block highlight — this is very wrong
- Sidebar width appears narrow

REFERENCE STATE:
- Sidebar background is a MEDIUM-DARK BLUE — specifically looks like #1565c0 
  (a true royal/material blue, NOT dark navy, NOT bright cyan)
- The active item does NOT have a background color block
- The active item has ONLY a white left border indicator: 
  borderLeft: '3px solid #ffffff' on the active list item
- Active item text and icon: white #ffffff, same as inactive (no color change)
- Inactive item text and icon: white #ffffff with ~70% opacity (rgba(255,255,255,0.7))
- Icons sit ABOVE their label text (vertically stacked), centered
- Sidebar items have consistent padding: ~12px top/bottom, centered

EXACT FIXES IN TechnicalModule.tsx:
1. Find the sidebar container. Change background from bg-[#16569e] or 
   whatever dark navy class is used TO: backgroundColor: '#1565c0'
   OR Tailwind equivalent: bg-[#1565c0]

EXACT FIXES IN SideMenuBar.tsx:
2. For the ACTIVE sidebar item:
   - REMOVE the bg-[#52baf3] or any bright background class
   - ADD: borderLeft: '3px solid rgba(255,255,255,0.9)'
   - backgroundColor: 'rgba(255,255,255,0.12)' (very subtle white overlay only)
   - Text color: '#ffffff'
   - Icon color: '#ffffff'
3. For INACTIVE sidebar items:
   - Remove any background
   - Text color: 'rgba(255,255,255,0.75)'
   - Icon color: 'rgba(255,255,255,0.75)'
   - On hover: backgroundColor: 'rgba(255,255,255,0.08)'


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 3: DASHBOARD HEADER ROW (Dashboard.tsx)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CURRENT STATE (the header row that contains "Dashboard", tabs, year "2026", toggles):
- "Dashboard" title: appears in large bold dark text — OK
- "Overview" tab: dark navy filled pill — OK-ish
- "Management" tab: outline/border pill — OK-ish
- "2026": large bold text — OK
- "All Vessel": dark navy filled pill — correct (it's the active one)
- "My Vessel": outlined with a border — but border looks heavy/dark
- "Filters" button: has a border and icon — OK

REFERENCE STATE:
- "Dashboard" title: font-size ~20px, font-weight 600, color #1a2b4a (dark navy)
- "Operational/Management" tabs: 
  • ACTIVE tab (Management): backgroundColor '#1a2b4a', color '#ffffff', 
    borderRadius '20px', padding '5px 18px', fontSize '13px', fontWeight 600, border: none
  • INACTIVE tab (Operational): backgroundColor '#f1f5f9', color '#64748b', 
    borderRadius '20px', padding '5px 18px', fontSize '13px', border: none
- "All Vessel / My Vessel" toggle:
  • ACTIVE (All Vessel in PMS): backgroundColor '#1a2b4a', color '#ffffff', 
    borderRadius '20px', padding '5px 16px', fontSize '13px', border: none
  • INACTIVE (My Vessel): backgroundColor '#ffffff', color '#374151', 
    borderRadius '20px', padding '5px 16px', fontSize '13px', 
    border: '1px solid #e2e8f0' (very light gray — NOT dark navy border)
- "Filters" button: backgroundColor '#ffffff', border: '1px solid #e2e8f0', 
  borderRadius '8px', color '#374151', padding '6px 12px'
- The year "2026": fontSize ~18px, fontWeight 700, color '#1a2b4a'

EXACT FIXES:
1. In Dashboard.tsx, find the Overview/Management tab toggle styles.
   Set active tab: { backgroundColor: '#1a2b4a', color: '#fff', borderRadius: '20px', 
   padding: '5px 18px', border: 'none', cursor: 'pointer', fontSize: '13px', fontWeight: 600 }
   Set inactive tab: { backgroundColor: '#f1f5f9', color: '#64748b', borderRadius: '20px', 
   padding: '5px 18px', border: 'none', cursor: 'pointer', fontSize: '13px' }

2. In FleetVesselContextBar.tsx (or wherever All Vessel / My Vessel is rendered):
   Active: { backgroundColor: '#1a2b4a', color: '#fff', borderRadius: '20px', 
   padding: '5px 16px', border: 'none', fontSize: '13px' }
   Inactive: { backgroundColor: '#fff', color: '#374151', borderRadius: '20px', 
   padding: '5px 16px', border: '1px solid #e2e8f0', fontSize: '13px' }


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 4: SECTION COLUMN HEADERS (Dashboard.tsx)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CURRENT STATE:
- "WORK ORDER KPIS", "WORK ORDER STATUS & TRENDS", "INVENTORY & FLEET ANALYSIS" 
  appear as small blue uppercase labels on a WHITE background — this is mostly correct now
- BUT they appear too close to the top with no breathing room
- The font size and weight may not exactly match

REFERENCE STATE (e.g. "KPI EXTERNAL INSPECTIONS"):
- Text: uppercase, fontSize: '11px', fontWeight: 700, color: '#1a6eb5', letterSpacing: '0.8px'
- No background color
- Padding below the label before content starts: ~12px
- The label sits at the very top of its column section with paddingTop: '16px', paddingLeft: '4px'

EXACT FIXES:
1. Find sectionHeaderBar or equivalent style object in Dashboard.tsx
   Set: { 
     backgroundColor: 'transparent', 
     color: '#1a6eb5', 
     fontSize: '11px', 
     fontWeight: 700, 
     textTransform: 'uppercase', 
     letterSpacing: '0.8px',
     padding: '16px 4px 12px 4px',
     borderBottom: 'none'
   }
2. Remove any backgroundColor that may be white-ish (#fafafa etc) — make it fully transparent


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 5: GAUGE / SPEEDOMETER CHARTS (SemiCircleGauge.tsx)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CURRENT STATE:
- Gauge arc is THICK and heavy — takes up a lot of visual space
- Arc color is dark navy — correct color but wrong thickness
- Number inside gauge is large and dark — correct
- The min/max tick labels (0 and 2618) sit at the bottom ends of the arc — 
  they are very small gray numbers — these exist in reference too, so keep them

REFERENCE STATE (KPI gauges like SIRE 2.0):
- Arc is THIN — strokeWidth equivalent of about 8-10px (very slim arc)
- Background track: light gray #e5e7eb, same thin stroke width
- Active arc color: #2563eb (a medium blue — NOT dark navy #1a2b4a) 
  Wait — reference uses GRAY arc color because KPI values show gray 
  when no target is set. But for our PMS, we want: 
  • OVERDUE WOS gauge arc: color #e74c3c (red, because overdue is bad)
  • COMPLETION RATE gauge arc: color #16a34a (green, because completion is good)  
  • OUTSTANDING TASKS gauge arc: color #f59e0b (amber, neutral)
  This semantic coloring is INTENTIONAL and should stay. 
  What needs to change is ONLY the stroke thickness.
- Number inside: fontSize 28-30px, fontWeight 700, color #1a2b4a (dark, not matching arc color)
- Supporting text below number: fontSize 12px, color #6b7280

EXACT FIXES in SemiCircleGauge.tsx:
1. Find where strokeWidth is defined on the SVG arc paths
   Change the strokeWidth from current value to: 10 (for a 200px wide gauge)
   If the gauge width is different, set strokeWidth as ~5% of the viewBox width
2. Do NOT change arc colors — keep red/green/amber for semantic meaning
3. Change the displayed number style:
   fontSize: 28, fontWeight: 700, fill: '#1a2b4a' (dark navy, not the arc color)
4. The gauge TITLE text above (OVERDUE WOS etc) — make sure it is:
   fontSize: '11px', fontWeight: 700, color: '#1a6eb5', textTransform: 'uppercase',
   letterSpacing: '0.6px'
   This is already done but verify it is applying correctly


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 6: TREND LINE CHART — CRITICAL BUG (Dashboard.tsx)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CURRENT STATE — THIS IS COMPLETELY BROKEN:
- The Y-axis shows values like "12522%", "7000%", "3500%", "0%" 
  which are NONSENSICAL percentage values way over 100%
- The red "Overdue" line spikes to ~12522% in March
- The green "Completed" and orange "Outstanding" lines are completely flat at 0%
- This is because the data is likely being calculated as 
  (count / total) * 100 but "total" is wrong or data is raw count 
  being treated as percentage

ROOT CAUSE FIX REQUIRED:
The chart data must be computed correctly. For each month in the 6-month window:
- Get the totalWOs for that month (or use a fixed total like 2618)
- completedPct = (completedThisMonth / totalWOs) * 100  → capped at 100
- overduePct = (overdueThisMonth / totalWOs) * 100  → capped at 100
- outstandingPct = (outstandingThisMonth / totalWOs) * 100 → capped at 100

EXACT FIXES in Dashboard.tsx (trend chart data preparation):
1. Find where trendData array is being built for the chart
2. Ensure each month object has: 
   { month: 'Oct', completed: 45, overdue: 30, outstanding: 25 } 
   where all values are PERCENTAGES between 0–100, NOT raw counts
3. If the calculation is (value / total * 100), make sure 'total' 
   is the correct total work orders number (2618 in this case)
4. Add a hard clamp: Math.min(100, Math.round((val / total) * 100))
5. In the LineChart component:
   - Set Y-axis: domain={[0, 100]}, tickFormatter={(v) => `${v}%`}
   - Set tickCount={5} on Y-axis (showing 0%, 25%, 50%, 75%, 100%)
6. The chart should show 3 lines:
   - Line 1: dataKey="completed" stroke="#16a34a" strokeWidth={2} dot={{ r: 4 }} name="Completed %"
   - Line 2: dataKey="outstanding" stroke="#f59e0b"
