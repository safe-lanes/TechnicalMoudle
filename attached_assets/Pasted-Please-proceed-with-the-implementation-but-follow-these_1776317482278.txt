Please proceed with the implementation, but follow these confirmed business rules and constraints exactly:

Confirmed functional rules
The vessel org chart is the single source of truth for team structure.

The Me / My Team toggle must work in the Operation Dashboard based on the logged-in user’s rank and org chart position.

Me = show only data related to the logged-in user’s own rank.

My Team = show data for the logged-in user plus all subordinate ranks under that user in the vessel org chart.

For Chief Officer, My Team must include Chief Officer + all deck subordinates under him in that vessel’s org chart.

For Chief Engineer, My Team must include Chief Engineer + all engine subordinates under him in that vessel’s org chart.

For non-HOD users with no subordinates, My Team should behave the same as Me. Keep the UI consistent; do not hide the toggle just because the user has no descendants.

The filtering logic must be centralized and reusable, so the same scope can be applied consistently across dashboard cards, tables, and detail views.

Scope handling rules
Apply this scope logic first to all dashboard data that is rank/user assignable and logically belongs to the operational responsibility of the logged-in user.

If a widget does not have a valid rank/user ownership concept, do not force incorrect filtering. Instead, clearly isolate that logic and mark it for review.

Do not use fake or guessed mappings for widgets like spares, anomalies, or approvals if the ownership field is unclear in the schema.

Where filtering cannot yet be correctly applied because of missing ownership linkage, keep the code structured so those modules can be plugged in later.

Fallback and error rules
If org chart is missing for the vessel or the logged-in rank is not mapped correctly, do not break the dashboard.

In such cases, use a safe fallback:
Me = only own rank data if possible

My Team = same as Me if team hierarchy cannot be resolved


Do not default to full vessel-wide data for vessel users when org chart mapping is missing, unless existing access rules explicitly require it.

Technical expectations
Reuse the existing backend scope resolution if already present, but review it carefully for correctness.

Verify that descendant resolution includes the current user’s own rank in My Team scope.

Verify that all relevant dashboard API responses and frontend widgets are actually consuming scoped data consistently.

If some widgets are currently using unscoped endpoints or separate queries, refactor them to use the same scope engine where appropriate.

Avoid duplicating team-resolution logic in multiple places; create one clear reusable resolver/service/helper.

What I want from you now
Please do the following in order:

Audit the current implementation and identify exactly what is already working, partially working, or missing.

List the files, functions, APIs, and components that need changes.

Then implement the feature fully.

After implementation, give me a concise test checklist with scenarios for:
Chief Officer → Me

Chief Officer → My Team

Chief Engineer → Me

Chief Engineer → My Team

Non-HOD user → Me / My Team same result

Missing org chart fallback


Also mention any widgets that still cannot be correctly scoped because of missing ownership fields in the database.

Important
Do not change unrelated dashboard behavior.

Do not simplify business rules without asking.

If you find schema ambiguity, report it explicitly before making assumptions.

Prefer correct partial implementation over broad but incorrect filtering.