text
In the file client/src/pages/reports/ReportsModule.tsx, I need to add a "Clear" button to the filter row to match the consistent pattern used in other modules (Work Orders, Running Hours, Stores).

## CURRENT STATE
The Reports module has:
- Search bar: "Search reports..."
- Two filter dropdowns: "Vessel" and "Date Range"
- NO Clear button

## REQUIRED IMPLEMENTATION
Add a "Clear" button that resets all filters to their default state, matching the implementation pattern from other modules.

## REFERENCE IMPLEMENTATIONS

### 1. Work Orders Module Pattern (WorkOrders.tsx)
Location: The Clear button appears on the right side of the filter row
Format: "× Clear" (X icon + text)
Positioning: After the filter dropdowns, before the action buttons

### 2. Running Hours Module Pattern (RunningHours.tsx)
The Clear button appears after the "Export" button
Format: "Clear" text only (no icon visible in UI)

### 3. Stores Module Pattern (Stores.tsx)
The Clear button appears after "Export" button
Format: "Clear" text

## IMPLEMENTATION REQUIREMENTS

### Visual Styling:
Use the same styling as seen in other modules:
- Simple text button appearance
- Text color: gray (text-gray-600 or text-gray-700)
- Hover state: darker gray or underline
- No background color or border in default state
- Should look like: "Clear" as plain text link

### Positioning:
Place the Clear button to the right of the "Date Range" dropdown filter
It should be inline with the filter row, not on a separate line

### Functionality:
When clicked, the Clear button should:
1. Reset the search input to empty string: setSearchQuery('')
2. Reset Vessel filter to default: 'All Vessels' or empty value
3. Reset Date Range filter to default: null or empty value
4. Reset any other filters if they exist (Department, Priority, etc.)
5. Show all report categories without any filtering applied

### Code Structure:
```tsx
// Add Clear button in the filter row
<div className="flex items-center gap-3 mb-6">
  {/* Search Bar */}
  <div className="relative flex-1">
    <Input
      type="search"
      placeholder="Search reports..."
      value={searchQuery}
      onChange={(e) => setSearchQuery(e.target.value)}
      className="w-full"
    />
  </div>

  {/* Vessel Filter */}
  <div>
    <label className="text-xs text-gray-600 mb-1 block">Vessel</label>
    <Select value={globalFilters.vessel} onValueChange={(value) => handleFilterChange('vessel', value)}>
      <SelectTrigger className="w-[200px]">
        <SelectValue placeholder="All Vessels" />
      </SelectTrigger>
      <SelectContent>
        {/* Vessel options */}
      </SelectContent>
    </Select>
  </div>

  {/* Date Range Filter */}
  <div>
    <label className="text-xs text-gray-600 mb-1 block">Date Range</label>
    <Popover>
      <PopoverTrigger asChild>
        <Button variant="outline" className="w-[200px]">
          <Calendar className="mr-2 h-4 w-4" />
          {globalFilters.dateRange ? 'Custom Range' : 'Select date range'}
        </Button>
      </PopoverTrigger>
      <PopoverContent>
        {/* Date picker content */}
      </PopoverContent>
    </Popover>
  </div>

  {/* Clear Button - ADD THIS */}
  <Button
    variant="ghost"
    onClick={handleClearFilters}
    className="text-gray-600 hover:text-gray-900"
  >
    Clear
  </Button>
</div>
Handler Function:
Add the clear filters handler function:

tsx
const handleClearFilters = () => {
  // Reset search query
  setSearchQuery('');
  
  // Reset all global filters to default values
  setGlobalFilters({
    vessel: '',
    department: '',
    priority: '',
    dateRange: null
  });
  
  // Optional: If you have a handleFiltersReset function from GlobalFilters component, call it
  // handleFiltersReset();
};
IMPORTANT NOTES
Button Variant: Use variant="ghost" for the Button component to match the minimal styling seen in other modules

No Icon Needed: Based on the screenshots, just use "Clear" text without an X icon (unlike Work Orders which shows ×)

Alignment: The button should align vertically with the filter dropdowns

Conditional Display (Optional): Consider showing the Clear button only when filters are active:

tsx
{(searchQuery || globalFilters.vessel || globalFilters.dateRange) && (
  <Button variant="ghost" onClick={handleClearFilters}>Clear</Button>
)}
Responsive Behavior: On mobile, the Clear button should stack appropriately with other filters

EXPECTED RESULT
After implementation, the Reports module filter row should have:

Search input on the left

Vessel dropdown

Date Range dropdown

"Clear" button on the right (subtle, text-only appearance)

Clicking Clear resets all filters to defaults

This will bring the Reports module in line with the consistent UX pattern across all other modules in the application.

text

***

This prompt provides Replit AI with:
1. ✅ Clear context and current state
2. ✅ Reference implementations from other modules
3. ✅ Exact positioning and styling requirements
4. ✅ Code structure with examples
5. ✅ Handler function implementation
6. ✅ Visual consistency guidelines
7. ✅ Expected behavior and result