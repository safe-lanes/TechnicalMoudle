Issues #1 and #2 are fixed. Now let's fix Issue #3: After editing ROB in the spares section, the changed ROB history is not appearing in the history section.

Current behavior:
1. Edit ROB (Remaining On Board) quantity for a spare
2. Save the change
3. History section doesn't show the ROB change

Please investigate and fix:

1. First, find how ROB updates are handled. In server/persistentStorage.ts, look for:
   - updateSpare method (search for "async updateSpare")
   - Check if it creates a SpareHistory entry when ROB changes
   - Add logging:
   console.log('[SPARE_UPDATE] ROB change:', { 
     spareId, 
     oldRob: existingSpare.rob, 
     newRob: data.rob,
     historyCreated: true/false 
   });

2. Check if there's an API endpoint to GET spare history. In server/routes.ts, search for:
   - "/api/spares/:id/history" or similar
   - If missing, add this endpoint:
   
   app.get("/api/spares/:id/history", async (req, res) => {
     try {
       const spareId = parseInt(req.params.id);
       console.log('[API] Fetching history for spare:', spareId);
       const history = await storage.getSpareHistoryBySpareId(spareId);
       console.log('[API] Found', history.length, 'history entries');
       res.json(history);
     } catch (error: any) {
       console.error('[API] Spare history error:', error.message);
       res.status(500).json({ error: "Failed to fetch spare history" });
     }
   });

3. On the client side, in client/src/pages/spares/Spares.tsx (or SparesNew.tsx):
   - Find where history is displayed
   - Check what API endpoint it calls for history
   - Add logging: console.log('[SPARES_UI] Fetching history for spare:', spareId);

4. Verify test-data.json has a "sparesHistory" array and check its contents after a ROB update.

Test by:
1. Find any spare and note its current ROB
2. Update the ROB to a different value
3. Check server logs for history creation
4. Check if history appears in UI
5. Check test-data.json sparesHistory array