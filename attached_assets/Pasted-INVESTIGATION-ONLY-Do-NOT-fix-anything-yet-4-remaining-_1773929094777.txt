INVESTIGATION ONLY — Do NOT fix anything yet.

4 remaining HIGH priority issues, all related to Vessel tab behavior. Investigate each:

--- ISSUE H1 (Doc: Cert Vessel #8) ---
Bug: Added new certificate entry in Vessel 1, but it shows on other vessels too (e.g. Vessel 6).
Expected: Vessel-specific entries should only appear for that vessel.

Investigate:
1. Go to Admin > Ship Certificates > Vessel tab > Select Vessel 1
2. Add a test entry "H1 Test Cert" and Save
3. Check DB: SELECT master_id, name, vessel_id FROM vessel_certificate_applicability WHERE name LIKE '%H1 Test%';
   - Does it have vessel_id set to Vessel 1's vuuid?
   - Or is vessel_id NULL/empty?
4. Now select Vessel 6 on the Vessel tab — does "H1 Test Cert" appear there too?
5. How does the Vessel tab query data for a specific vessel?
   grep -rn "vesselId\|vessel_id" server/modules/cert-surveys/ | grep -i "applic"
6. Does the GET endpoint filter by vesselId? Or does it return all records?
7. Is the VES- prefixed entry stored in ship_certificates_master (shared table) or vessel_certificate_applicability (vessel-specific table)?

--- ISSUE H4 (Doc: Survey Vessel #13) ---
Bug: Unable to save new vessel survey entry.

Investigate:
1. Go to Admin > Ship Surveys > Vessel tab > Select any vessel
2. Try adding a new survey entry and click Save
3. Check browser Network tab or test with curl — what error does the API return?
4. Check the save endpoint:
   grep -rn "survey.*vessel\|vessel.*survey" server/modules/cert-surveys/routes.ts
5. Is the vesselId being passed in the save payload?
6. Check if the save tries to insert into ship_surveys_master with category='Vessel' — does it need a vessel_id column there?

--- ISSUE H5 (Doc: Survey Vessel #14) ---
Bug: Unable to save entry in Survey Company tab. Disappears after refresh.

Investigate:
1. This was supposed to be fixed by our earlier applicableToCompany filter fix
2. Test: Go to Admin > Ship Surveys > Company tab > Add new entry > Save
3. Check DB: SELECT * FROM ship_surveys_master WHERE master_id LIKE 'CMP-%';
4. Does the entry exist in DB after save?
5. If yes, does it disappear from Company tab on refresh? Check the Company tab query — is it filtering correctly?
6. If no, what error does the save API return?

--- ISSUE H6 (Doc: Survey Vessel #15) ---
Bug: Added new survey entry in Vessel 1, but it shows on other vessels (e.g. Vessel 8).
Expected: Same as H1 — vessel-specific entries should only appear for that vessel.

Investigate:
1. Same pattern as H1 but for surveys
2. Check: when a VES- survey entry is saved, where is it stored?
3. Does it go into ship_surveys_master (shared) or a vessel-specific table?
4. If stored in ship_surveys_master, there's no vessel_id column — so ALL vessels would see it
5. Check: SELECT column_name FROM information_schema.columns WHERE table_name = 'ship_surveys_master';
   - Is there a vessel_id or vessel column?

--- REPORT FORMAT ---
For each issue:
- Root cause: what exactly is wrong
- Is the entry stored in the correct table?
- Is vessel_id being saved with the entry?
- Which file/line needs to change
- Is this a backend or frontend issue

Also answer this critical question:
Where should vessel-specific certificates/surveys be stored?
A) In ship_certificates_master / ship_surveys_master with a vessel_id column
B) In vessel_certificate_applicability / vessel_survey_applicability
C) Somewhere else

Report findings. Do NOT fix anything.