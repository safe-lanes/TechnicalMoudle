I need you to fix the pagination counter at the bottom of the Running Hours table on the /pms/running-hrs page. Currently, the bottom-right of the table shows a label that reads "Page 6 of 6" (or similar "Page X of Y" text), but there are no navigation buttons — no Previous, no Next, no page number buttons. The entire table is already rendered as a single scrollable list showing all components at once. This makes the pagination counter meaningless, misleading, and confusing to the user.

Step 1 — Investigate First:

Before making any changes, look at the codebase and understand the following:

Where is the "Page X of Y" counter rendered in the Running Hours table component? Find the exact component or element responsible for this.

Is there actual server-side or client-side pagination logic wired up behind it (i.e., does the API accept page/limit parameters and return paginated data), or is all data being fetched in a single API call and rendered at once?

Is the table currently rendering all components in one go with no true pagination, or is the scroll behavior itself acting as the "page"?

Check if any other table/list in the application (Work Orders, Components, Spares, etc.) uses a similar pagination pattern — understand the shared component being used, if any.

Step 2 — Decision Logic (implement based on what you find):

Case A — If there is NO real pagination logic (all data is fetched in one call and rendered as a scrollable list):

This is the most likely scenario based on the current behavior. In this case:

Remove the "Page X of Y" counter entirely. It is inaccurate and serves no purpose when there are no navigation controls.

Replace it with a simple total count label positioned at the bottom-left of the table (below the last row, inside the table container), styled as subtle gray text. The label should read: "Showing X components" where X is the actual total number of components currently displayed. For example: "Showing 19 components".

When the Search box is active and the list is filtered, this label should dynamically update to reflect the filtered count. For example, if the user searches "diesel" and 4 results show, the label should read: "Showing 4 of 19 components". When the search is cleared, it should return to "Showing 19 components".

This label should update reactively — whenever the displayed list changes (due to search/filter), the count must reflect the current visible count, not the total database count.

Do not add any pagination navigation buttons in this case — the table should continue to be a single scrollable list as it is now.

Case B — If real server-side pagination IS implemented (API supports page/limit, and only a subset of components is fetched per page):

In this case the "Page X of Y" counter is correct in concept but incomplete in implementation. Do the following:

Keep the "Page X of Y" text but add proper Previous and Next navigation buttons flanking it — a left arrow button (‹ or ←) on the left side of the counter and a right arrow button (› or →) on the right side.

The Previous button should be disabled when the user is on Page 1.

The Next button should be disabled when the user is on the last page.

Additionally, add clickable page number buttons between the Previous and Next arrows — show up to 5 page numbers at a time, with ellipsis (...) if there are more pages. This is the standard pagination pattern.

Style all buttons consistently with the existing app design — use the same border radius, font, and color scheme already in use. Do not introduce new colors or styles.

Also add the total count label described in Case A above (e.g., "Showing 19 components") but place it at the bottom-left, with the pagination controls at the bottom-right. This is the standard table footer layout.

Step 3 — Specific Behavior Requirements (applies to whichever Case is implemented):

The total component count displayed must be dynamically fetched from the actual data — do not hardcode the number 19 or any other fixed number. If components are added or removed from the system in future, the count must update automatically.

The count/pagination area must sit below the table's last visible row, inside or directly beneath the table container — not floating over it or overlapping any other UI element. Currently the "Page" text appears to be overlapping or very close to the PMS Assistant chat button — ensure the layout is clean and there is no overlap.

If the search bar is active and filters the list, the count label must reflect the filtered result count, not the total. Format: "Showing X of Y components" when filtered, "Showing Y components" when unfiltered.

The bottom area of the table should feel like a proper table footer — not an afterthought floating element.

Step 4 — Clean Up:

Remove any leftover code, state variables, or props related to the old broken "Page X of Y" pagination if they are no longer needed after this fix.

If the pagination component is shared with other modules (Work Orders, Spares, etc.), be careful not to break those. Only apply the Running Hours-specific changes to the Running Hours page. If a shared component must be modified, make the changes in a backwards-compatible way.

Do not change anything else on the /pms/running-hrs page — the table columns, search bar, Export button, Bulk Update RH button, individual gear icon modals, and all other functionality must remain exactly as they are.

General Instructions:

Do not touch any other module, route, or page outside of /pms/running-hrs.

Do not change the visual design, color scheme, or layout of any other part of the page.

Prioritize Case A logic unless your investigation confirms that real server-side pagination is already implemented and working.

The end result should be: the confusing "Page 6 of 6" with no buttons is completely gone, replaced with either a clean count label (Case A) or a fully functional pagination control with navigation buttons (Case B).