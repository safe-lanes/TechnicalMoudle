Please implement the following changes in the PMS Work Orders module and the Unplanned Work Order form. These changes must preserve the existing UI design, colors, spacing, and overall PMS look and feel. This is a workflow and behavior enhancement, not a redesign.

1) Add a new Work Orders tab: Unplanned
Add one more tab in the Work Orders module called Unplanned. The tab order must be exactly:

Planned

Due

Overdue

Postponed

Unplanned

Pending Approval

Completed

This new Unplanned tab should display all unplanned work orders that are saved as unplanned records and are not yet routed into approval/completion flow. Please implement the tab in the same style as the existing tabs, with the same visual treatment and behavior pattern used by the current Work Orders tabs.

Please ensure:

The Unplanned tab loads the correct unplanned work orders only.

The tab count badge works correctly if counts are shown for this tab.

Existing search, filters, pagination, export, and row actions continue to work correctly with the Unplanned tab.

The change is integrated cleanly with the current tabs including Pending Approval and Postponed.

2) Freeze the top header area in the Unplanned Work Order form
In the Unplanned Work Order Form, the top red-marked header area must not scroll away when the user scrolls the form. The area that includes the page header section such as:

Back

Work Order Form — Unplanned

Save

Work Instructions

must remain fixed/frozen at the top while the user scrolls through the long form. This should behave like a sticky header inside the form page so that the action buttons and page identity remain visible at all times.

Important:

Keep the current styling exactly the same.

Do not change the layout design of the header.

Only make this top area sticky/fixed during form scrolling.

Ensure it does not overlap form content incorrectly; proper spacing/padding should be maintained below it.

3) Add a Save button for draft
Add a Save button in the top sticky header area of the Unplanned Work Order form, as indicated in the marked screenshot. This Save button must save the work order as a draft only. It must not send the work order to Pending Approval.

Draft save behavior:

Save the current entered data from the Unplanned Work Order form.

Keep it as an unplanned draft/in-progress record.

The saved draft should appear under the new Unplanned tab.

It should not appear in Pending Approval when saved as draft.

The user should be able to reopen and continue editing that draft later.

4) Submit Work Order behavior
Keep the existing Submit Work Order action, but change/confirm its behavior so that only this button sends the work order for approval. When the user clicks Submit Work Order, the record should move into the Pending Approval tab.

Submission behavior:

Save the full work order data.

Change the workflow state/status appropriately for approval.

Make the record visible in Pending Approval.

It should no longer remain only as a draft in the Unplanned tab after submission, unless your workflow intentionally keeps a reference view there; if so, approval status must still be clearly correct.

5) Workflow rule
Please apply this workflow rule clearly for Unplanned Work Orders only:

Save = save as draft, remain under Unplanned tab, do not go to Pending Approval.

Submit Work Order = submit for approval, go to Pending Approval tab.

This distinction must be clear in both frontend behavior and backend/state handling.

6) Data/state handling
Please update the workflow/state logic so the system can distinguish between:

Unplanned draft

Unplanned submitted for approval

If needed, add or reuse a status/state field so the record can be routed correctly between the Unplanned tab and Pending Approval tab. Make sure this state logic is applied consistently for:

tab listing,

counts,

search,

filters,

export,

edit/reopen behavior,

final submission behavior.

7) Scope restriction
These changes must be applied specifically to:

Work Orders tab structure

Unplanned Work Order form workflow

Do not redesign unrelated modules. Do not change the visual appearance of the existing PMS interface.

Acceptance criteria
A new Unplanned tab is added in the exact order: Planned / Due / Overdue / Postponed / Unplanned / Pending Approval / Completed.

Draft unplanned work orders are shown under the Unplanned tab.

The top header area of the Unplanned Work Order form remains fixed while scrolling.

A Save button is available in the top header area.

Clicking Save stores the work order as draft only and does not send it to Pending Approval.

Clicking Submit Work Order sends the work order to Pending Approval.

Existing UI colors, spacing, styling, and design remain unchanged.

Search, filters, pagination, export, and edit flows continue to work correctly with the new Unplanned workflow.