Based on the diagnostic analysis, I need you to fix the horizontal overflow issue in client/src/pages/spares/SparesNew.tsx. The problem is that the CSS Grid table has fixed pixel widths totaling 1000px+ but no horizontal scroll container.

Specific Changes Required:

1. Modify the table container wrapper (Line ~XXX where the grid is defined):

Current code:

tsx
<div className="flex-1 bg-white border border-gray-200 rounded-lg overflow-hidden">
  {/* Grid content */}
</div>
Change to:

tsx
<div className="flex-1 bg-white border border-gray-200 rounded-lg overflow-hidden">
  <div className="overflow-x-auto">
    {/* Grid content */}
  </div>
</div>
2. Update the gridTemplateColumns inline style:

Current code:

tsx
style={{ gridTemplateColumns: FEATURES.IHM 
  ? '90px 130px 150px 100px 80px 50px 50px 60px 100px 50px 140px' 
  : '90px 130px 150px 100px 80px 50px 50px 60px 100px 140px' 
}}
Change to:

tsx
style={{ 
  gridTemplateColumns: FEATURES.IHM 
    ? 'minmax(90px, auto) minmax(130px, 1fr) minmax(150px, 1.5fr) minmax(100px, auto) minmax(80px, auto) minmax(50px, auto) minmax(50px, auto) minmax(60px, auto) minmax(100px, auto) minmax(50px, auto) minmax(140px, auto)' 
    : 'minmax(90px, auto) minmax(130px, 1fr) minmax(150px, 1.5fr) minmax(100px, auto) minmax(80px, auto) minmax(50px, auto) minmax(50px, auto) minmax(60px, auto) minmax(100px, auto) minmax(140px, auto)',
  minWidth: 'max-content'
}}
3. Ensure the grid itself has proper width:

Add the following to the actual grid element (the one with display: grid):

tsx
className="... min-w-max"
Implementation Steps:

Locate the table container div in SparesNew.tsx (around line where you have the grid implementation)

Wrap the grid with <div className="overflow-x-auto">

Update the gridTemplateColumns style to use minmax() for flexible widths

Add minWidth: 'max-content' to the style object

Add min-w-max Tailwind class to the grid element itself

Expected Result:

Table will scroll horizontally within its container when viewport is narrow

Columns will have minimum widths but can expand when space is available

Part Name and Component columns get more space (1fr and 1.5fr)

No content will overflow outside the visible screen

Sidebar remains at 320px, table scrolls independently

Files to modify:

client/src/pages/spares/SparesNew.tsx

Please implement these changes and verify that:

The table scrolls horizontally when the viewport is narrow

All action buttons (View Details, Edit, Consume/Receive, Adjust ROB, Delete) are accessible via horizontal scroll

The component tree sidebar remains fixed at 320px

No page-level horizontal scrolling occurs