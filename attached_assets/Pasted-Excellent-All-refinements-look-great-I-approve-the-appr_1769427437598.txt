Excellent! All refinements look great. I approve the approach with these final critical adjustments:

✅ Final Approval with Critical Tweaks:
1. ChatMessage.tsx - Security Fix
Critical Issue: Using dangerouslySetInnerHTML is a security risk (XSS vulnerability).

Fix: Use a proper markdown library instead:

typescript
// Install: npm install react-markdown
import ReactMarkdown from 'react-markdown';

export function ChatMessage({ message }: ChatMessageProps) {
  // ... existing code
  
  return (
    // ... existing JSX
    <div className={cn(/* ... */)}>
      <ReactMarkdown 
        className="whitespace-pre-wrap prose prose-sm dark:prose-invert max-w-none"
        components={{
          // Custom rendering for links (to enable deep linking)
          a: ({href, children}) => (
            <a href={href} className="text-primary hover:underline" onClick={(e) => {
              if (href?.startsWith('/')) {
                e.preventDefault();
                // Use wouter's navigation
                window.location.href = href;
              }
            }}>
              {children}
            </a>
          )
        }}
      >
        {message.content}
      </ReactMarkdown>
    </div>
  );
}
Or if you want to keep it lightweight without dependencies, at minimum escape HTML:

typescript
function escapeHtml(text: string): string {
  const div = document.createElement('div');
  div.textContent = text;
  return div.innerHTML;
}

function formatContent(content: string): string {
  const escaped = escapeHtml(content);
  return escaped
    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
    .replace(/\n/g, '<br/>');
}
2. chatbotService.ts - Add Date Filtering Logic
For get_due_work_orders with date ranges, add this helper:

typescript
case "get_due_work_orders":
  const dueWOs = await storage.getWorkOrders(args.vesselId);
  const now = new Date();
  
  let filtered = dueWOs.filter(wo => wo.status === "Due");
  
  if (args.dateRange === "week") {
    const weekFromNow = new Date(now.getTime() + 7 * 24 * 60 * 60 * 1000);
    filtered = filtered.filter(wo => 
      wo.dueDate && new Date(wo.dueDate) <= weekFromNow
    );
  } else if (args.dateRange === "month") {
    const monthFromNow = new Date(now.getTime() + 30 * 24 * 60 * 60 * 1000);
    filtered = filtered.filter(wo => 
      wo.dueDate && new Date(wo.dueDate) <= monthFromNow
    );
  }
  
  return filtered;
3. System Prompt - Add Tool Usage Guidance
Add this section to help the model use tools correctly:

typescript
## Tool Usage Guidelines
- ALWAYS call get_work_order_counts first for summary questions like "PMS status"
- Use generate_deep_link when user asks to "show me", "take me to", or "open" something
- After listing items, ALWAYS offer to navigate or show details
- Combine multiple tool calls when needed (e.g., overdue WOs + low stock spares for priority analysis)
- If a work order number/ID is mentioned, call get_work_order_detail
4. useChat.ts - Fix Auth Integration
Change this line to properly get user role:

typescript
import { useAuth } from "@/hooks/useAuth"; // Adjust import based on your auth hook

export function useChat() {
  const { vesselId, vessels } = useVessel();
  const { currentUser } = useAuth(); // Add this
  
  // ... later in sendMessage:
  const context: ChatContext = {
    vesselId,
    vesselName: currentVessel?.name,
    currentPage,
    userRole: currentUser?.role || "Ship"
  };
5. App.tsx Integration - Show Me the Code
Please provide the exact code changes for:

Mounting chatbot router in server/routes.ts:

typescript
import chatbotRouter from "./routes/chatbot";
// ... where in the file should this go?
app.use(chatbotRouter); // Or is it mounted differently?
Adding to client/src/App.tsx:

typescript
import { ChatButton } from "@/components/chat/ChatButton";

// Where exactly should <ChatButton /> be rendered?
// Should it be inside a specific component or at the root level?
6. OpenAI Integration Setup
Please confirm the exact OpenAI initialization for Replit:

typescript
import OpenAI from "openai";

// Is this correct for Replit's integration?
const openai = new OpenAI({
  apiKey: process.env.AI_INTEGRATIONS_OPENAI_API_KEY
});

// Or does Replit require a different initialization?
7. Error Handling - Add to chatbotService.ts
Wrap the OpenAI calls with proper error handling:

typescript
try {
  let response = await openai.chat.completions.create({
    model: "gpt-4o",
    messages,
    tools: CHATBOT_TOOLS,
    tool_choice: "auto"
  });
  // ... rest of logic
} catch (error) {
  console.error("OpenAI API error:", error);
  
  // Fallback response
  return {
    response: "I'm having trouble connecting to my AI service right now. Please try again in a moment, or contact your system administrator if this persists.",
    toolsUsed: []
  };
}
Final Checklist Before Build:
 Fix dangerouslySetInnerHTML security issue in ChatMessage

 Add date filtering logic for get_due_work_orders

 Add tool usage guidance to system prompt

 Properly integrate user auth in useChat hook

 Show exact App.tsx and routes.ts integration code

 Confirm OpenAI initialization for Replit

 Add error handling wrapper

Once you address these 7 items and provide the integration code snippets (items 5 & 6), I give FULL APPROVAL to proceed with the complete build.