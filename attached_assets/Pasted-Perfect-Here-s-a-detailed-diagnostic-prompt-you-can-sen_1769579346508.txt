Perfect! Here's a detailed diagnostic prompt you can send to Replit AI to gather information about the current UI implementation:

PROMPT FOR REPLIT AI (DIAGNOSTIC INQUIRY):

I'm experiencing responsive layout issues on the Spares Inventory page (/spares) where content is overflowing horizontally and requiring scrolling. Before fixing it, I need to understand the current implementation. Please analyze and provide the following details:

1. Layout Structure:

What is the current HTML structure for the main content area containing the spares table?

How is the layout organized? (Flexbox, Grid, fixed widths?)

What are the parent containers wrapping the data table?

Is there a sidebar layout, and if yes, what's the width allocation?

2. Table Container Details:

What component/element is being used for the data table? (native table, custom component, library component?)

What are the current width properties set on the table and its container?

Is there an overflow property set anywhere? If yes, where and what value?

What CSS classes are applied to the table container?

3. Column Width Configuration:

How are column widths currently defined? (fixed px, %, flex, auto?)

List the width values for each column: Part Code, Part Name, Component, Part Number, Criticality, ROB, Min, Stock, Location, IHM, Actions

Are there any min-width or max-width constraints?

4. Responsive Design:

Are there any media queries currently implemented for this page?

What breakpoints are defined (if any)?

Is there mobile/tablet specific styling?

5. Viewport and Container Constraints:

What is the max-width of the main content area?

Are there any viewport width (vw) or viewport height (vh) units being used?

What's the relationship between the sidebar width and main content width?

6. CSS Framework/Libraries:

What CSS framework or component library is being used? (Tailwind, Material-UI, Bootstrap, custom CSS?)

Are you using any table-specific libraries (React Table, AG Grid, etc.)?

7. Current CSS Properties:

Show me the relevant CSS/styling code for:

The main page container

The table wrapper/container

The table itself

The column definitions

8. File Locations:

Which files contain the styling for the Spares Inventory page?

Where is the table component defined?

Please provide code snippets for the relevant sections so I can understand exactly how the layout is currently implemented before we proceed with fixes.