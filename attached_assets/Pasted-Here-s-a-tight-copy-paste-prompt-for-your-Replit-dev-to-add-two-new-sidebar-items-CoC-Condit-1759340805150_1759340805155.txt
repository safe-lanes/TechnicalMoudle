Here’s a tight, copy-paste prompt for your Replit dev to **add two new sidebar items**—**CoC (Condition of Class)** and **Recurring Defects**—without touching existing UI or flows.

---

# Replit Task — Defects Sidebar: Add **CoC** and **Recurring Defects**

## 🔒 Do NOT change

* Keep **existing styling, spacing, typography, icon weight, colors, and hover/active behavior** identical to the current sidebar (Dashboard / Defect Log / Reports).
* Do not alter the Defect Log or Reports functionality.

## ✅ What to add (navigation only; no business logic yet)

Add two items to the **left vertical sidebar** (under Defects module):

1. **CoC**

   * Label: `CoC`
   * Sub-label (small, muted): `Condition of Class`
   * Icon: use the same icon set already used in the app; pick a caution/flag/triangle icon consistent with our set.
   * Route: `/defects/coc`
   * Active state styling must match existing “active” pill/highlight behavior.
   * Keyboard accessible (Enter/Space triggers), proper `aria-current="page"` when active.

2. **Recurring Defects**

   * Label: `Recurring Defects`
   * Icon: repeat/refresh loop icon (same icon family as above).
   * Route: `/defects/recurring`
   * Same active/hover/ARIA/keyboard behavior as CoC.

### Order in sidebar

```
Dashboard
Defect Log
CoC
Recurring Defects
Reports
```

## 🧭 Routing & placeholders

* Register routes:

  * `/defects/coc`
  * `/defects/recurring`
* For **now**, render lightweight placeholder pages that preserve our app shell (top bar, breadcrumbs if any, page container width, standard h1).

  * **CoC page H1:** `Condition of Class (CoC)`
    Subtext (muted): `Setup pending – UI placeholders only`
  * **Recurring page H1:** `Recurring Defects`
    Subtext (muted): `Setup pending – UI placeholders only`
* Add a standard empty-state panel with our neutral illustration style and a brief line:

  * CoC: `This section will track active CoC items and their closure.`
  * Recurring: `This section will list defects flagged as recurring and their root-cause status.`
* Include a top-right **“Back to Defect Log”** text button that routes to `/defects`.

## 🧩 Reuse components

* Reuse the **SidebarItem** component used for Dashboard/Defect Log/Reports:

  * Same props (icon, label, sublabel, route).
  * Same tooltip behavior (if present).
* Reuse the **PageContainer** for content area spacing.

## 🔐 Permissions (simple for now)

* Both pages visible to all authenticated users (same visibility as Defect Log).
* Hook into existing auth guard; if unauthenticated, redirect to login as usual.

## ♿ Accessibility

* Each sidebar item must be focusable, navigable via keyboard, and have:

  * `role="link"` (or semantic `<a>`),
  * `aria-label` including the sublabel (e.g., `aria-label="CoC, Condition of Class"`),
  * `aria-current="page"` on active route.

## 🧱 Feature flags (optional)

* If a global `features.defects.coc` or `features.defects.recurring` flag exists, respect it; otherwise, render items unconditionally.

## 🧪 Smoke tests

* Clicking **CoC** highlights the item and loads `/defects/coc`.
* Clicking **Recurring Defects** highlights the item and loads `/defects/recurring`.
* Browser refresh on those routes returns to the correct placeholder.
* Tab/Enter activates items; screen reader announces active state.

## 📦 Deliverables

* Sidebar items added with correct order and icons.
* Routes and placeholder pages wired.
* No visual regressions to existing items (Dashboard / Defect Log / Reports).

---

> Do not implement CoC or Recurring logic yet—**navigation scaffolding only** with perfect visual parity to existing sidebar.
