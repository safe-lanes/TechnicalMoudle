Here’s a clean, copy-paste **prompt for Replit** to implement the **Immediate Cause** flow only. It focuses on functionality and data—**do not change any colors, spacing, fonts, or label placement**.

---

## PROMPT FOR REPLIT — Implement “Immediate Cause” selection

**Scope**
Implement the full workflow for **Immediate Cause** only:

* Open the **Immediate Cause** modal when the **“Select”** button (under *Immediate Cause* in the *Cause Analysis* section) is clicked.
* Allow multi-select of causes in two groups: **Unsafe Act** and **Unsafe Condition**.
* Persist the selected items to the defect record in the database.
* On modal **Save**, close the modal and render the selected items back on the main form under **Immediate Cause** (in the text area).
* Do **not** modify any UI styling or layout (colors, fonts, spacing, label positions). Functionality only.

---

### UX & behavior (must match exactly)

1. **Open modal**

   * Clicking the **Immediate Cause → Select** button opens the modal titled **“Immediate Cause”** (this modal already exists in the design).
   * Modal content shows two columns: **Unsafe Act** (left) and **Unsafe Condition** (right), each with a checklist.

2. **Select**

   * User can tick **multiple** checkboxes across both columns.
   * Keep the item order exactly as shown in the reference modal.

3. **Save / Cancel**

   * **Save**: write the selected values to the defect’s **immediateCause** field in DB, close modal, and populate the **Immediate Cause** text area on the main form with the selected items (one per line, in this format):

     ```
     [Unsafe Act] Non conformance to Legislative rules or regulations
     [Unsafe Act] Improper use of Tools / Equipment
     [Unsafe Condition] Maintenance / Repair Error
     ```

     * Preserve the group tag (`[Unsafe Act]` / `[Unsafe Condition]`) exactly.
   * **Cancel / close**: close modal without changing stored selections.

4. **Re-open & edit**

   * If the user reopens the modal, **pre-check** the previously saved items.
   * On save, overwrite the stored list and re-render the text area.

5. **Validation**

   * No hard validation required to save the main form, but if **Critical** is checked elsewhere and business rules require an immediate cause, surface a **non-blocking warning** below the Immediate Cause text area: “Immediate Cause is recommended for critical defects.”

6. **No UI restyle**

   * Do **not** change button shapes, colors, fonts, borders, spacing, placeholder style, or label positions anywhere on the page.

---

### Data model (use this shape)

Add (or use if present) to the **Defect** entity:

```json
{
  "id": "string",
  "immediateCause": {
    "unsafeAct": [
      "Non conformance to Legislative rules or regulations",
      "Improper use of Tools / Equipment"
    ],
    "unsafeCondition": [
      "Maintenance / Repair Error"
    ]
  }
}
```

* Store **exact strings** as seen in the modal labels.
* If your backend prefers flat storage, also compute a flat list:

```json
{
  "immediateCauseFlat": [
    "Unsafe Act|Non conformance to Legislative rules or regulations",
    "Unsafe Act|Improper use of Tools / Equipment",
    "Unsafe Condition|Maintenance / Repair Error"
  ]
}
```

---

### API (adjust names to your stack, but keep behavior)

* **GET** `/api/defects/:id` → returns current defect including `immediateCause`.
* **PUT/PATCH** `/api/defects/:id/immediate-cause`

  ```json
  {
    "unsafeAct": ["..."],
    "unsafeCondition": ["..."]
  }
  ```

  * Response returns the updated object.

Implement optimistic update on the client, but reconcile with server response.

---

### Front-end implementation (suggested)

* Component for the **Select** button listens for click → opens **ImmediateCauseModal**.

* **ImmediateCauseModal**:

  * Props: `open`, `initialSelection`, `onSave(selection)`, `onClose()`.
  * Internal state: `{ unsafeAct: Set<string>, unsafeCondition: Set<string> }`.
  * On mount, pre-populate from `initialSelection`.
  * On **Save**:

    1. Call API to persist.
    2. On success, call parent `onSave(updatedSelection)` and close.

* Main form:

  * Holds `immediateCause` state.
  * Renders to the **Immediate Cause** textarea as described (group tag + label, one per line, read-only).
  * If no selection yet, keep the placeholder “IMMEDIATE CAUSE”.

---

### Edge cases

* If the user unchecks everything and saves → store empty arrays; textarea becomes empty (show placeholder).
* Long lists: textarea should remain read-only and auto-height as per current style; **do not** change control type or styling.
* Accessibility: ensure checkboxes are keyboard navigable and labelled; modal must trap focus; Escape closes without saving.

---

### Test checklist (must pass)

* Clicking **Select** opens the correct modal.
* Selecting multiple items across both columns → **Save** writes to DB and renders formatted list in textarea.
* Re-opening shows the previous selection **checked**.
* Saving with no items clears textarea and DB.
* No visual regressions (colors, fonts, spacing, borders, label positions) anywhere on the form.

---

**Out of scope for this task:** Root Cause, any styling changes, severity logic, or other sections.

Please implement exactly as above.
