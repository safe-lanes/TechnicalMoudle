We now have an Excel test case sheet named pms_lead_grace_test_cases.xlsx which defines all required test cases for the new Lead Time & Grace Period logic. Your task now is only testing, not changing any business logic or backend code. Important constraints: Do not modify any business rules or calculation logic, even if a test fails. Do not “fix” anything during this task. Perform actual, UI-driven testing using the running PMS app (Work Orders, Admin settings, etc.), not by reasoning from code alone. Use only real vessels Vessel 4 and Vessel 11 exactly as referenced in the Excel. Follow the test steps in the sheet exactly as written. What you must do Open the pms_lead_grace_test_cases.xlsx file created earlier. For each test case row: Read Precondition / Setup, Test Steps, and Expected Result. Execute those steps in the UI and with live data (and, where needed, verify logs to confirm status recalculation). Do not adjust underlying logic; only change configuration and data as described in the preconditions (e.g., company standard settings, vessel mode, lead/grace values). For each test case, update the following columns in the Excel: Status: “Pass” or “Fail”. Actual Result: What you actually observed (status, tab, date, marker, etc.). Defect / Observation: If Pass: note any minor quirks. If Fail: describe clearly what went wrong (e.g., “WO remained in Planned; expected Due when 0 ≤ daysUntilDue ≤ leadDays”), and reference any relevant log lines or screenshots. If a test cannot be executed because required data does not exist (e.g., no WO with that exact due/remaining condition): Mark Status as “Blocked”. In Defect / Observation, clearly state why (e.g., “No calendar WO on Vessel 4 due in last 7 days; needs seeded data or date adjustment”). Do not change any logic to force it to pass. Absolutely out of scope for this task No code changes in: status calculation, company standard logic, vessel override logic, generation logic, lead or grace rule functions. No new features. No refactors. This is test execution only. If you discover any defects while executing the test cases: Record them only in the Excel (Defect / Observation column) with maximum clarity. Do not attempt to fix them in this task. Deliverables At the end of this task, you must provide: The updated Excel file with: Status, Actual Result, and Defect / Observation filled for every test case (or marked Blocked with explanation). A short summary listing: Number of test cases Passed, Failed, Blocked. A brief bullet list of the most critical failures found (if any), referencing the Test Case IDs. Remember: the purpose of this task is to validate how the system behaves today, not to adjust logic. Any fixes will be handled as separate tasks after we review your completed test report.