Raplit Prompt – UI Change Only
Bulk Update Spare – Transaction Mode Selector
Module

PMS → Inventory → Bulk Update Spare

🎯 Objective

Improve UI clarity in the Bulk Update Spare screen by introducing a Transaction Mode selector (Consume / Receive) at the top.

This is strictly a UI-only change.

No backend modification

No logic change

No calculation change

No database change

All existing processing must be reused.

📌 Scope of Change (UI Only)
1️⃣ New UI Control – Transaction Mode

Add dropdown at top of screen:

Field	Value
Label	Mode of Transaction
Type	Dropdown
Options	Consume / Receive
Rules:

User must select mode before interacting with grid

No default auto-processing logic

Only affects UI labeling

2️⃣ Existing UI Controls – No Change

The following must remain exactly as-is:

Upper Search Bar

Received Place

Transaction Date

Comments

No repositioning.
No renaming.
No logic modification.

📊 3️⃣ Grid Layout (Required Table Format)

The grid must be displayed exactly in the following structure:

			Current Stock (ROB)		Consumed / Received		
Part Code	Part Name	Part Number	Location A / ROB	Location B / ROB	Location A / ROB	Location B / ROB	New ROB
Column Behavior
Part Identification

Part Code (Read-only)

Part Name (Read-only)

Part Number (Read-only)

Current Stock (ROB)

Location A / ROB → Read-only

Location B / ROB → Read-only

Transaction Entry Section

Location A / ROB → Editable

Location B / ROB → Editable

New ROB

Display calculated value (using existing backend logic)

Read-only field

Must follow existing computation rules

🔄 Dynamic Label Rule

The header:

Consumed / Received


Must dynamically change based on selected mode:

Mode Selected	Column Header Text
Consume	Consumed
Receive	Received

⚠ Label-level change only.
No logic-level branching.

⚙️ Functional Behavior (Reuse Existing Logic)

When user selects:

Consume

On Save → Call existing bulk consume logic

Receive

On Save → Call existing bulk receive logic

System must:

Use current APIs

Use current validation rules

Use current ROB update logic

No new API calls.
No additional conditional backend coding.

✅ Expected Outcome

Clear transaction intent visibility

Clean and structured bulk entry table

Same backend behavior

Reduced operational confusion

Consistent inventory UX

🚫 Strict Non-Impact Constraints

This UI change must NOT:

Modify backend APIs

Change inventory calculation logic

Change ROB update mechanism

Alter validation rules

Add new database columns

Affect audit/history logs

Modify approval workflows

Introduce new consume/receive processes