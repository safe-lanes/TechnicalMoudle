/**
 * STUB — jspdf-autotable is temporarily unavailable (jspdf dependency is blocked).
 * PDF export features will show a user-facing error until the package is restored.
 */

export default function autoTable(_doc: unknown, _opts: unknown): void {
  console.warn('[jspdf-autotable stub] autoTable is unavailable — jspdf is blocked by the security policy.');
}
