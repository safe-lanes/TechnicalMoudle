import type { ClientSideRowModelStage, GridOptions, IRowGroupStage, NamedBean, NestedDataGetter, RowNode, StageExecuteParams } from 'ag-grid-community';
import { BeanStub } from 'ag-grid-community';
export declare class GroupStage<TData> extends BeanStub implements NamedBean, IRowGroupStage {
    beanName: "groupStage";
    step: ClientSideRowModelStage;
    readonly refreshProps: (keyof GridOptions<any>)[];
    treeData: boolean;
    private hasTreeData;
    private needReset;
    private nested;
    private strategy;
    postConstruct(): void;
    destroy(): void;
    getNode(id: string): RowNode<TData> | undefined;
    getNestedDataGetter(): NestedDataGetter<TData> | null | undefined;
    onPropChange(changedProps: ReadonlySet<keyof GridOptions<any>>): boolean;
    extractData(): TData[];
    execute(params: StageExecuteParams<TData>): boolean | undefined;
    loadLeafs(node: RowNode): RowNode[] | null;
    private getStrategy;
}
